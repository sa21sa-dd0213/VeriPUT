// SPDX-License-Identifier: MIT
// Auto-generated by VeriPUT after ESBMC emitted no concrete test.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {ConfiguratorProxy} from "../src/flat.sol";

contract ConfiguratorProxyCovTest_ConfiguratorProxy_upgradeToAndCall_put3p1 is Test {
  ConfiguratorProxy c0;
  function setUp() public {
    // path-cov fixture: constructor skipped by ESBMC
    address _esbmc_fixture_c0 = address(uint160(1337));
    vm.etch(_esbmc_fixture_c0, type(ConfiguratorProxy).runtimeCode);
    c0 = ConfiguratorProxy(payable(_esbmc_fixture_c0));
  }
  // claim: sol:@C@ConfiguratorProxy@F@upgradeToAndCall#505:path:3

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@ConfiguratorProxy@F@upgradeToAndCall#505:path:3   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   data.length in [0, 18446744073709551615]
  //   msg.sender in [1, 4294967295]
  //   newImplementation in [0, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 0
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 0
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet', '--path-cov-fixture', '<host-path>']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'16': 'msg.sender==0', '17': 'msg.sender==0', '20': 'msg.sender==0', '21': 'msg.sender==0', '3': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, newImplementation, data
  // ORACLE: none emitted -- NOT ONE RUNG HOLDS:
  // no candidate held over the certified region, so there is nothing to render.
  // The exit-kind expectation below is still an assertion.
  //   path guard DROPPED: 'msg.sender == return_value$__getAdmin$1' (`return_value$__getAdmin$1` is not nameable in this PUT)
  //   environment ESTABLISHED: msg.sender in [1, 4294967295] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: block.timestamp == 0 is ESTABLISHED with `vm.warp(0)` before the target call
  //   environment ESTABLISHED: block.number == 0 is ESTABLISHED with `vm.roll(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, address newImplementation, bytes memory data) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 4294967295)));
    newImplementation = address(uint160(bound(uint256(uint160(newImplementation)), 0, 1461501637330902918203684832716283019655932542975)));
    vm.assume(uint256(data.length) <= 18446744073709551615);
    // VeriPUT synthesized this call from source declarations; the
    // certified region and all oracle claims still come from ESBMC.
    vm.warp(0);
    vm.roll(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    c0.upgradeToAndCall(newImplementation, data);
    // VERIPUT_FIXED_REPLAY_ASSERTIONS // R0 is already region-wide
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_ConfiguratorProxy_upgradeToAndCall_path3p1(address p_msg_sender, address newImplementation, bytes memory data) public {
    _veriput_parameterized(p_msg_sender, newImplementation, data);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { ConfiguratorProxyCovTest_ConfiguratorProxy_upgradeToAndCall_concrete3p1__basis_root__W _veriput_w = new ConfiguratorProxyCovTest_ConfiguratorProxy_upgradeToAndCall_concrete3p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by VeriPUT after ESBMC emitted no concrete test.
contract ConfiguratorProxyCovTest_ConfiguratorProxy_upgradeToAndCall_concrete3p1__basis_root__W is Test {
  ConfiguratorProxy c0;
  function setUp() public {
    // path-cov fixture: constructor skipped by ESBMC
    address _esbmc_fixture_c0 = address(uint160(1337));
    vm.etch(_esbmc_fixture_c0, type(ConfiguratorProxy).runtimeCode);
    c0 = ConfiguratorProxy(payable(_esbmc_fixture_c0));
  }
  // claim: sol:@C@ConfiguratorProxy@F@upgradeToAndCall#505:path:3
  // witness-fingerprint-sha256: d60850ec193aadcdefb4b5bf4a4433170758d2e85f0dfb5e5f32a7812cdbffaf
  function _w_test_cov_0() public {
    // VeriPUT synthesized this call from source declarations; the
    // certified region and all oracle claims still come from ESBMC.
    vm.warp(0);
    vm.roll(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(4294967295)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    c0.upgradeToAndCall(address(uint160(0)), hex"");
    _veriput_concrete_completed = true;
    assertTrue(_veriput_concrete_completed, "fixed witness call must complete");
  }
}
