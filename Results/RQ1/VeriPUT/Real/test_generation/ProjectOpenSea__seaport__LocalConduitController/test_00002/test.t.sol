// SPDX-License-Identifier: MIT
// Auto-generated by VeriPUT after ESBMC emitted no concrete test.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {LocalConduitController} from "../src/flat.sol";

contract LocalConduitControllerCovTest_LocalConduitController_acceptOwnership_put2p1 is Test {
  LocalConduitController c0;
  function setUp() public {
    c0 = new LocalConduitController();
  }
  // claim: sol:@C@LocalConduitController@F@acceptOwnership#646:path:2

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@LocalConduitController@F@acceptOwnership#646:path:2   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': '(none)', '15': 'msg.sender==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, msg.value, conduit
  // ORACLE: rollback exit plus 9 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (and funded with `vm.deal(p_msg_sender, p_msg_value)`, because the call sends value and the sender pays) (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: the compiler-only nonpayable reject replay was rewritten as a low-level value-bearing call taking the bound() fuzz parameter `p_msg_value`
  function _veriput_parameterized(address p_msg_sender, uint256 p_msg_value, address conduit) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    p_msg_value = bound(p_msg_value, 1, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    conduit = address(uint160(bound(uint256(uint160(conduit)), 0, 1461501637330902918203684832716283019655932542975)));
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_conduits_conduit__owner = (uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(conduit, uint256(0)))) + 1))) & 1461501637330902918203684832716283019655932542975);
    uint256 _pre_conduits_conduit__potentialOwner = (uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(conduit, uint256(0)))) + 2))) & 1461501637330902918203684832716283019655932542975);
    uint256 _pre_conduits_conduit__key = uint256(vm.load(address(c0), keccak256(abi.encode(conduit, uint256(0)))));
    // VeriPUT synthesized this call from source declarations; the
    // certified region and all oracle claims still come from ESBMC.
    vm.deal(p_msg_sender, p_msg_value);
    vm.prank(p_msg_sender);
    (bool _esbmc_value_gate_ok, ) = address(c0).call{value: p_msg_value}(abi.encodeWithSignature("acceptOwnership(address)", conduit));
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_conduits_conduit__owner = (uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(conduit, uint256(0)))) + 1))) & 1461501637330902918203684832716283019655932542975);
    uint256 _post_conduits_conduit__potentialOwner = (uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(conduit, uint256(0)))) + 2))) & 1461501637330902918203684832716283019655932542975);
    uint256 _post_conduits_conduit__key = uint256(vm.load(address(c0), keccak256(abi.encode(conduit, uint256(0)))));
    assertEq(_post_conduits_conduit__owner, _pre_conduits_conduit__owner, "_conduits[conduit].owner: post == pre");
    assertEq(_post_conduits_conduit__potentialOwner, _pre_conduits_conduit__potentialOwner, "_conduits[conduit].potentialOwner: post == pre");
    assertEq(_post_conduits_conduit__key, _pre_conduits_conduit__key, "_conduits[conduit].key: post == pre");
    assertGe(_post_conduits_conduit__owner, _pre_conduits_conduit__owner, "_conduits[conduit].owner: post >= pre");
    assertLe(_post_conduits_conduit__owner, _pre_conduits_conduit__owner, "_conduits[conduit].owner: post <= pre");
    assertGe(_post_conduits_conduit__potentialOwner, _pre_conduits_conduit__potentialOwner, "_conduits[conduit].potentialOwner: post >= pre");
    assertLe(_post_conduits_conduit__potentialOwner, _pre_conduits_conduit__potentialOwner, "_conduits[conduit].potentialOwner: post <= pre");
    assertGe(_post_conduits_conduit__key, _pre_conduits_conduit__key, "_conduits[conduit].key: post >= pre");
    assertLe(_post_conduits_conduit__key, _pre_conduits_conduit__key, "_conduits[conduit].key: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_esbmc_value_gate_ok, "value sent to a non-payable entry must revert");

    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    // Structural ABI-gate replay: R0 is already asserted over the certified nonzero-value region.
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_LocalConduitController_acceptOwnership_path2p1(address p_msg_sender, uint256 p_msg_value, address conduit) public {
    _veriput_parameterized(p_msg_sender, p_msg_value, conduit);
  }
}
