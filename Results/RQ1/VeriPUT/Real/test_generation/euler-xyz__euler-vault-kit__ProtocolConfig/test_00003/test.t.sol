// SPDX-License-Identifier: MIT
// Auto-generated by VeriPUT after ESBMC emitted no concrete test.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {ProtocolConfig} from "../src/flat.sol";

contract ProtocolConfigCovTest_ProtocolConfig_protocolFeeConfig_put2p1 is Test {
  ProtocolConfig c0;
  function setUp() public {
    c0 = new ProtocolConfig(address(uint160(1000)), address(uint160(1001)));
  }
  // claim: sol:@C@ProtocolConfig@F@protocolFeeConfig#236:path:2

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@ProtocolConfig@F@protocolFeeConfig#236:path:2   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'7': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, msg.value, vault
  // ORACLE: rollback exit plus 13 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   rung DROPPED: all return rungs DROPPED: the `retlive` witness came back HOLDS, not REFUTED -- no execution of this path was shown to reach a return, so every other return rung holds VACUOUSLY
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (and funded with `vm.deal(p_msg_sender, p_msg_value)`, because the call sends value and the sender pays) (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: the compiler-only nonpayable reject replay was rewritten as a low-level value-bearing call taking the bound() fuzz parameter `p_msg_value`
  // 10 snapshot local(s) HOISTED to storage: 3 fuzz parameter(s) + 10 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_feeReceiver; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_protocolFeeShare; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_protocolFeeConfig_vault__exists; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_protocolFeeConfig_vault__feeReceiver; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_protocolFeeConfig_vault__protocolFeeShare; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_feeReceiver; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_protocolFeeShare; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_protocolFeeConfig_vault__exists; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_protocolFeeConfig_vault__feeReceiver; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_protocolFeeConfig_vault__protocolFeeShare; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, uint256 p_msg_value, address vault) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    p_msg_value = bound(p_msg_value, 1, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vault = address(uint160(bound(uint256(uint160(vault)), 0, 1461501637330902918203684832716283019655932542975)));
    // pre-state for the oracle, at this path's own entry
    _pre_feeReceiver = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 1461501637330902918203684832716283019655932542975);
    _pre_protocolFeeShare = ((uint256(vm.load(address(c0), bytes32(uint256(1)))) >> 160) & 65535);
    _pre_protocolFeeConfig_vault__exists = (uint256(vm.load(address(c0), keccak256(abi.encode(vault, uint256(3))))) & 255);
    _pre_protocolFeeConfig_vault__feeReceiver = ((uint256(vm.load(address(c0), keccak256(abi.encode(vault, uint256(3))))) >> 8) & 1461501637330902918203684832716283019655932542975);
    _pre_protocolFeeConfig_vault__protocolFeeShare = ((uint256(vm.load(address(c0), keccak256(abi.encode(vault, uint256(3))))) >> 168) & 65535);
    // VeriPUT synthesized this call from source declarations; the
    // certified region and all oracle claims still come from ESBMC.
    vm.deal(p_msg_sender, p_msg_value);
    vm.prank(p_msg_sender);
    (bool _esbmc_value_gate_ok, ) = address(c0).call{value: p_msg_value}(abi.encodeWithSignature("protocolFeeConfig(address)", vault));
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_feeReceiver = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 1461501637330902918203684832716283019655932542975);
    _post_protocolFeeShare = ((uint256(vm.load(address(c0), bytes32(uint256(1)))) >> 160) & 65535);
    _post_protocolFeeConfig_vault__exists = (uint256(vm.load(address(c0), keccak256(abi.encode(vault, uint256(3))))) & 255);
    _post_protocolFeeConfig_vault__feeReceiver = ((uint256(vm.load(address(c0), keccak256(abi.encode(vault, uint256(3))))) >> 8) & 1461501637330902918203684832716283019655932542975);
    _post_protocolFeeConfig_vault__protocolFeeShare = ((uint256(vm.load(address(c0), keccak256(abi.encode(vault, uint256(3))))) >> 168) & 65535);
    assertEq(_post_feeReceiver, _pre_feeReceiver, "feeReceiver: post == pre");
    assertEq(_post_protocolFeeShare, _pre_protocolFeeShare, "protocolFeeShare: post == pre");
    assertEq(_post_protocolFeeConfig_vault__exists, _pre_protocolFeeConfig_vault__exists, "_protocolFeeConfig[vault].exists: post == pre");
    assertEq(_post_protocolFeeConfig_vault__feeReceiver, _pre_protocolFeeConfig_vault__feeReceiver, "_protocolFeeConfig[vault].feeReceiver: post == pre");
    assertEq(_post_protocolFeeConfig_vault__protocolFeeShare, _pre_protocolFeeConfig_vault__protocolFeeShare, "_protocolFeeConfig[vault].protocolFeeShare: post == pre");
    assertGe(_post_feeReceiver, _pre_feeReceiver, "feeReceiver: post >= pre");
    assertLe(_post_feeReceiver, _pre_feeReceiver, "feeReceiver: post <= pre");
    assertGe(_post_protocolFeeShare, _pre_protocolFeeShare, "protocolFeeShare: post >= pre");
    assertLe(_post_protocolFeeShare, _pre_protocolFeeShare, "protocolFeeShare: post <= pre");
    assertGe(_post_protocolFeeConfig_vault__feeReceiver, _pre_protocolFeeConfig_vault__feeReceiver, "_protocolFeeConfig[vault].feeReceiver: post >= pre");
    assertLe(_post_protocolFeeConfig_vault__feeReceiver, _pre_protocolFeeConfig_vault__feeReceiver, "_protocolFeeConfig[vault].feeReceiver: post <= pre");
    assertGe(_post_protocolFeeConfig_vault__protocolFeeShare, _pre_protocolFeeConfig_vault__protocolFeeShare, "_protocolFeeConfig[vault].protocolFeeShare: post >= pre");
    assertLe(_post_protocolFeeConfig_vault__protocolFeeShare, _pre_protocolFeeConfig_vault__protocolFeeShare, "_protocolFeeConfig[vault].protocolFeeShare: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_esbmc_value_gate_ok, "value sent to a non-payable entry must revert");

    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    // Structural ABI-gate replay: R0 is already asserted over the certified nonzero-value region.
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_ProtocolConfig_protocolFeeConfig_path2p1(address p_msg_sender, uint256 p_msg_value, address vault) public {
    _veriput_parameterized(p_msg_sender, p_msg_value, vault);
  }
}
