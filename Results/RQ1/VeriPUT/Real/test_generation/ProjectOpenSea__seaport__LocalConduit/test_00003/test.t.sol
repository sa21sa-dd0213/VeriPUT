// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {LocalConduit} from "../src/flat.sol";

contract LocalConduitCovTest_0_LocalConduit_updateChannel_put14p1 is Test {
  LocalConduit c0;
  function setUp() public {
    vm.startPrank(address(uint160(4294967295)), address(uint160(4294967295)));
    c0 = new LocalConduit();
    vm.stopPrank();
  }
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:15
  // witness-fingerprint-sha256: fd6ab79a34898e016876941d6529d5759e8b13cf9183e75d8878244fdf09bca6
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:14
  // witness-fingerprint-sha256: 8bd554b9daacd3ba6ad90640050d959375e047feb734f53cf8b1e6a972b16b96

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:14   depth=3
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   channel in [0, 1461501637330902918203684832716283019655932542975]
  //   isOpen in [0, 0]
  //   msg.sender in [4294967295, 4294967295]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN state._controller$458 == 4294967295   [NOT ESTABLISHED -- see the dropped-bound line]
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': 'isOpen==0, msg.sender==4294967295', '15': 'isOpen==1, msg.sender==4294967295'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: channel, isOpen
  // ORACLE: rollback exit plus 1 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   path guard ESTABLISHED: `!((_channels$462[channel]) == isOpen)` -> `vm.assume(_pre_channels_462_channel == 0);`
  //   entry-state bound DROPPED: state._controller$458 (no storage slot: solc's layout does not list it, so it is a constant/immutable and no test can set it)
  //   environment ESTABLISHED: msg.sender == 4294967295 is ESTABLISHED: the governing vm.prank was rewritten to the certified value. The emitted case's own sender came from a different query's counterexample and is not the value this region is a statement about (replacing `vm.prank(address(uint160(4294967295)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address channel, bool isOpen) internal {
    channel = address(uint160(bound(uint256(uint160(channel)), 0, 1461501637330902918203684832716283019655932542975)));
    isOpen = false;
    // VERIPUT_FIXED_REPLAY_STATE_PROJECTION {"state._controller$458":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":4294967295}}
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_channels_channel = (uint256(vm.load(address(c0), keccak256(abi.encode(channel, uint256(0))))) & 255);
    uint256 _pre_channels_462_channel = (uint256(vm.load(address(c0), keccak256(abi.encode(channel, uint256(0))))) & 255);
    // complete-path guard recovered from the emit report
    vm.assume(_pre_channels_462_channel == 0);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(4294967295)));
    // [revert-tolerant] outcome not asserted
    bool _put_ok = true;
    try c0.updateChannel(channel, isOpen) {} catch { _put_ok = false; }
    // VERIPUT_FIXED_REPLAY_ASSERTIONS // R0 is already region-wide
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_channels_channel = (uint256(vm.load(address(c0), keccak256(abi.encode(channel, uint256(0))))) & 255);
    assertEq(_post_channels_channel, _pre_channels_channel, "_channels[channel]: post == pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_put_ok, "path enc=14p1 exits through a REVERT: the call must fail on the unmodified contract");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_LocalConduit_updateChannel_path14p1(address channel, bool isOpen) public {
    _veriput_parameterized(channel, isOpen);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { LocalConduitCovTest_0_LocalConduit_updateChannel_concrete14p1__basis_root__W _veriput_w = new LocalConduitCovTest_0_LocalConduit_updateChannel_concrete14p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_1(); }
}
}

contract LocalConduitCovTest_1 is Test {
  LocalConduit c0;
  function setUp() public {
    vm.startPrank(address(uint160(2147483647)), address(uint160(2147483647)));
    c0 = new LocalConduit();
    vm.stopPrank();
  }
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:6
  // witness-fingerprint-sha256: 35a2f2455a85d53df3d3ba11e52f2e979f2ecbdd14a830ef7c83b6d6c6590bfa
}

contract LocalConduitCovTest_2 is Test {
  LocalConduit c0;
  function setUp() public {
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new LocalConduit();
    vm.stopPrank();
  }
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:2
  // witness-fingerprint-sha256: 6757e38c903b1dcefc892b87e480811793ff1711af1ff97b0779e12573bd76dc
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract LocalConduitCovTest_0_LocalConduit_updateChannel_concrete14p1__basis_root__W is Test {
  LocalConduit c0;
  function setUp() public {
    vm.startPrank(address(uint160(4294967295)), address(uint160(4294967295)));
    c0 = new LocalConduit();
    vm.stopPrank();
  }
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:15
  // witness-fingerprint-sha256: fd6ab79a34898e016876941d6529d5759e8b13cf9183e75d8878244fdf09bca6
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:14
  // witness-fingerprint-sha256: 8bd554b9daacd3ba6ad90640050d959375e047feb734f53cf8b1e6a972b16b96
  function _w_test_cov_1() public {
    // VERIPUT_FIXED_REPLAY_STATE_PROJECTION {"state._controller$458":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":4294967295}}
    // [revert-tolerant] outcome not asserted
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(4294967295)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    try c0.updateChannel(address(uint160(0)), false) {
      _veriput_concrete_completed = true;
    } catch {}
    assertFalse(_veriput_concrete_completed, "fixed witness call must revert");
  }
}

contract LocalConduitCovTest_1__W is Test {
  LocalConduit c0;
  function setUp() public {
    vm.startPrank(address(uint160(2147483647)), address(uint160(2147483647)));
    c0 = new LocalConduit();
    vm.stopPrank();
  }
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:6
  // witness-fingerprint-sha256: 35a2f2455a85d53df3d3ba11e52f2e979f2ecbdd14a830ef7c83b6d6c6590bfa
}

contract LocalConduitCovTest_2__W is Test {
  LocalConduit c0;
  function setUp() public {
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new LocalConduit();
    vm.stopPrank();
  }
  // claim: sol:@C@LocalConduit@F@updateChannel#634:path:2
  // witness-fingerprint-sha256: 6757e38c903b1dcefc892b87e480811793ff1711af1ff97b0779e12573bd76dc
}
