// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test, Vm} from "forge-std/Test.sol";
import {MarketUpdateTimelock} from "../src/flat.sol";

contract MarketUpdateTimelockCovTest_0_MarketUpdateTimelock_setDelay_put31p1_p1_part_part0_w_w is Test {
  MarketUpdateTimelock c0;
  function setUp() public {
    c0 = new MarketUpdateTimelock(address(uint160(4294967295)), 2592000);
  }
  // claim: sol:@C@MarketUpdateTimelock@F@setDelay#148:path:31
  // witness-fingerprint-sha256: b15891e3d87080b984f488579f591c5988d15f33c9b89f7204e6d293266e2f50
  // claim: sol:@C@MarketUpdateTimelock@F@setDelay#148:path:30
  // witness-fingerprint-sha256: 06f56d39484af71f21c381a186bb74c2abbbceb3b23c5296bc2de6afdb26db52
  // claim: sol:@C@MarketUpdateTimelock@F@setDelay#148:path:14
  // witness-fingerprint-sha256: fdf9212add4338f9558625c718a07547d7c95fb28523a132d3763d333a67ff44

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@MarketUpdateTimelock@F@setDelay#148:path:31   depth=4
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   delay_ in [172806, 2592000]
  //   msg.sender in [4294967295, 4294967295]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639935
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639935
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': '(none)', '30': '(none)', '31': '(none)', '6': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = True
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: delay_
  // ORACLE: 43 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 43 over POST-STATE, read through vm.load at
  // the slot solc reports, and 0 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): governor: post >= pre (entailed by a stronger rung that also HOLDS on governor, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): governor: post <= pre (entailed by a stronger rung that also HOLDS on governor, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): governor: post == (pre + msg.value) (entailed by a stronger rung that also HOLDS on governor, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): governor: post in [4294967295, 4294967295] (entailed by a stronger rung that also HOLDS on governor, so asserting it detects nothing the stronger one misses)
  //   path guard ESTABLISHED: `!(msg.sender == governor)` -> `vm.assume(4294967295 == _pre_governor);`
  //   path guard DROPPED: '!(delay_ >= MINIMUM_DELAY)' (`MINIMUM_DELAY` is not nameable in this PUT)
  //   path guard DROPPED: '!(delay_ <= MAXIMUM_DELAY)' (`MAXIMUM_DELAY` is not nameable in this PUT)
  //   environment ESTABLISHED: msg.sender == 4294967295 is ESTABLISHED: the governing vm.prank was rewritten to the certified value. The emitted case's own sender came from a different query's counterexample and is not the value this region is a statement about (replacing `vm.prank(address(uint160(4294967295)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639935 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639935 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(uint256 delay_) internal {
    delay_ = bound(delay_, 172806, 2592000);
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_delay = uint256(vm.load(address(c0), bytes32(uint256(2))));
    uint256 _pre_governor = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    // complete-path guard recovered from the emit report
    vm.assume(4294967295 == _pre_governor);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(4294967295)));
    // [asserted] path exits normally; a revert fails the test
    vm.recordLogs();
    c0.setDelay(delay_);
    Vm.Log[] memory _veriputFixedLogs = vm.getRecordedLogs();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (delay_ == uint256(2592000)) {
      assertEq(_veriputFixedLogs.length, 1);
      assertEq(_veriputFixedLogs[0].emitter, address(c0));
      assertEq(_veriputFixedLogs[0].topics.length, 2);
      assertEq(_veriputFixedLogs[0].topics[0], keccak256("SetDelay(uint256)"));
      assertEq(_veriputFixedLogs[0].topics[1], bytes32(uint256(uint256(2592000))));
      assertEq(_veriputFixedLogs[0].data, abi.encode());
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(2)))), uint256(2592000), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(4294967295), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(1)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_delay = uint256(vm.load(address(c0), bytes32(uint256(2))));
    uint256 _post_governor = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    assertGe(_post_delay, 172800, "delay: post in [172800, 2592000]");
    assertLe(_post_delay, 2592000, "delay: post in [172800, 2592000]");
    assertGe(_post_delay, 172800, "delay: post in [172800, 115792089237316195423570985008687907853269984665640564039457584007913129639935]");
    assertLe(_post_delay, 115792089237316195423570985008687907853269984665640564039457584007913129639935, "delay: post in [172800, 115792089237316195423570985008687907853269984665640564039457584007913129639935]");
    assertGe(_post_delay, 0, "delay: post in [0, 2592000]");
    assertLe(_post_delay, 2592000, "delay: post in [0, 2592000]");
    assertEq(_post_governor, _pre_governor, "governor: post == pre");
    assertEq(_post_delay, delay_, "delay: post == delay_");
    assertEq(_post_governor, uint256(uint160(address(uint160(4294967295)))), "governor: post == msg.sender");
    assertGe(_post_governor, 0, "governor: post in [0, pre]");
    assertLe(_post_governor, _pre_governor, "governor: post in [0, pre]");
    assertGe(_post_governor, 0, "governor: post in [0, 4294967295]");
    assertLe(_post_governor, 4294967295, "governor: post in [0, 4294967295]");
    assertGe(_post_governor, 0, "governor: post in [0, (pre + pre)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) + uint256(_pre_governor)), "governor: post in [0, (pre + pre)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre * pre)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) * uint256(_pre_governor)), "governor: post in [0, (pre * pre)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre + delay_)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) + uint256(delay_)), "governor: post in [0, (pre + delay_)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre * delay_)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) * uint256(delay_)), "governor: post in [0, (pre * delay_)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre + 172800)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) + uint256(172800)), "governor: post in [0, (pre + 172800)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre * 172800)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) * uint256(172800)), "governor: post in [0, (pre * 172800)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre + 2592000)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) + uint256(2592000)), "governor: post in [0, (pre + 2592000)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre * 2592000)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) * uint256(2592000)), "governor: post in [0, (pre * 2592000)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre + 4294967295)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) + uint256(4294967295)), "governor: post in [0, (pre + 4294967295)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (pre * 4294967295)]");
    unchecked { assertLe(_post_governor, (uint256(_pre_governor) * uint256(4294967295)), "governor: post in [0, (pre * 4294967295)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (delay_ * delay_)]");
    unchecked { assertLe(_post_governor, (uint256(delay_) * uint256(delay_)), "governor: post in [0, (delay_ * delay_)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (delay_ * 172800)]");
    unchecked { assertLe(_post_governor, (uint256(delay_) * uint256(172800)), "governor: post in [0, (delay_ * 172800)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (delay_ * 2592000)]");
    unchecked { assertLe(_post_governor, (uint256(delay_) * uint256(2592000)), "governor: post in [0, (delay_ * 2592000)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (delay_ + 4294967295)]");
    unchecked { assertLe(_post_governor, (uint256(delay_) + uint256(4294967295)), "governor: post in [0, (delay_ + 4294967295)]"); }
    assertGe(_post_governor, 0, "governor: post in [0, (delay_ * 4294967295)]");
    unchecked { assertLe(_post_governor, (uint256(delay_) * uint256(4294967295)), "governor: post in [0, (delay_ * 4294967295)]"); }
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_MarketUpdateTimelock_setDelay_path31p1_part_part0_w_w(uint256 delay_) public {
    _veriput_parameterized(delay_);
  }
}

contract MarketUpdateTimelockCovTest_1 is Test {
  MarketUpdateTimelock c0;
  function setUp() public {
    c0 = new MarketUpdateTimelock(address(uint160(1461501637330902918203684832716283019655932542975)), 2592000);
  }
  // claim: sol:@C@MarketUpdateTimelock@F@setDelay#148:path:6
  // witness-fingerprint-sha256: bbdf24b0210bfd0bef326edc712b065b8d3cc911d0c2370bac66e1e3886048c2
  // claim: sol:@C@MarketUpdateTimelock@F@setDelay#148:path:2
  // witness-fingerprint-sha256: 532ce25fa6ab376a5c8b5b38bf676db5012331088cc9050bc61fc0f23adc8816
}
