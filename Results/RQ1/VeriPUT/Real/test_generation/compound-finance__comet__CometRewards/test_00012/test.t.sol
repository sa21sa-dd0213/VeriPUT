// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {CometRewards} from "../src/flat.sol";

contract CometRewardsCovTest_0_CometRewards_transferGovernor_put6p1_p1_part_part0_w_r is Test {
  CometRewards c0;
  function setUp() public {
    c0 = new CometRewards(address(uint160(4294967295)));
  }
  // claim: sol:@C@CometRewards@F@transferGovernor#314:path:7
  // witness-fingerprint-sha256: af3a89aae8ab059cd2e6d4db9d1a183a529439d99dba85b88c8b49b894833164
  // claim: sol:@C@CometRewards@F@transferGovernor#314:path:6
  // witness-fingerprint-sha256: d32710504e31d441ba885b0ec2b1c4fdd83e29ace3972c56745d1186b114175d

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@CometRewards@F@transferGovernor#314:path:6   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [2147483648, 3221225471]
  //   newGovernor in [0, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 57896044618658097711785492504343953926634992332820282019728792003956564819969
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 57896044618658097711785492504343953926634992332820282019728792003956564819969
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, newGovernor
  // ORACLE: rollback exit plus 3 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   path guard DROPPED: '!(msg.sender != governor)' (`governor` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   environment ESTABLISHED: msg.sender in [2147483648, 3221225471] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(4294967294)));`)
  //   environment ESTABLISHED: block.timestamp == 57896044618658097711785492504343953926634992332820282019728792003956564819969 is ESTABLISHED with `vm.warp(57896044618658097711785492504343953926634992332820282019728792003956564819969)` before the target call
  //   environment ESTABLISHED: block.number == 57896044618658097711785492504343953926634992332820282019728792003956564819969 is ESTABLISHED with `vm.roll(57896044618658097711785492504343953926634992332820282019728792003956564819969)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, address newGovernor) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 2147483648, 3221225471)));
    newGovernor = address(uint160(bound(uint256(uint160(newGovernor)), 0, 1461501637330902918203684832716283019655932542975)));
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_governor = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    vm.warp(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.roll(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [revert-tolerant] outcome not asserted
    bool _put_ok = true;
    try c0.transferGovernor(newGovernor) {} catch { _put_ok = false; }
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(3221225471)) && newGovernor == address(uint160(0))) {
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_governor = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_post_governor, _pre_governor, "governor: post == pre");
    assertGe(_post_governor, _pre_governor, "governor: post >= pre");
    assertLe(_post_governor, _pre_governor, "governor: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_put_ok, "path enc=6p1_part_part0_w_r exits through a REVERT: the call must fail on the unmodified contract");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_CometRewards_transferGovernor_path6p1_part_part0_w_r(address p_msg_sender, address newGovernor) public {
    _veriput_parameterized(p_msg_sender, newGovernor);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { CometRewardsCovTest_0_CometRewards_transferGovernor_concrete6p1__basis_part0_w_r__W _veriput_w = new CometRewardsCovTest_0_CometRewards_transferGovernor_concrete6p1__basis_part0_w_r__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_1(); }
}
}

contract CometRewardsCovTest_1 is Test {
  CometRewards c0;
  function setUp() public {
    c0 = new CometRewards(address(uint160(0)));
  }
  // claim: sol:@C@CometRewards@F@transferGovernor#314:path:2
  // witness-fingerprint-sha256: cc0366e735b307a60dc2dca439f1276e0eacd9b17109384d49417072fa1fe885
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract CometRewardsCovTest_0_CometRewards_transferGovernor_concrete6p1__basis_part0_w_r__W is Test {
  CometRewards c0;
  function setUp() public {
    c0 = new CometRewards(address(uint160(4294967295)));
  }
  // claim: sol:@C@CometRewards@F@transferGovernor#314:path:7
  // witness-fingerprint-sha256: af3a89aae8ab059cd2e6d4db9d1a183a529439d99dba85b88c8b49b894833164
  // claim: sol:@C@CometRewards@F@transferGovernor#314:path:6
  // witness-fingerprint-sha256: d32710504e31d441ba885b0ec2b1c4fdd83e29ace3972c56745d1186b114175d
  function _w_test_cov_1() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(0) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "entry pin state.governor did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // VERIPUT_FIXED_REPLAY_STATE_PROJECTION {"state.FACTOR_SCALE$30":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":1000000000000000000}}
    // [revert-tolerant] outcome not asserted
    vm.warp(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.roll(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(3221225471)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    try c0.transferGovernor(address(uint160(0))) {
      _veriput_concrete_completed = true;
    } catch {}
    assertFalse(_veriput_concrete_completed, "fixed witness call must revert");
    uint256 _veriput_fixed_state_governor_1 = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_governor_1, uint256(0), "fixed witness state");
  }
}

contract CometRewardsCovTest_1__W is Test {
  CometRewards c0;
  function setUp() public {
    c0 = new CometRewards(address(uint160(0)));
  }
  // claim: sol:@C@CometRewards@F@transferGovernor#314:path:2
  // witness-fingerprint-sha256: cc0366e735b307a60dc2dca439f1276e0eacd9b17109384d49417072fa1fe885
}
