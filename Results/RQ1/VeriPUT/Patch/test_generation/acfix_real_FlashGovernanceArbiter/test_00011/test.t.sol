// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {FlashGovernanceArbiter} from "../src/flat.sol";

contract FlashGovernanceArbiterCovTest_FlashGovernanceArbiter_enforceToleranceInt_concrete7p1 is Test {
  FlashGovernanceArbiter c0;
  function setUp() public {
    c0 = new FlashGovernanceArbiter(address(uint160(0)));
  }
  // claim: sol:@C@FlashGovernanceArbiter@F@enforceToleranceInt#888:path:7
  // witness-fingerprint-sha256: 722e39dccb70d8bf27eaf87ca0214f2a5add4b4a45151aba92d3bc757011091e
  function test_cov_0() public {
    vm.prank(address(uint160(0)));
    // [revert-tolerant] outcome not asserted
    bool _veriput_concrete_completed = false;
    try c0.enforceToleranceInt(0, 0) {
      _veriput_concrete_completed = true;
    } catch {}
    assertTrue(_veriput_concrete_completed, "fixed witness call must complete");
  }
  // claim: sol:@C@FlashGovernanceArbiter@F@enforceToleranceInt#888:path:2
  // witness-fingerprint-sha256: 014e61fc863512fb213b2b7b1a6b03bf663f1e063721d98ee1fffbe36f7e8739
}
