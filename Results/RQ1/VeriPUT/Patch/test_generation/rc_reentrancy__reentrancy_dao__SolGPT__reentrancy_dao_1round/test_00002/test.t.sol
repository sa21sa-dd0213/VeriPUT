// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {ReentrancyDAO} from "../src/flat.sol";

contract ReentrancyDAOCovTest_ReentrancyDAO_withdrawAll_put7p1_p1_part_part0_r is Test {
  ReentrancyDAO c0;
  function setUp() public {
    c0 = new ReentrancyDAO();
  }
  // claim: sol:@C@ReentrancyDAO@F@withdrawAll#48:path:7
  // witness-fingerprint-sha256: b02791206726643c87044abfc8fdd4c617c1b468d465ea3b5c229f3a8cccf8ab
  // claim: sol:@C@ReentrancyDAO@F@withdrawAll#48:path:2
  // witness-fingerprint-sha256: 3789628d5a416de3c18559dac2b6d11255dfd1c570e9d6cb861ca90eb51f9c19

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@ReentrancyDAO@F@withdrawAll#48:path:7   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [4294967294, 50499692625]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639935
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639935
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'7': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender
  // ORACLE: 6 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 6 over POST-STATE, read through vm.load at
  // the slot solc reports, and 0 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): balance: post >= pre (entailed by a stronger rung that also HOLDS on balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balance: post <= pre (entailed by a stronger rung that also HOLDS on balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): credit[msg.sender]: post >= pre (entailed by a stronger rung that also HOLDS on credit[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): credit[msg.sender]: post <= pre (entailed by a stronger rung that also HOLDS on credit[msg.sender], so asserting it detects nothing the stronger one misses)
  //   path guard DROPPED: 'oCredit > 0' (`oCredit` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   environment ESTABLISHED: msg.sender in [4294967294, 50499692625] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639935 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639935 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 4294967294, 50499692625)));
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_balance = uint256(vm.load(address(c0), bytes32(uint256(1))));
    uint256 _pre_credit_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(0)))));
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [asserted] path exits normally; a revert fails the test
    c0.withdrawAll();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(4294967294))) {
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(1)))), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_balance = uint256(vm.load(address(c0), bytes32(uint256(1))));
    uint256 _post_credit_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(0)))));
    assertEq(_post_balance, _pre_balance, "balance: post == pre");
    assertEq(_post_credit_msg_sender, _pre_credit_msg_sender, "credit[msg.sender]: post == pre");
    assertGe(_pre_balance, _post_balance, "balance: pre - post in [state.credit[msg.sender], state.credit[msg.sender]] with pre >= post");
    unchecked { assertGe(_pre_balance - _post_balance, _pre_credit_msg_sender, "balance: pre - post in [state.credit[msg.sender], state.credit[msg.sender]] with pre >= post"); }
    unchecked { assertLe(_pre_balance - _post_balance, _pre_credit_msg_sender, "balance: pre - post in [state.credit[msg.sender], state.credit[msg.sender]] with pre >= post"); }
    assertEq(_post_credit_msg_sender, 0, "credit[msg.sender]: post == 0");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_ReentrancyDAO_withdrawAll_path7p1_part_part0_r(address p_msg_sender) public {
    _veriput_parameterized(p_msg_sender);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { ReentrancyDAOCovTest_ReentrancyDAO_withdrawAll_concrete7p1__basis_part0_r__W _veriput_w = new ReentrancyDAOCovTest_ReentrancyDAO_withdrawAll_concrete7p1__basis_part0_r__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract ReentrancyDAOCovTest_ReentrancyDAO_withdrawAll_concrete7p1__basis_part0_r__W is Test {
  ReentrancyDAO c0;
  function setUp() public {
    c0 = new ReentrancyDAO();
  }
  // claim: sol:@C@ReentrancyDAO@F@withdrawAll#48:path:7
  // witness-fingerprint-sha256: b02791206726643c87044abfc8fdd4c617c1b468d465ea3b5c229f3a8cccf8ab
  function _w_test_cov_0() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(1))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(1)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(1)))), uint256(0), "entry pin state.balance did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // [asserted] path exits normally; a revert fails the test
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(4294967294)), address(uint160(0)));
    c0.withdrawAll();
    uint256 _veriput_fixed_state_balance_0 = uint256(vm.load(address(c0), bytes32(uint256(1))));
    assertEq(_veriput_fixed_state_balance_0, uint256(0), "fixed witness state");
  }
  // claim: sol:@C@ReentrancyDAO@F@withdrawAll#48:path:2
  // witness-fingerprint-sha256: 3789628d5a416de3c18559dac2b6d11255dfd1c570e9d6cb861ca90eb51f9c19
}
