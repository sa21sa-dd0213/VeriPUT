// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {Cooler} from "../src/flat.sol";

contract CoolerCovTest_Cooler_approveTransfer_put6p1 is Test {
  Cooler c0;
  function setUp() public {
    c0 = new Cooler();
  }
  // claim: sol:@C@Cooler@F@approveTransfer#1916:path:7
  // witness-fingerprint-sha256: db82eaf82ee45e2c4a86229bad01c9ac18e7386a9f298377d0bc0b213e5f790b
  // claim: sol:@C@Cooler@F@approveTransfer#1916:path:6
  // witness-fingerprint-sha256: 09b57c2e744ad4aea3c69584b71a0024eac8623b1f96e06ac4d0cc4f8187db98
  // claim: sol:@C@Cooler@F@approveTransfer#1916:path:2
  // witness-fingerprint-sha256: a1820fe3d4ab83234c64e7bfdb89ff6f22b4ae1c606a22a50bdceaf3b10be6ae

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@Cooler@F@approveTransfer#1916:path:6   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   loanID_ in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   msg.sender in [1, 4294967296]
  //   to_ in [0, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 57896044618658097711785492504343953926634992332820282019728792003956564819969
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 57896044618658097711785492504343953926634992332820282019728792003956564819969
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'6': '(none)', '7': 'msg.sender==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, to_, loanID_
  // ORACLE: rollback exit plus 3 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   rung DROPPED: state.loans[return_value$__ESBMC_dynarr_idx$1].lender (`loans` is not a mapping solc's layout reports with a scalar value, so the R2 endpoint cannot be read before the call)
  //   path guard DROPPED: '!(msg.sender != loans[return_value$__ESBMC_dynarr_idx$1].lender)' (`loans[return_value$__ESBMC_dynarr_idx$1].lender` is not nameable in this PUT)
  //   environment ESTABLISHED: msg.sender in [1, 4294967296] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(1)));`)
  //   environment ESTABLISHED: block.timestamp == 57896044618658097711785492504343953926634992332820282019728792003956564819969 is ESTABLISHED with `vm.warp(57896044618658097711785492504343953926634992332820282019728792003956564819969)` before the target call
  //   environment ESTABLISHED: block.number == 57896044618658097711785492504343953926634992332820282019728792003956564819969 is ESTABLISHED with `vm.roll(57896044618658097711785492504343953926634992332820282019728792003956564819969)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, address to_, uint256 loanID_) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 4294967296)));
    to_ = address(uint160(bound(uint256(uint160(to_)), 0, 1461501637330902918203684832716283019655932542975)));
    loanID_ = bound(loanID_, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_approvals_loanID = (uint256(vm.load(address(c0), keccak256(abi.encode(loanID_, uint256(2))))) & 1461501637330902918203684832716283019655932542975);
    vm.warp(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.roll(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [revert-tolerant] outcome not asserted
    bool _put_ok = true;
    try c0.approveTransfer(to_, loanID_) {} catch { _put_ok = false; }
    // VERIPUT_FIXED_REPLAY_ASSERTIONS // R0 is already region-wide
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_approvals_loanID = (uint256(vm.load(address(c0), keccak256(abi.encode(loanID_, uint256(2))))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_post_approvals_loanID, _pre_approvals_loanID, "approvals[loanID_]: post == pre");
    assertGe(_post_approvals_loanID, _pre_approvals_loanID, "approvals[loanID_]: post >= pre");
    assertLe(_post_approvals_loanID, _pre_approvals_loanID, "approvals[loanID_]: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_put_ok, "path enc=6p1 exits through a REVERT: the call must fail on the unmodified contract");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_Cooler_approveTransfer_path6p1(address p_msg_sender, address to_, uint256 loanID_) public {
    _veriput_parameterized(p_msg_sender, to_, loanID_);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { CoolerCovTest_Cooler_approveTransfer_concrete6p1__basis_root__W _veriput_w = new CoolerCovTest_Cooler_approveTransfer_concrete6p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_1(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract CoolerCovTest_Cooler_approveTransfer_concrete6p1__basis_root__W is Test {
  Cooler c0;
  function setUp() public {
    c0 = new Cooler();
  }
  // claim: sol:@C@Cooler@F@approveTransfer#1916:path:7
  // witness-fingerprint-sha256: db82eaf82ee45e2c4a86229bad01c9ac18e7386a9f298377d0bc0b213e5f790b
  // claim: sol:@C@Cooler@F@approveTransfer#1916:path:6
  // witness-fingerprint-sha256: 09b57c2e744ad4aea3c69584b71a0024eac8623b1f96e06ac4d0cc4f8187db98
  function _w_test_cov_1() public {
    // VERIPUT_FIXED_REPLAY_STATE_PROJECTION {"state.DECIMALS_INTEREST":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":1000000000000000000}}
    // [revert-tolerant] outcome not asserted
    vm.warp(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.roll(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(1)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    try c0.approveTransfer(address(uint160(0)), uint256(0)) {
      _veriput_concrete_completed = true;
    } catch {}
    assertFalse(_veriput_concrete_completed, "fixed witness call must revert");
  }
  // claim: sol:@C@Cooler@F@approveTransfer#1916:path:2
  // witness-fingerprint-sha256: a1820fe3d4ab83234c64e7bfdb89ff6f22b4ae1c606a22a50bdceaf3b10be6ae
}
