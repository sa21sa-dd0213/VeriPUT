// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {DAO} from "../src/flat.sol";

contract DAOCovTest_DAO_cancelProposal_put6p1 is Test {
  DAO c0;
  function setUp() public {
    c0 = new DAO();
  }
  // claim: sol:@C@DAO@F@cancelProposal#1244:path:6
  // witness-fingerprint-sha256: 39c602413bdca7aaf9fa6ff12bc331421feb8930becb51577b2c8a6252566bd1
  // claim: sol:@C@DAO@F@cancelProposal#1244:path:2
  // witness-fingerprint-sha256: 8ed9b467539faa7a930073c32750fef33f0db5e0d0d99bb3ae433812a10cdfb9

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@DAO@F@cancelProposal#1244:path:6   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   newProposalID in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   oldProposalID in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   state.VAULT in [0, 1461501637330902918203684832716283019655932542975]
  //   state.mapPID_finalising$868[oldProposalID] in [0, 0]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 0
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 0
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'6': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, oldProposalID, newProposalID
  // ORACLE: rollback exit plus 11 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   path guard DROPPED: '!(!mapPID_finalising[oldProposalID])' (contradicts the certified fixed coordinate value; the region assignment is authoritative)
  //   entry-state bound DROPPED: state.VAULT in [0, 1461501637330902918203684832716283019655932542975] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query -- the rungs were proved about the constructor's own value. Establishing a fuzz-chosen value here would test entry states the proof never covered, which is how this PUT came back RED on the unmodified contract. Both endpoints are the type's own limits, so there is not even an in-region check to make: the bound says nothing at all)
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (no prank was present, so one was inserted)
  //   environment ESTABLISHED: block.timestamp == 0 is ESTABLISHED with `vm.warp(0)` before the target call
  //   environment ESTABLISHED: block.number == 0 is ESTABLISHED with `vm.roll(0)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  // 10 snapshot local(s) HOISTED to storage: 3 fuzz parameter(s) + 10 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_VAULT; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_mapPID_finalising_868_oldProposalID; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_mapPID_finalising_oldProposalID; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_mapPID_votes_newProposalID; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_mapPID_votes_oldProposalID; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_VAULT; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_mapPID_finalising_868_oldProposalID; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_mapPID_finalising_oldProposalID; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_mapPID_votes_newProposalID; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_mapPID_votes_oldProposalID; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, uint256 oldProposalID, uint256 newProposalID) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    oldProposalID = bound(oldProposalID, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    newProposalID = bound(newProposalID, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // entry state the certified region names, ESTABLISHED (not assumed):
    //   state.mapPID_finalising$868[oldProposalID] := 0
    {
      uint256 _w = uint256(vm.load(address(c0), keccak256(abi.encode(oldProposalID, uint256(11)))));
      _w = (_w & ~uint256(255)) | ((uint256(0) & 255) << 0);
      vm.store(address(c0), keccak256(abi.encode(oldProposalID, uint256(11))), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), keccak256(abi.encode(oldProposalID, uint256(11))))) & 255), uint256(0), "entry pin state.mapPID_finalising$868[oldProposalID] did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // pre-state for the oracle, at this path's own entry
    _pre_VAULT = (uint256(vm.load(address(c0), bytes32(uint256(4)))) & 1461501637330902918203684832716283019655932542975);
    _pre_mapPID_finalising_868_oldProposalID = (uint256(vm.load(address(c0), keccak256(abi.encode(oldProposalID, uint256(11))))) & 255);
    _pre_mapPID_finalising_oldProposalID = (uint256(vm.load(address(c0), keccak256(abi.encode(oldProposalID, uint256(11))))) & 255);
    _pre_mapPID_votes_newProposalID = uint256(vm.load(address(c0), keccak256(abi.encode(newProposalID, uint256(9)))));
    _pre_mapPID_votes_oldProposalID = uint256(vm.load(address(c0), keccak256(abi.encode(oldProposalID, uint256(9)))));
    // [revert-tolerant] outcome not asserted
    vm.warp(0);
    vm.roll(0);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    bool _put_ok = true;
    try c0.cancelProposal(oldProposalID, newProposalID) {} catch { _put_ok = false; }
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(0)) && oldProposalID == uint256(0) && newProposalID == uint256(0)) {
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(4)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(5)))), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(1)))), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_VAULT = (uint256(vm.load(address(c0), bytes32(uint256(4)))) & 1461501637330902918203684832716283019655932542975);
    _post_mapPID_finalising_868_oldProposalID = (uint256(vm.load(address(c0), keccak256(abi.encode(oldProposalID, uint256(11))))) & 255);
    _post_mapPID_finalising_oldProposalID = (uint256(vm.load(address(c0), keccak256(abi.encode(oldProposalID, uint256(11))))) & 255);
    _post_mapPID_votes_newProposalID = uint256(vm.load(address(c0), keccak256(abi.encode(newProposalID, uint256(9)))));
    _post_mapPID_votes_oldProposalID = uint256(vm.load(address(c0), keccak256(abi.encode(oldProposalID, uint256(9)))));
    assertEq(_post_VAULT, _pre_VAULT, "VAULT: post == pre");
    assertEq(_post_mapPID_finalising_868_oldProposalID, _pre_mapPID_finalising_868_oldProposalID, "mapPID_finalising$868[oldProposalID]: post == pre");
    assertEq(_post_mapPID_finalising_oldProposalID, _pre_mapPID_finalising_oldProposalID, "mapPID_finalising[oldProposalID]: post == pre");
    assertEq(_post_mapPID_votes_newProposalID, _pre_mapPID_votes_newProposalID, "mapPID_votes[newProposalID]: post == pre");
    assertEq(_post_mapPID_votes_oldProposalID, _pre_mapPID_votes_oldProposalID, "mapPID_votes[oldProposalID]: post == pre");
    assertGe(_post_VAULT, _pre_VAULT, "VAULT: post >= pre");
    assertLe(_post_VAULT, _pre_VAULT, "VAULT: post <= pre");
    assertGe(_post_mapPID_votes_newProposalID, _pre_mapPID_votes_newProposalID, "mapPID_votes[newProposalID]: post >= pre");
    assertLe(_post_mapPID_votes_newProposalID, _pre_mapPID_votes_newProposalID, "mapPID_votes[newProposalID]: post <= pre");
    assertGe(_post_mapPID_votes_oldProposalID, _pre_mapPID_votes_oldProposalID, "mapPID_votes[oldProposalID]: post >= pre");
    assertLe(_post_mapPID_votes_oldProposalID, _pre_mapPID_votes_oldProposalID, "mapPID_votes[oldProposalID]: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_put_ok, "path enc=6p1 exits through a REVERT: the call must fail on the unmodified contract");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_DAO_cancelProposal_path6p1(address p_msg_sender, uint256 oldProposalID, uint256 newProposalID) public {
    _veriput_parameterized(p_msg_sender, oldProposalID, newProposalID);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { DAOCovTest_DAO_cancelProposal_concrete6p1__basis_root__W _veriput_w = new DAOCovTest_DAO_cancelProposal_concrete6p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract DAOCovTest_DAO_cancelProposal_concrete6p1__basis_root__W is Test {
  DAO c0;
  function setUp() public {
    c0 = new DAO();
  }
  // claim: sol:@C@DAO@F@cancelProposal#1244:path:6
  // witness-fingerprint-sha256: 39c602413bdca7aaf9fa6ff12bc331421feb8930becb51577b2c8a6252566bd1
  function _w_test_cov_0() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(3))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(0) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(3)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "entry pin state.USDV did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(2))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(0) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(2)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "entry pin state.VADER did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(4))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(0) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(4)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(4)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "entry pin state.VAULT did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(5))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(5)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(5)))), uint256(0), "entry pin state.coolOffPeriod did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(255)) | ((uint256(0) & 255) << 0);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255), uint256(0), "entry pin state.inited did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(1))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(1)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(1)))), uint256(0), "entry pin state.proposalCount did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // [revert-tolerant] outcome not asserted
    vm.warp(0);
    vm.roll(0);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(0)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    try c0.cancelProposal(uint256(0), uint256(0)) {
      _veriput_concrete_completed = true;
    } catch {}
    assertFalse(_veriput_concrete_completed, "fixed witness call must revert");
    uint256 _veriput_fixed_state_USDV_0 = (uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_USDV_0, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_VADER_1 = (uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_VADER_1, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_VAULT_2 = (uint256(vm.load(address(c0), bytes32(uint256(4)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_VAULT_2, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_coolOffPeriod_3 = uint256(vm.load(address(c0), bytes32(uint256(5))));
    assertEq(_veriput_fixed_state_coolOffPeriod_3, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_inited_4 = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    assertEq(_veriput_fixed_state_inited_4, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_proposalCount_5 = uint256(vm.load(address(c0), bytes32(uint256(1))));
    assertEq(_veriput_fixed_state_proposalCount_5, uint256(0), "fixed witness state");
  }
  // claim: sol:@C@DAO@F@cancelProposal#1244:path:2
  // witness-fingerprint-sha256: 8ed9b467539faa7a930073c32750fef33f0db5e0d0d99bb3ae433812a10cdfb9
}
