// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {ACCURAL_DEPOSIT} from "../src/flat.sol";

contract ACCURAL_DEPOSITCovTest_ACCURAL_DEPOSIT_Collect_concrete3_fb is Test {
  ACCURAL_DEPOSIT c0;
  function setUp() public {
    c0 = new ACCURAL_DEPOSIT();
    // ESBMC runtime fixture: local Foundry has no code at 0x0486cF65A2F2F3A392CBEa398AFB7F5f0B72FF46.
    address _esbmc_ext_mock_0 = address(0x0486cF65A2F2F3A392CBEa398AFB7F5f0B72FF46);
    vm.etch(_esbmc_ext_mock_0, hex"60006000f3");
    vm.mockCall(_esbmc_ext_mock_0, abi.encodeWithSignature("AddMessage(address,uint256,string)"), bytes(""));
  }
  // claim: sol:@C@ACCURAL_DEPOSIT@F@Collect#155:path:3
  // witness-fingerprint-sha256: 97e0a19c869f088de14e064c759701186a355c27e9ae61256248703b29431d36
  function test_cov_0() public {
    vm.prank(address(uint160(0)));
    // [asserted] path exits normally; a revert fails the test
    c0.Collect(1);
    uint256 _veriput_fixed_state_intitalized_0 = ((uint256(vm.load(address(c0), bytes32(uint256(2)))) >> 160) & 255);
    assertEq(_veriput_fixed_state_intitalized_0, uint256(0), "fixed witness state");
  }
}
