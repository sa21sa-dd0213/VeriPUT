// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {keepMyEther} from "../src/flat.sol";

contract keepMyEtherCovTest_keepMyEther_withdraw_put15p1_p1_part_part0_r is Test {
  keepMyEther c0;
  function setUp() public {
    c0 = new keepMyEther();
    // certified extcall success pin: the value transfer must not fail for want of balance
    vm.deal(address(c0), uint256(1) << 128);
  }
  // claim: sol:@C@keepMyEther@F@withdraw#77:path:15
  // witness-fingerprint-sha256: f178437a90b65e6a70f62a7cdca2db0ffee093593eed3c6df1b21a72efea2673
  // claim: sol:@C@keepMyEther@F@withdraw#77:path:14
  // witness-fingerprint-sha256: 94620ef3654b6fccfa17f1695de3690c82005cae24b3a692994d6e8cac9dc4f1
  // claim: sol:@C@keepMyEther@F@withdraw#77:path:2
  // witness-fingerprint-sha256: a0b347b86e71a9323e99e4967e7f08defff263414d3e7afba295e80b13537706

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@keepMyEther@F@withdraw#77:path:15   depth=3
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [3791923280, 50499692625]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': 'state.__lock_modifier0_lock$11==0', '15': 'state.__lock_modifier0_lock$11==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender
  // ORACLE: 4 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 4 over POST-STATE, read through vm.load at
  // the slot solc reports, and 0 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): balances[msg.sender]: post >= pre (entailed by a stronger rung that also HOLDS on balances[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balances[msg.sender]: post <= pre (entailed by a stronger rung that also HOLDS on balances[msg.sender], so asserting it detects nothing the stronger one misses)
  //   path guard ESTABLISHED: `!(!__lock_modifier0_lock$11)` -> `vm.assume(_pre_lock_modifier0_lock_11 == 0);`
  //   path guard DROPPED: '!__sent_result100' (`__sent_result100` is not nameable in this PUT)
  //   environment ESTABLISHED: msg.sender in [3791923280, 50499692625] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 3791923280, 50499692625)));
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_lock_modifier0_lock = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    uint256 _pre_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    uint256 _pre_lock_modifier0_lock_11 = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    // complete-path guard recovered from the emit report
    vm.assume(_pre_lock_modifier0_lock_11 == 0);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    // certified extcall success pin on msg.sender: the lifted caller must accept a bare call
    assumeNotPrecompile(p_msg_sender);
    vm.assume(p_msg_sender.code.length == 0);
    vm.prank(p_msg_sender);
    // [asserted] path exits normally; a revert fails the test
    c0.withdraw();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(3791923280))) {
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(3791923280)), uint256(1))))), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_lock_modifier0_lock = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    uint256 _post_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    assertEq(_post_lock_modifier0_lock, _pre_lock_modifier0_lock, "__lock_modifier0_lock: post == pre");
    assertEq(_post_balances_msg_sender, _pre_balances_msg_sender, "balances[msg.sender]: post == pre");
    assertEq(_post_lock_modifier0_lock, 0, "__lock_modifier0_lock: post == false");
    assertEq(_post_balances_msg_sender, 0, "balances[msg.sender]: post == 0");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_keepMyEther_withdraw_path15p1_part_part0_r(address p_msg_sender) public {
    _veriput_parameterized(p_msg_sender);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { keepMyEtherCovTest_keepMyEther_withdraw_concrete15p1__basis_part0_r__W _veriput_w = new keepMyEtherCovTest_keepMyEther_withdraw_concrete15p1__basis_part0_r__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract keepMyEtherCovTest_keepMyEther_withdraw_concrete15p1__basis_part0_r__W is Test {
  keepMyEther c0;
  function setUp() public {
    c0 = new keepMyEther();
    // certified extcall success pin: the value transfer must not fail for want of balance
    vm.deal(address(c0), uint256(1) << 128);
  }
  // claim: sol:@C@keepMyEther@F@withdraw#77:path:15
  // witness-fingerprint-sha256: f178437a90b65e6a70f62a7cdca2db0ffee093593eed3c6df1b21a72efea2673
  function _w_test_cov_0() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(255)) | ((uint256(0) & 255) << 0);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255), uint256(0), "entry pin state.__lock_modifier0_lock$11 did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // [asserted] path exits normally; a revert fails the test
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(3791923280)), address(uint160(0)));
    c0.withdraw();
    uint256 _veriput_fixed_state_lock_modifier0_lock_11_0 = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    assertEq(_veriput_fixed_state_lock_modifier0_lock_11_0, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_balances_3791923280_1 = uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(3791923280)), uint256(1)))));
    assertEq(_veriput_fixed_state_balances_3791923280_1, uint256(0), "fixed witness state");
  }
  // claim: sol:@C@keepMyEther@F@withdraw#77:path:14
  // witness-fingerprint-sha256: 94620ef3654b6fccfa17f1695de3690c82005cae24b3a692994d6e8cac9dc4f1
  // claim: sol:@C@keepMyEther@F@withdraw#77:path:2
  // witness-fingerprint-sha256: a0b347b86e71a9323e99e4967e7f08defff263414d3e7afba295e80b13537706
}
