// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test, Vm} from "forge-std/Test.sol";
import {VaultAdapter} from "../src/flat.sol";

contract VaultAdapterCovTest_VaultAdapter_setLimits_put7p1 is Test {
  VaultAdapter c0;
  function setUp() public {
    c0 = new VaultAdapter();
  }
  // claim: sol:@C@VaultAdapter@F@setLimits#2285:path:6, sol:@C@VaultAdapter@F@setLimits#2285:path:7
  // witness-fingerprint-sha256: e1352237f9ab7f73d881fa5a8bf3de6904eb2bd80203e2286440cef76b01964f
  // claim: sol:@C@VaultAdapter@F@setLimits#2285:path:2
  // witness-fingerprint-sha256: 49ea87977767aa9451976e13c582aca5e64e7eab6b1f68c4b8164462716d0190

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@VaultAdapter@F@setLimits#2285:path:7   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   _maxMultiplier in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   _minMultiplier in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   _rate in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 57896044618658097711785492504343953926634992332820282019728792003956564819969
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 57896044618658097711785492504343953926634992332820282019728792003956564819969
  //   PIN msg.value == 0
  //   PIN state.AccessStorageLocation$1311 == 81451362312688391278139228193936175012705416214812506152872947521953082600192   [NOT ESTABLISHED -- see the dropped-bound line]
  //   PIN state.INITIALIZABLE_STORAGE$974 == 0   [NOT ESTABLISHED -- see the dropped-bound line]
  //   PIN state.VaultAdapterStorageLocation$1545 == 19501336369257708946939779389804098070606822925710035835875274658093540759040   [NOT ESTABLISHED -- see the dropped-bound line]
  //   PIN state.__self$1910 == 0   [NOT ESTABLISHED -- see the dropped-bound line]
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'6': '(none)', '7': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, _maxMultiplier, _minMultiplier, _rate
  // ORACLE: none emitted -- NOT ONE RUNG HOLDS:
  // no candidate held over the certified region, so there is nothing to render.
  // The exit-kind expectation below is still an assertion.
  //   path guard DROPPED: '!hasAccess' (`hasAccess` is not nameable in this PUT)
  //   entry-state bound DROPPED: state.AccessStorageLocation$1311 (no storage slot: solc's layout does not list it, so it is a constant/immutable and no test can set it)
  //   entry-state bound DROPPED: state.INITIALIZABLE_STORAGE$974 (no storage slot: solc's layout does not list it, so it is a constant/immutable and no test can set it)
  //   entry-state bound DROPPED: state.VaultAdapterStorageLocation$1545 (no storage slot: solc's layout does not list it, so it is a constant/immutable and no test can set it)
  //   entry-state bound DROPPED: state.__self$1910 (no storage slot: solc's layout does not list it, so it is a constant/immutable and no test can set it)
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (no prank was present, so one was inserted)
  //   environment ESTABLISHED: block.timestamp == 57896044618658097711785492504343953926634992332820282019728792003956564819969 is ESTABLISHED with `vm.warp(57896044618658097711785492504343953926634992332820282019728792003956564819969)` before the target call
  //   environment ESTABLISHED: block.number == 57896044618658097711785492504343953926634992332820282019728792003956564819969 is ESTABLISHED with `vm.roll(57896044618658097711785492504343953926634992332820282019728792003956564819969)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, uint256 _maxMultiplier, uint256 _minMultiplier, uint256 _rate) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    _maxMultiplier = bound(_maxMultiplier, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    _minMultiplier = bound(_minMultiplier, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    _rate = bound(_rate, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // VERIPUT_FIXED_REPLAY_STATE_PROJECTION {"state.AccessStorageLocation$1311":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":81451362312688391278139228193936175012705416214812506152872947521953082600192},"state.INITIALIZABLE_STORAGE$974":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":0},"state.VaultAdapterStorageLocation$1545":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":19501336369257708946939779389804098070606822925710035835875274658093540759040},"state.__self$1910":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":0}}
    // [asserted] path exits normally; a revert fails the test
    vm.warp(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.roll(57896044618658097711785492504343953926634992332820282019728792003956564819969);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // certified external-call pin on a high-level interface call: the handle read from storage slot 0xb413d65cb88f23816c329284a0d3eb15a99df7963ab7402ade4c5da22bff6b00 answers `checkAccess(bytes4,address,address)` with true
    { address _vp_ec0 = address(uint160(uint256(vm.load(address(c0), bytes32(uint256(0xb413d65cb88f23816c329284a0d3eb15a99df7963ab7402ade4c5da22bff6b00))))));
      vm.mockCall(_vp_ec0, abi.encodeWithSignature("checkAccess(bytes4,address,address)"), abi.encode(true)); }
    vm.recordLogs();
    c0.setLimits(_maxMultiplier, _minMultiplier, _rate);
    Vm.Log[] memory _veriputFixedLogs = vm.getRecordedLogs();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(0)) && _maxMultiplier == uint256(0) && _minMultiplier == uint256(0) && _rate == uint256(0)) {
      assertEq(_veriputFixedLogs.length, 1);
      assertEq(_veriputFixedLogs[0].emitter, address(c0));
      assertEq(_veriputFixedLogs[0].topics.length, 1);
      assertEq(_veriputFixedLogs[0].topics[0], keccak256("SetLimits(uint256,uint256,uint256)"));
      assertEq(_veriputFixedLogs[0].data, abi.encode(uint256(0), uint256(0), uint256(0)));
    }
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_VaultAdapter_setLimits_path7p1(address p_msg_sender, uint256 _maxMultiplier, uint256 _minMultiplier, uint256 _rate) public {
    _veriput_parameterized(p_msg_sender, _maxMultiplier, _minMultiplier, _rate);
  }
}
