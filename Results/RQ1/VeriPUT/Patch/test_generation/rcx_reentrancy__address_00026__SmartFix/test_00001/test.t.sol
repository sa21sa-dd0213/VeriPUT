// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {MY_BANK} from "../src/flat.sol";

contract MY_BANKCovTest_MY_BANK_Collect_concrete3_fb is Test {
  MY_BANK c0;
  function setUp() public {
    c0 = new MY_BANK(address(uint160(0)));
    // ESBMC runtime fixture: constructor argument `log` is stored in `LogFile`.
    address _esbmc_ctor_state_mock_0_0 = address(uint160(0));
    vm.etch(_esbmc_ctor_state_mock_0_0, hex"60006000f3");
    vm.mockCall(_esbmc_ctor_state_mock_0_0, abi.encodeWithSignature("AddMessage(address,uint256,string)"), bytes(""));
  }
  // claim: sol:@C@MY_BANK@F@Collect#130:path:3
  // witness-fingerprint-sha256: ad3ccc97b7e6787c170f1d81d921f5828471cd7e2dd9be990329eb6c9aa95e46
  function test_cov_0() public {
    vm.warp(1);
    vm.prank(address(uint160(0)));
    // [asserted] path exits normally; a revert fails the test
    bool _veriput_concrete_completed = false;
    c0.Collect(0);
    _veriput_concrete_completed = true;
    assertTrue(_veriput_concrete_completed, "fixed witness call must complete");
  }
}
