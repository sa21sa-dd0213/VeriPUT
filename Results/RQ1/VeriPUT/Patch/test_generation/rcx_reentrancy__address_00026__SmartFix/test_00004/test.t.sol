// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {MY_BANK} from "../src/flat.sol";

contract MY_BANKCovTest_MY_BANK_fallback_put14p1 is Test {
  MY_BANK c0;
  function setUp() public {
    c0 = new MY_BANK(address(uint160(0)));
    // ESBMC runtime fixture: constructor argument `log` is stored in `LogFile`.
    address _esbmc_ctor_state_mock_0_0 = address(uint160(0));
    vm.etch(_esbmc_ctor_state_mock_0_0, hex"60006000f3");
    vm.mockCall(_esbmc_ctor_state_mock_0_0, abi.encodeWithSignature("AddMessage(address,uint256,string)"), bytes(""));
  }
  // claim: sol:@C@MY_BANK@F@fallback#138:path:14
  // witness-fingerprint-sha256: 28729f916f0d4928bb6fd7a91685dd88a37d5a33b535b4389d8f692f0f4a9e89

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@MY_BANK@F@fallback#138:path:14   depth=3
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [0, 50499692625]
  //   state.Acc$148[msg.sender].balance in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 1
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 1
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = False
  //   skip_bracket = True
  // ⚠ NO LADDER AND NO SUBTRACTION RAN FOR THIS ROW. Every interval below rests on
  // a neighbourhood probe, which the work order does NOT accept as a width: a
  // probe shows some nearby value also walks the path, not where the path stops.
  // The region is still CERTIFIED -- an independent query admitted it -- but its
  // WIDTH is not evidence of a measured boundary.
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender
  // ORACLE: 15 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 15 over POST-STATE, read through vm.load at
  // the slot solc reports, and 0 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): Acc$148[msg.sender].balance: post >= pre (entailed by a stronger rung that also HOLDS on Acc$148[msg.sender].balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): Acc$148[msg.sender].balance: post <= pre (entailed by a stronger rung that also HOLDS on Acc$148[msg.sender].balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): Acc[msg.sender].balance: post >= pre (entailed by a stronger rung that also HOLDS on Acc[msg.sender].balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): Acc[msg.sender].balance: post <= pre (entailed by a stronger rung that also HOLDS on Acc[msg.sender].balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): Acc[msg.sender].unlockTime: post != pre (entailed by a stronger rung that also HOLDS on Acc[msg.sender].unlockTime, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): Acc[msg.sender].unlockTime: post >= pre (entailed by a stronger rung that also HOLDS on Acc[msg.sender].unlockTime, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): Acc$148[msg.sender].balance: post == (pre + msg.value) (entailed by a stronger rung that also HOLDS on Acc$148[msg.sender].balance, so asserting it detects nothing the stronger one misses)
  //   path guard DROPPED: 'overflow("+", Acc[msg.sender].balance, msg.value)' (not a simple binary decision)
  //   path guard DROPPED: '!(Acc[msg.sender].balance + msg.value >= Acc[msg.sender].balance)' (`Acc[msg.sender].balance + msg.value` is not nameable in this PUT)
  //   path guard DROPPED: '_unlockTime > block.timestamp' (`_unlockTime` is not nameable in this PUT)
  //   entry-state bound DROPPED: state.Acc$148[msg.sender].balance in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query; both endpoints are the type's own limits, so there is not even an in-region check to make)
  //   environment ESTABLISHED: msg.sender in [0, 50499692625]  \ {0} is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it, and the 1 punched value(s) are excluded by vm.assume; 0 is excluded because Foundry cannot prank address(0), and the remaining executable subregion is a subset of the certified region (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: block.timestamp == 1 is ESTABLISHED with `vm.warp(1)` before the target call
  //   environment ESTABLISHED: block.number == 1 is ESTABLISHED with `vm.roll(1)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 0, 50499692625)));
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_Acc_148_msg_sender__balance = uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(0)))) + 1)));
    uint256 _pre_Acc_msg_sender__balance = uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(0)))) + 1)));
    uint256 _pre_Acc_msg_sender__unlockTime = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(0)))));
    vm.warp(1);
    vm.warp(1);
    vm.roll(1);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    (bool ok1, ) = address(c0).call(hex"deadbeef");
    // VERIPUT_FIXED_REPLAY_ASSERTIONS // R0 is already region-wide
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_Acc_148_msg_sender__balance = uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(0)))) + 1)));
    uint256 _post_Acc_msg_sender__balance = uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(0)))) + 1)));
    uint256 _post_Acc_msg_sender__unlockTime = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(0)))));
    assertEq(_post_Acc_148_msg_sender__balance, _pre_Acc_148_msg_sender__balance, "Acc$148[msg.sender].balance: post == pre");
    assertEq(_post_Acc_msg_sender__balance, _pre_Acc_msg_sender__balance, "Acc[msg.sender].balance: post == pre");
    assertGt(_post_Acc_msg_sender__unlockTime, _pre_Acc_msg_sender__unlockTime, "Acc[msg.sender].unlockTime: post > pre");
    assertGe(_post_Acc_msg_sender__balance, _pre_Acc_msg_sender__balance, "Acc[msg.sender].balance: post - pre in [msg.value, msg.value] with post >= pre");
    unchecked { assertGe(_post_Acc_msg_sender__balance - _pre_Acc_msg_sender__balance, 0, "Acc[msg.sender].balance: post - pre in [msg.value, msg.value] with post >= pre"); }
    unchecked { assertLe(_post_Acc_msg_sender__balance - _pre_Acc_msg_sender__balance, 0, "Acc[msg.sender].balance: post - pre in [msg.value, msg.value] with post >= pre"); }
    unchecked { assertEq(_post_Acc_148_msg_sender__balance, (uint256(_pre_Acc_148_msg_sender__balance) / uint256(1)), "Acc$148[msg.sender].balance: post == (pre / 1)"); }
    assertGe(_post_Acc_148_msg_sender__balance, 0, "Acc$148[msg.sender].balance: post in [0, pre]");
    assertLe(_post_Acc_148_msg_sender__balance, _pre_Acc_148_msg_sender__balance, "Acc$148[msg.sender].balance: post in [0, pre]");
    assertGe(_post_Acc_148_msg_sender__balance, 0, "Acc$148[msg.sender].balance: post in [0, (msg.value + 115792089237316195423570985008687907853269984665640564039457584007913129639935)]");
    unchecked { assertLe(_post_Acc_148_msg_sender__balance, (uint256(0) + uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)), "Acc$148[msg.sender].balance: post in [0, (msg.value + 115792089237316195423570985008687907853269984665640564039457584007913129639935)]"); }
    assertGe(_post_Acc_148_msg_sender__balance, 0, "Acc$148[msg.sender].balance: post in [0, (pre / 1)]");
    unchecked { assertLe(_post_Acc_148_msg_sender__balance, (uint256(_pre_Acc_148_msg_sender__balance) / uint256(1)), "Acc$148[msg.sender].balance: post in [0, (pre / 1)]"); }
    assertGe(_post_Acc_148_msg_sender__balance, 0, "Acc$148[msg.sender].balance: post in [0, (115792089237316195423570985008687907853269984665640564039457584007913129639935 / 1)]");
    unchecked { assertLe(_post_Acc_148_msg_sender__balance, (uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935) / uint256(1)), "Acc$148[msg.sender].balance: post in [0, (115792089237316195423570985008687907853269984665640564039457584007913129639935 / 1)]"); }
    // VERIPUT_ORACLE_REFINEMENT_END
    assertTrue(ok1, "covered receive/fallback path must return normally");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_MY_BANK_fallback_path14p1(address p_msg_sender) public {
    _veriput_parameterized(p_msg_sender);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { MY_BANKCovTest_MY_BANK_fallback_concrete14p1__basis_root__W _veriput_w = new MY_BANKCovTest_MY_BANK_fallback_concrete14p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract MY_BANKCovTest_MY_BANK_fallback_concrete14p1__basis_root__W is Test {
  MY_BANK c0;
  function setUp() public {
    c0 = new MY_BANK(address(uint160(0)));
    // ESBMC runtime fixture: constructor argument `log` is stored in `LogFile`.
    address _esbmc_ctor_state_mock_0_0 = address(uint160(0));
    vm.etch(_esbmc_ctor_state_mock_0_0, hex"60006000f3");
    vm.mockCall(_esbmc_ctor_state_mock_0_0, abi.encodeWithSignature("AddMessage(address,uint256,string)"), bytes(""));
  }
  // claim: sol:@C@MY_BANK@F@fallback#138:path:14
  // witness-fingerprint-sha256: 28729f916f0d4928bb6fd7a91685dd88a37d5a33b535b4389d8f692f0f4a9e89
  function _w_test_cov_0() public {
    vm.warp(1);
    vm.roll(1);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(0)), address(uint160(0)));
    (bool ok1, ) = address(c0).call(hex"deadbeef");
    assertTrue(ok1, "covered receive/fallback path must return normally");
  }
}
