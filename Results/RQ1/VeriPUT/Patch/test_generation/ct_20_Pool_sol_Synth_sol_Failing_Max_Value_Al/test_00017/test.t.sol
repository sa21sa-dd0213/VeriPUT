// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test, Vm} from "forge-std/Test.sol";
import {Synth} from "../src/flat.sol";

contract SynthCovTest_Synth_transfer_put15p1_p1_part_part0_w_w is Test {
  Synth c0;
  function setUp() public {
    vm.warp(1);
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    // ESBMC constructor fixture: local Foundry has no code at constructor argument `_token`.
    address _esbmc_ctor_arg_mock_0_0 = address(uint160(0));
    bytes memory _esbmc_ctor_arg_mock_0_0_code = _esbmc_ctor_arg_mock_0_0.code;
    vm.etch(_esbmc_ctor_arg_mock_0_0, hex"60006000f3");
    vm.mockCall(_esbmc_ctor_arg_mock_0_0, abi.encodeWithSignature("name()"), abi.encode(""));
    // ESBMC constructor fixture: local Foundry has no code at constructor argument `_token`.
    address _esbmc_ctor_arg_mock_0_5 = address(uint160(0));
    bytes memory _esbmc_ctor_arg_mock_0_5_code = _esbmc_ctor_arg_mock_0_5.code;
    vm.etch(_esbmc_ctor_arg_mock_0_5, hex"60006000f3");
    vm.mockCall(_esbmc_ctor_arg_mock_0_5, abi.encodeWithSignature("symbol()"), abi.encode(""));
    // ESBMC constructor fixture: local Foundry has no code at constructor argument `_token`.
    address _esbmc_ctor_arg_mock_0_10 = address(uint160(0));
    bytes memory _esbmc_ctor_arg_mock_0_10_code = _esbmc_ctor_arg_mock_0_10.code;
    vm.etch(_esbmc_ctor_arg_mock_0_10, hex"60006000f3");
    vm.mockCall(_esbmc_ctor_arg_mock_0_10, abi.encodeWithSignature("decimals()"), abi.encode(uint8(1)));
    c0 = new Synth(address(uint160(0)), address(uint160(0)));
    vm.clearMockedCalls();
    vm.etch(_esbmc_ctor_arg_mock_0_10, _esbmc_ctor_arg_mock_0_10_code);
    vm.etch(_esbmc_ctor_arg_mock_0_5, _esbmc_ctor_arg_mock_0_5_code);
    vm.etch(_esbmc_ctor_arg_mock_0_0, _esbmc_ctor_arg_mock_0_0_code);
    vm.stopPrank();
  }
  // claim: sol:@C@Synth@F@transfer#2469:path:15
  // witness-fingerprint-sha256: 30556cd598be742c293d51470653f7c10127e730df54e5cb85d5ce2e55fd284c
  // claim: sol:@C@Synth@F@transfer#2469:path:14
  // witness-fingerprint-sha256: 61a9efafcb2059937abea9d284e636297ae98fe6d5b45e8fd36513efe52e5c9b
  // claim: sol:@C@Synth@F@transfer#2469:path:6
  // witness-fingerprint-sha256: fb331434c0f550f9783f0285e388c94e074d964220ba026a83771608f5420beb
  // claim: sol:@C@Synth@F@transfer#2469:path:2
  // witness-fingerprint-sha256: 1ab83f4822460165ec0b54f8cec981077199b77031967184163037c1803d28bb

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@Synth@F@transfer#2469:path:15   depth=3
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   amount in [0, 0]
  //   msg.sender in [2147483650, 4294967295]
  //   recipient in [0, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': '(none)', '15': 'amount==0', '6': 'msg.sender==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, recipient, amount
  // ORACLE: 9 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 8 over POST-STATE, read through vm.load at
  // the slot solc reports, and 1 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): _balances[msg.sender]: post >= pre (entailed by a stronger rung that also HOLDS on _balances[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[msg.sender]: post <= pre (entailed by a stronger rung that also HOLDS on _balances[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[recipient]: post >= pre (entailed by a stronger rung that also HOLDS on _balances[recipient], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[recipient]: post <= pre (entailed by a stronger rung that also HOLDS on _balances[recipient], so asserting it detects nothing the stronger one misses)
  //   path guard ESTABLISHED: `!(sender != 0)` -> `vm.assume(uint256(uint160(p_msg_sender)) != 0);`
  //   path guard ESTABLISHED: `!(senderBalance >= amount)` -> `vm.assume(_pre_balances_msg_sender >= 0);`
  //   environment ESTABLISHED: msg.sender in [2147483650, 4294967295] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(4294967295)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, address recipient, uint256 amount) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 2147483650, 4294967295)));
    recipient = address(uint160(bound(uint256(uint160(recipient)), 0, 1461501637330902918203684832716283019655932542975)));
    amount = bound(amount, 0, 0);
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(8)))));
    uint256 _pre_balances_recipient = uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(8)))));
    // complete-path guard recovered from the emit report
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    vm.assume(_pre_balances_msg_sender >= 0);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [asserted] path exits normally; a revert fails the test
    vm.recordLogs();
    bool _put_ret = c0.transfer(recipient, amount);
    Vm.Log[] memory _veriputFixedLogs = vm.getRecordedLogs();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(4294967295)) && recipient == address(uint160(4294967295)) && amount == uint256(0)) {
      assertEq(_put_ret, true, "fixed witness return");
      assertEq(_veriputFixedLogs.length, 1);
      assertEq(_veriputFixedLogs[0].emitter, address(c0));
      assertEq(_veriputFixedLogs[0].topics.length, 3);
      assertEq(_veriputFixedLogs[0].topics[0], keccak256("Transfer(address,address,uint256)"));
      assertEq(_veriputFixedLogs[0].topics[1], bytes32(uint256(uint160(address(uint160(4294967295))))));
      assertEq(_veriputFixedLogs[0].topics[2], bytes32(uint256(uint160(address(uint160(4294967295))))));
      assertEq(_veriputFixedLogs[0].data, abi.encode(uint256(0)));
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(1)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(4294967295)), uint256(8))))), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(6)))) & 255), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(2)))), uint256(1), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(7)))), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(8)))));
    uint256 _post_balances_recipient = uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(8)))));
    assertEq(_post_balances_msg_sender, _pre_balances_msg_sender, "_balances[msg.sender]: post == pre");
    assertEq(_post_balances_recipient, _pre_balances_recipient, "_balances[recipient]: post == pre");
    assertGe(_pre_balances_msg_sender, _post_balances_msg_sender, "_balances[msg.sender]: pre - post in [amount, amount] with pre >= post");
    unchecked { assertGe(_pre_balances_msg_sender - _post_balances_msg_sender, amount, "_balances[msg.sender]: pre - post in [amount, amount] with pre >= post"); }
    unchecked { assertLe(_pre_balances_msg_sender - _post_balances_msg_sender, amount, "_balances[msg.sender]: pre - post in [amount, amount] with pre >= post"); }
    assertGe(_post_balances_recipient, _pre_balances_recipient, "_balances[recipient]: post - pre in [amount, amount] with post >= pre");
    unchecked { assertGe(_post_balances_recipient - _pre_balances_recipient, amount, "_balances[recipient]: post - pre in [amount, amount] with post >= pre"); }
    unchecked { assertLe(_post_balances_recipient - _pre_balances_recipient, amount, "_balances[recipient]: post - pre in [amount, amount] with post >= pre"); }
    assertTrue(_put_ret, "return: return == true");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_Synth_transfer_path15p1_part_part0_w_w(address p_msg_sender, address recipient, uint256 amount) public {
    _veriput_parameterized(p_msg_sender, recipient, amount);
  }
}
