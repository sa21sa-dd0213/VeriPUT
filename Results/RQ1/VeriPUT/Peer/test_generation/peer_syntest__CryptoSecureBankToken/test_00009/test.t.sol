// SPDX-License-Identifier: MIT
// Auto-generated by VeriPUT after ESBMC emitted no concrete test.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {CryptoSecureBankToken} from "../src/flat.sol";

contract CryptoSecureBankTokenCovTest_CryptoSecureBankToken_setParams_put2p1 is Test {
  CryptoSecureBankToken c0;
  function setUp() public {
    c0 = new CryptoSecureBankToken();
  }
  // claim: sol:@C@CryptoSecureBankToken@F@setParams#1155:path:2

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@CryptoSecureBankToken@F@setParams#1155:path:2   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': '(none)', '30': '(none)', '6': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, msg.value, newBasisPoints, newMaxFee
  // ORACLE: rollback exit plus 15 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (and funded with `vm.deal(p_msg_sender, p_msg_value)`, because the call sends value and the sender pays) (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: the compiler-only nonpayable reject replay was rewritten as a low-level value-bearing call taking the bound() fuzz parameter `p_msg_value`
  // 10 snapshot local(s) HOISTED to storage: 4 fuzz parameter(s) + 10 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_decimals; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_basisPointsRate; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_maximumFee; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_owner; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_owner2; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_decimals; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_basisPointsRate; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_maximumFee; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_owner; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_owner2; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, uint256 p_msg_value, uint256 newBasisPoints, uint256 newMaxFee) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    p_msg_value = bound(p_msg_value, 1, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    newBasisPoints = bound(newBasisPoints, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    newMaxFee = bound(newMaxFee, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // pre-state for the oracle, at this path's own entry
    _pre_decimals = uint256(vm.load(address(c0), bytes32(uint256(10))));
    _pre_basisPointsRate = uint256(vm.load(address(c0), bytes32(uint256(4))));
    _pre_maximumFee = uint256(vm.load(address(c0), bytes32(uint256(5))));
    _pre_owner = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    _pre_owner2 = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 1461501637330902918203684832716283019655932542975);
    // VeriPUT synthesized this call from source declarations; the
    // certified region and all oracle claims still come from ESBMC.
    vm.deal(p_msg_sender, p_msg_value);
    vm.prank(p_msg_sender);
    (bool _esbmc_value_gate_ok, ) = address(c0).call{value: p_msg_value}(abi.encodeWithSignature("setParams(uint256,uint256)", newBasisPoints, newMaxFee));
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_decimals = uint256(vm.load(address(c0), bytes32(uint256(10))));
    _post_basisPointsRate = uint256(vm.load(address(c0), bytes32(uint256(4))));
    _post_maximumFee = uint256(vm.load(address(c0), bytes32(uint256(5))));
    _post_owner = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    _post_owner2 = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_post_decimals, _pre_decimals, "decimals: post == pre");
    assertEq(_post_basisPointsRate, _pre_basisPointsRate, "basisPointsRate: post == pre");
    assertEq(_post_maximumFee, _pre_maximumFee, "maximumFee: post == pre");
    assertEq(_post_owner, _pre_owner, "owner: post == pre");
    assertEq(_post_owner2, _pre_owner2, "owner2: post == pre");
    assertGe(_post_decimals, _pre_decimals, "decimals: post >= pre");
    assertLe(_post_decimals, _pre_decimals, "decimals: post <= pre");
    assertGe(_post_basisPointsRate, _pre_basisPointsRate, "basisPointsRate: post >= pre");
    assertLe(_post_basisPointsRate, _pre_basisPointsRate, "basisPointsRate: post <= pre");
    assertGe(_post_maximumFee, _pre_maximumFee, "maximumFee: post >= pre");
    assertLe(_post_maximumFee, _pre_maximumFee, "maximumFee: post <= pre");
    assertGe(_post_owner, _pre_owner, "owner: post >= pre");
    assertLe(_post_owner, _pre_owner, "owner: post <= pre");
    assertGe(_post_owner2, _pre_owner2, "owner2: post >= pre");
    assertLe(_post_owner2, _pre_owner2, "owner2: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_esbmc_value_gate_ok, "value sent to a non-payable entry must revert");

    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    // Structural ABI-gate replay: R0 is already asserted over the certified nonzero-value region.
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_CryptoSecureBankToken_setParams_path2p1(address p_msg_sender, uint256 p_msg_value, uint256 newBasisPoints, uint256 newMaxFee) public {
    _veriput_parameterized(p_msg_sender, p_msg_value, newBasisPoints, newMaxFee);
  }
}
