// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {A3} from "../src/flat.sol";

contract A3CovTest_0_A3_f3_put31p1_p1_part_part0_w_w is Test {
  A3 c0;
  function setUp() public {
    c0 = new A3(3);
  }
  // claim: sol:@C@A3@F@f3#55:path:31
  // witness-fingerprint-sha256: 30a9d75f6991b87f6f4f77815ffe88915f4be284e1d8d2d3450c7389a629f607

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@A3@F@f3#55:path:31   depth=4
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   block.number in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   block.timestamp in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   msg.sender in [0, 4294967291]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.prevrandao == 0
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'25': '(none)', '27': '(none)', '31': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, block.timestamp, block.number
  // ORACLE: 22 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 5 over POST-STATE, read through vm.load at
  // the slot solc reports, and 17 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): x: post != pre (entailed by a stronger rung that also HOLDS on x, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): x: post >= pre (entailed by a stronger rung that also HOLDS on x, so asserting it detects nothing the stronger one misses)
  //   path guard DROPPED: 'x > 3' (`x` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   path guard DROPPED: 'x > 7' (`x` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   path guard DROPPED: '!(x > 0)' (`x` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   environment ESTABLISHED: msg.sender in [0, 4294967291]  \ {0} is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it, and the 1 punched value(s) are excluded by vm.assume; 0 is excluded because Foundry cannot prank address(0), and the remaining executable subregion is a subset of the certified region (no prank was present, so one was inserted)
  //   environment ESTABLISHED: block.timestamp in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: vm.warp now takes the bound() fuzz parameter `p_block_timestamp`
  //   environment ESTABLISHED: block.number in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: vm.roll now takes the bound() fuzz parameter `p_block_number`
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, uint256 p_block_timestamp, uint256 p_block_number) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 0, 4294967291)));
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    p_block_timestamp = bound(p_block_timestamp, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    p_block_number = bound(p_block_number, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_x = uint256(vm.load(address(c0), bytes32(uint256(0))));
    // [asserted] path exits normally; a revert fails the test
    vm.warp(p_block_timestamp);
    vm.roll(p_block_number);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    uint256 _put_ret = c0.f3();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(0)) && p_block_timestamp == uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935) && p_block_number == uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) {
      assertEq(_put_ret, uint256(4), "fixed witness return");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(0)))), uint256(4), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_x = uint256(vm.load(address(c0), bytes32(uint256(0))));
    assertGt(_post_x, _pre_x, "x: post > pre");
    unchecked { assertEq(_post_x, (uint256(_pre_x) + uint256(1)), "x: post == (state.x + 1)"); }
    assertGe(_post_x, _pre_x, "x: post - pre in [1, 1] with post >= pre");
    unchecked { assertGe(_post_x - _pre_x, 1, "x: post - pre in [1, 1] with post >= pre"); }
    unchecked { assertLe(_post_x - _pre_x, 1, "x: post - pre in [1, 1] with post >= pre"); }
    assertTrue(uint256(_put_ret) != 0, "return: return != 0");
    assertGe(uint256(_put_ret), 0, "return: return in [0, 6]");
    assertLe(uint256(_put_ret), 6, "return: return in [0, 6]");
    assertGe(uint256(_put_ret), 0, "return: return in [0, 7]");
    assertLe(uint256(_put_ret), 7, "return: return in [0, 7]");
    assertGe(uint256(_put_ret), 0, "return: return in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]");
    assertLe(uint256(_put_ret), 115792089237316195423570985008687907853269984665640564039457584007913129639935, "return: return in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]");
    assertGe(uint256(_put_ret), 0, "return: return in [0, 50499692625]");
    assertLe(uint256(_put_ret), 50499692625, "return: return in [0, 50499692625]");
    assertGe(uint256(_put_ret), 0, "return: return in [0, (msg.value + 6)]");
    assertLe(uint256(_put_ret), (uint256(0) + uint256(6)), "return: return in [0, (msg.value + 6)]");
    assertGe(uint256(_put_ret), 0, "return: return in [0, (msg.value + 7)]");
    assertLe(uint256(_put_ret), (uint256(0) + uint256(7)), "return: return in [0, (msg.value + 7)]");
    assertGe(uint256(_put_ret), 0, "return: return in [0, (msg.value + 115792089237316195423570985008687907853269984665640564039457584007913129639935)]");
    assertLe(uint256(_put_ret), (uint256(0) + uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)), "return: return in [0, (msg.value + 115792089237316195423570985008687907853269984665640564039457584007913129639935)]");
    assertGe(uint256(_put_ret), 0, "return: return in [0, (msg.value + 50499692625)]");
    assertLe(uint256(_put_ret), (uint256(0) + uint256(50499692625)), "return: return in [0, (msg.value + 50499692625)]");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_A3_f3_path31p1_part_part0_w_w(address p_msg_sender, uint256 p_block_timestamp, uint256 p_block_number) public {
    _veriput_parameterized(p_msg_sender, p_block_timestamp, p_block_number);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { A3CovTest_0_A3_f3_concrete31p1__basis_part0_w_w__W _veriput_w = new A3CovTest_0_A3_f3_concrete31p1__basis_part0_w_w__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

contract A3CovTest_1 is Test {
  A3 c0;
  function setUp() public {
    c0 = new A3(4);
  }
  // claim: sol:@C@A3@F@f3#55:path:27
  // witness-fingerprint-sha256: 10ce4baf77f9cfb8d5583aaa346308625d20369fb4d082edf5436ca2401c4338
}

contract A3CovTest_2 is Test {
  A3 c0;
  function setUp() public {
    c0 = new A3(115792089237316195423570985008687907853269984665640564039457584007913129639933);
  }
  // claim: sol:@C@A3@F@f3#55:path:25
  // witness-fingerprint-sha256: e1397db39bcbf3a36a3b29eb71b6a29cbc65c5f06aa86767c990e79f78d7d644
}

contract A3CovTest_3 is Test {
  A3 c0;
  function setUp() public {
    c0 = new A3(0);
  }
  // claim: sol:@C@A3@F@f3#55:path:2
  // witness-fingerprint-sha256: 019e1c3535d3b2d25a17926651c7ae5b3151e348809002bfda15193762cb1d73
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract A3CovTest_0_A3_f3_concrete31p1__basis_part0_w_w__W is Test {
  A3 c0;
  function setUp() public {
    c0 = new A3(3);
  }
  // claim: sol:@C@A3@F@f3#55:path:31
  // witness-fingerprint-sha256: 30a9d75f6991b87f6f4f77815ffe88915f4be284e1d8d2d3450c7389a629f607
  function _w_test_cov_0() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(3) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(0)))), uint256(3), "entry pin state.x did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // [asserted] path exits normally; a revert fails the test
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(0)), address(uint160(0)));
    uint256 _veriput_concrete_return = c0.f3();
    assertEq(_veriput_concrete_return, uint256(4), "fixed witness return must match");
    uint256 _veriput_fixed_state_x_0 = uint256(vm.load(address(c0), bytes32(uint256(0))));
    assertEq(_veriput_fixed_state_x_0, uint256(4), "fixed witness state");
  }
}

contract A3CovTest_1__W is Test {
  A3 c0;
  function setUp() public {
    c0 = new A3(4);
  }
  // claim: sol:@C@A3@F@f3#55:path:27
  // witness-fingerprint-sha256: 10ce4baf77f9cfb8d5583aaa346308625d20369fb4d082edf5436ca2401c4338
}

contract A3CovTest_2__W is Test {
  A3 c0;
  function setUp() public {
    c0 = new A3(115792089237316195423570985008687907853269984665640564039457584007913129639933);
  }
  // claim: sol:@C@A3@F@f3#55:path:25
  // witness-fingerprint-sha256: e1397db39bcbf3a36a3b29eb71b6a29cbc65c5f06aa86767c990e79f78d7d644
}

contract A3CovTest_3__W is Test {
  A3 c0;
  function setUp() public {
    c0 = new A3(0);
  }
  // claim: sol:@C@A3@F@f3#55:path:2
  // witness-fingerprint-sha256: 019e1c3535d3b2d25a17926651c7ae5b3151e348809002bfda15193762cb1d73
}
