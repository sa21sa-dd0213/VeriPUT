// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {Payments} from "../src/flat.sol";

contract PaymentsCovTest_Payments_pay_concrete2_fb is Test {
  Payments c0;
  function setUp() public {
    c0 = new Payments();
  }
  // claim: sol:@C@Payments@F@pay#77:path:2
  // witness-fingerprint-sha256: 150a23c8940c24a655470410b5ce604b957f7db4913f7393734042b368265ad6
  function test_cov_0() public {
    // [revert-tolerant] outcome not asserted
    bool _veriput_concrete_completed = false;
    try c0.pay("", 57896044618658097711785492504343953926634992332820282019728792003956564819967) {
      _veriput_concrete_completed = true;
    } catch {}
    assertFalse(_veriput_concrete_completed, "fixed witness call must revert");
    uint256 _veriput_fixed_state_owner_0 = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_owner_0, uint256(0), "fixed witness state");
  }
}
