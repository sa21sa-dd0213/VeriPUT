// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {Randomness} from "../src/flat.sol";

contract RandomnessCovTest_0 is Test {
  Randomness c0;
  function setUp() public {
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new Randomness();
    vm.stopPrank();
  }
  // claim: sol:@C@Randomness@F@setSealedSeed#59:path:15
  // witness-fingerprint-sha256: 84870f1257862baac37b8618649188904ed6747944e4d4accc2f120959429f67
  // claim: sol:@C@Randomness@F@setSealedSeed#59:path:2
  // witness-fingerprint-sha256: 746bfb10b841b11a340d1ff4e767a2a781086db578cd8d4a00c7778cea95b19c
}

contract RandomnessCovTest_1_Randomness_setSealedSeed_put14p1 is Test {
  Randomness c0;
  function setUp() public {
    vm.startPrank(address(uint160(4294967295)), address(uint160(4294967295)));
    c0 = new Randomness();
    vm.stopPrank();
  }
  // claim: sol:@C@Randomness@F@setSealedSeed#59:path:14
  // witness-fingerprint-sha256: cd419fb4ff18a14d0b9a82300269fe1cb75312cf1900f4c6841965a49575a34d

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@Randomness@F@setSealedSeed#59:path:14   depth=3
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   _sealedSeed in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   block.number in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   msg.sender in [0, 1461501637330902918203684832716283019655932542975]  \ {4294967295}
  //   state.betsClosed in [0, 1]
  //   state.seedSet in [0, 0]
  //   state.storedBlockNumber in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   state.trustedParty in [4294967295, 4294967295]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639935
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, block.number, _sealedSeed
  // ORACLE: rollback exit plus 11 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   path guard ESTABLISHED: `!(!(msg.sender == trustedParty))` -> `vm.assume(uint256(uint160(p_msg_sender)) != 4294967295);`
  //   path guard DROPPED: '!(!seedSet)' (`seedSet` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   entry-state bound DROPPED: state.storedBlockNumber in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query -- the rungs were proved about the constructor's own value. Establishing a fuzz-chosen value here would test entry states the proof never covered, which is how this PUT came back RED on the unmodified contract. Both endpoints are the type's own limits, so there is not even an in-region check to make: the bound says nothing at all)
  //   environment ESTABLISHED: msg.sender in [0, 1461501637330902918203684832716283019655932542975]  \ {0, 4294967295} is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it, and the 2 punched value(s) are excluded by vm.assume; 0 is excluded because Foundry cannot prank address(0), and the remaining executable subregion is a subset of the certified region (replacing `vm.prank(address(uint160(4294967294)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639935 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935)` before the target call
  //   environment ESTABLISHED: block.number in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: vm.roll now takes the bound() fuzz parameter `p_block_number`
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  // 10 snapshot local(s) HOISTED to storage: 4 fuzz parameter(s) + 10 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_sealedSeed; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_seedSet; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_betsClosed; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_storedBlockNumber; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_trustedParty; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_sealedSeed; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_seedSet; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_betsClosed; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_storedBlockNumber; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_trustedParty; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, uint256 p_block_number, uint256 _sealedSeed, uint8 s_betsClosed) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 0, 1461501637330902918203684832716283019655932542975)));
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    vm.assume(uint256(uint160(p_msg_sender)) != 4294967295);
    p_block_number = bound(p_block_number, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    _sealedSeed = bound(_sealedSeed, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    s_betsClosed = uint8(bound(uint256(s_betsClosed), 0, 1));
    // entry state the certified region names, ESTABLISHED (not assumed):
    //   state.betsClosed := s_betsClosed in [0, 1] (SET and FUZZED: freed at the certification entry); state.seedSet := 0; state.trustedParty := 4294967295
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(1))));
      _w = (_w & ~uint256(65280)) | ((uint256(uint256(s_betsClosed)) & 255) << 8);
      vm.store(address(c0), bytes32(uint256(1)), bytes32(_w));
    }
    assertLe(((uint256(vm.load(address(c0), bytes32(uint256(1)))) >> 8) & 255), uint256(1), "the entry state is OUTSIDE the certified region: state.betsClosed was assumed in [0, 1] when the path was certified, so a value outside it means the assumption was vacuous and the rungs below were proved about no execution this test can reach");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(1))));
      _w = (_w & ~uint256(255)) | ((uint256(0) & 255) << 0);
      vm.store(address(c0), bytes32(uint256(1)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(1)))) & 255), uint256(0), "entry pin state.seedSet did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(3))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(4294967295) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(3)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975), uint256(4294967295), "entry pin state.trustedParty did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // pre-state for the oracle, at this path's own entry
    _pre_sealedSeed = uint256(vm.load(address(c0), bytes32(uint256(0))));
    _pre_seedSet = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 255);
    _pre_betsClosed = ((uint256(vm.load(address(c0), bytes32(uint256(1)))) >> 8) & 255);
    _pre_storedBlockNumber = uint256(vm.load(address(c0), bytes32(uint256(2))));
    _pre_trustedParty = (uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975);
    // complete-path guard recovered from the emit report
    vm.assume(uint256(uint160(p_msg_sender)) != 4294967295);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(p_block_number);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [revert-tolerant] outcome not asserted
    bool _put_ok = true;
    try c0.setSealedSeed(bytes32(_sealedSeed)) {} catch { _put_ok = false; }
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(4294967294)) && p_block_number == uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935) && _sealedSeed == uint256(0) && s_betsClosed == uint8(0)) {
      assertEq(((uint256(vm.load(address(c0), bytes32(uint256(1)))) >> 8) & 255), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(1)))) & 255), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(2)))), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975), uint256(4294967295), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_sealedSeed = uint256(vm.load(address(c0), bytes32(uint256(0))));
    _post_seedSet = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 255);
    _post_betsClosed = ((uint256(vm.load(address(c0), bytes32(uint256(1)))) >> 8) & 255);
    _post_storedBlockNumber = uint256(vm.load(address(c0), bytes32(uint256(2))));
    _post_trustedParty = (uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_post_sealedSeed, _pre_sealedSeed, "sealedSeed: post == pre");
    assertEq(_post_seedSet, _pre_seedSet, "seedSet: post == pre");
    assertEq(_post_betsClosed, _pre_betsClosed, "betsClosed: post == pre");
    assertEq(_post_storedBlockNumber, _pre_storedBlockNumber, "storedBlockNumber: post == pre");
    assertEq(_post_trustedParty, _pre_trustedParty, "trustedParty: post == pre");
    assertGe(_post_sealedSeed, _pre_sealedSeed, "sealedSeed: post >= pre");
    assertLe(_post_sealedSeed, _pre_sealedSeed, "sealedSeed: post <= pre");
    assertGe(_post_storedBlockNumber, _pre_storedBlockNumber, "storedBlockNumber: post >= pre");
    assertLe(_post_storedBlockNumber, _pre_storedBlockNumber, "storedBlockNumber: post <= pre");
    assertGe(_post_trustedParty, _pre_trustedParty, "trustedParty: post >= pre");
    assertLe(_post_trustedParty, _pre_trustedParty, "trustedParty: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_put_ok, "path enc=14p1 exits through a REVERT: the call must fail on the unmodified contract");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_Randomness_setSealedSeed_path14p1(address p_msg_sender, uint256 p_block_number, uint256 _sealedSeed, uint8 s_betsClosed) public {
    _veriput_parameterized(p_msg_sender, p_block_number, _sealedSeed, s_betsClosed);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { RandomnessCovTest_1_Randomness_setSealedSeed_concrete14p1__basis_root__W _veriput_w = new RandomnessCovTest_1_Randomness_setSealedSeed_concrete14p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_2(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract RandomnessCovTest_0__W is Test {
  Randomness c0;
  function setUp() public {
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new Randomness();
    vm.stopPrank();
  }
  // claim: sol:@C@Randomness@F@setSealedSeed#59:path:15
  // witness-fingerprint-sha256: 84870f1257862baac37b8618649188904ed6747944e4d4accc2f120959429f67
  // claim: sol:@C@Randomness@F@setSealedSeed#59:path:2
  // witness-fingerprint-sha256: 746bfb10b841b11a340d1ff4e767a2a781086db578cd8d4a00c7778cea95b19c
}

contract RandomnessCovTest_1_Randomness_setSealedSeed_concrete14p1__basis_root__W is Test {
  Randomness c0;
  function setUp() public {
    vm.startPrank(address(uint160(4294967295)), address(uint160(4294967295)));
    c0 = new Randomness();
    vm.stopPrank();
  }
  // claim: sol:@C@Randomness@F@setSealedSeed#59:path:14
  // witness-fingerprint-sha256: cd419fb4ff18a14d0b9a82300269fe1cb75312cf1900f4c6841965a49575a34d
  function _w_test_cov_2() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(1))));
      _w = (_w & ~uint256(65280)) | ((uint256(0) & 255) << 8);
      vm.store(address(c0), bytes32(uint256(1)), bytes32(_w));
    }
    assertEq(((uint256(vm.load(address(c0), bytes32(uint256(1)))) >> 8) & 255), uint256(0), "entry pin state.betsClosed did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(1))));
      _w = (_w & ~uint256(255)) | ((uint256(0) & 255) << 0);
      vm.store(address(c0), bytes32(uint256(1)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(1)))) & 255), uint256(0), "entry pin state.seedSet did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(2))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(2)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(2)))), uint256(0), "entry pin state.storedBlockNumber did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(3))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(4294967295) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(3)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975), uint256(4294967295), "entry pin state.trustedParty did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // [revert-tolerant] outcome not asserted
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(4294967294)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    try c0.setSealedSeed(bytes32(uint256(0))) {
      _veriput_concrete_completed = true;
    } catch {}
    assertFalse(_veriput_concrete_completed, "fixed witness call must revert");
    uint256 _veriput_fixed_state_betsClosed_0 = ((uint256(vm.load(address(c0), bytes32(uint256(1)))) >> 8) & 255);
    assertEq(_veriput_fixed_state_betsClosed_0, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_seedSet_1 = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 255);
    assertEq(_veriput_fixed_state_seedSet_1, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_storedBlockNumber_2 = uint256(vm.load(address(c0), bytes32(uint256(2))));
    assertEq(_veriput_fixed_state_storedBlockNumber_2, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_trustedParty_3 = (uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_trustedParty_3, uint256(4294967295), "fixed witness state");
  }
}
