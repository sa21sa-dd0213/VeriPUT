// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {GuessTheNumberChallenge} from "../src/flat.sol";

contract GuessTheNumberChallengeCovTest_GuessTheNumberChallenge_guess_put7p1_p1_part_part0_r is Test {
  GuessTheNumberChallenge c0;
  function setUp() public {
    vm.deal(address(this), 1000000000000000000);
    c0 = new GuessTheNumberChallenge{value: 1000000000000000000}();
  }
  // claim: sol:@C@GuessTheNumberChallenge@F@guess#56:path:7
  // witness-fingerprint-sha256: 2d5258779c54fcc3705a3c9d9d4c9a976ca52e919c7e786ba72884ab3440b32f
  // claim: sol:@C@GuessTheNumberChallenge@F@guess#56:path:2
  // witness-fingerprint-sha256: 000d4a4e65f6f3f37725a56c8aa8cd5d4dd9336b033650cd6e4d45b1084b0a36

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@GuessTheNumberChallenge@F@guess#56:path:7   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   msg.value in [1000000000000000000, 1000000000000000000]
  //   n in [0, 254]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639935
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639935
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'2': '(none)', '7': 'msg.value==1000000000000000000'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, n
  // ORACLE: 8 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 8 over POST-STATE, read through vm.load at
  // the slot solc reports, and 0 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): answer: post >= pre (entailed by a stronger rung that also HOLDS on answer, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): answer: post <= pre (entailed by a stronger rung that also HOLDS on answer, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): answer: post == (pre + 0) (entailed by a stronger rung that also HOLDS on answer, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): answer: post in [state.answer, state.answer] (entailed by a stronger rung that also HOLDS on answer, so asserting it detects nothing the stronger one misses)
  //   path guard ESTABLISHED: `n == answer` -> `vm.assume(n != _pre_answer);`
  //   path guard DROPPED: '!(msg.value == return_value$__ESBMC_ether$1)' (`return_value$__ESBMC_ether$1` is not nameable in this PUT)
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (and funded with `vm.deal(p_msg_sender, 1000000000000000000)`, because the call sends value and the sender pays) (no prank was present, so one was inserted)
  //   environment ESTABLISHED: msg.value in [1000000000000000000, 1000000000000000000] is ESTABLISHED: the existing value-bearing call now takes the certified constant `1000000000000000000`
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639935 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639935 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, uint8 n) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    n = uint8(bound(uint256(n), 0, 254));
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_answer = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    // complete-path guard recovered from the emit report
    vm.assume(n != _pre_answer);
    vm.deal(address(this), 1000000000000000000);
    // [asserted] path exits normally; a revert fails the test
    vm.deal(p_msg_sender, 1000000000000000000);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    c0.guess{value: 1000000000000000000}(n);
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(0)) && n == uint8(254)) {
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255), uint256(42), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_answer = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    assertEq(_post_answer, _pre_answer, "answer: post == pre");
    assertEq(_post_answer, _pre_answer, "answer: post == state.answer");
    assertGe(_post_answer, 0, "answer: post in [0, pre]");
    assertLe(_post_answer, _pre_answer, "answer: post in [0, pre]");
    assertGe(_post_answer, 0, "answer: post in [0, state.answer]");
    assertLe(_post_answer, _pre_answer, "answer: post in [0, state.answer]");
    assertGe(_post_answer, 0, "answer: post in [0, (0 + 255)]");
    unchecked { assertLe(_post_answer, (uint256(0) + uint256(255)), "answer: post in [0, (0 + 255)]"); }
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_GuessTheNumberChallenge_guess_path7p1_part_part0_r(address p_msg_sender, uint8 n) public {
    _veriput_parameterized(p_msg_sender, n);
  }
}
