// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {Cfb2} from "../src/flat.sol";

contract Cfb2CovTest_Cfb2_f_put51p1 is Test {
  Cfb2 c0;
  function setUp() public {
    c0 = new Cfb2();
  }
  // claim: sol:@C@Cfb2@F@f#27:path:15
  // witness-fingerprint-sha256: c1a465a1f26774cd26dbadf99566a94709816ba3d4940f4729139d3491632af6
  // claim: sol:@C@Cfb2@F@f#27:path:26
  // witness-fingerprint-sha256: 9ad7aca192f99a549cbe1d579b255a624e28c80a721f75a8c77de68f72b44828
  // claim: sol:@C@Cfb2@F@f#27:path:51
  // witness-fingerprint-sha256: 723a1d04508f2c46e8486c8c7ae12133c1f9a9b669d284270f2dc39a4cbb8cb7
  // claim: sol:@C@Cfb2@F@f#27:path:195
  // witness-fingerprint-sha256: 8891aceaa2df521a0735e0c7e8750eda4a1a3447ec8abc36710966b0294ab8b5
  // claim: sol:@C@Cfb2@F@f#27:path:771
  // witness-fingerprint-sha256: f200db453816488e1637be76cde12314dc322945c3700728be59ae40f3ca051d
  // claim: sol:@C@Cfb2@F@f#27:path:2
  // witness-fingerprint-sha256: e13f6722b4709f0da02cfea28ce6c51dd0c7cd427b9a4a78986028638bec146b

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@Cfb2@F@f#27:path:51   depth=5
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   b in [1, 1]
  //   block.number in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   block.timestamp in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   x in [9, 9]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.prevrandao == 0
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'15': '(none)', '195': 'b==1, x==8', '26': 'b==0', '51': 'b==1, x==9', '771': 'b==1, x==7'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, block.timestamp, block.number, x, b
  // ORACLE: none emitted -- NOT ONE RUNG HOLDS:
  // no candidate held over the certified region, so there is nothing to render.
  // The exit-kind expectation below is still an assertion.
  //   path guard DROPPED: '!(x < 10)' (`x` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   path guard DROPPED: '!b' (contradicts the certified fixed coordinate value; the region assignment is authoritative)
  //   path guard DROPPED: 'x < 10' (`x` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   path guard DROPPED: '!(x >= 10)' (`x` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (no prank was present, so one was inserted)
  //   environment ESTABLISHED: block.timestamp in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: vm.warp now takes the bound() fuzz parameter `p_block_timestamp`
  //   environment ESTABLISHED: block.number in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: vm.roll now takes the bound() fuzz parameter `p_block_number`
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, uint256 p_block_timestamp, uint256 p_block_number, uint256 x, bool b) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    p_block_timestamp = bound(p_block_timestamp, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    p_block_number = bound(p_block_number, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    x = bound(x, 9, 9);
    b = true;
    // [asserted] path exits normally; a revert fails the test
    vm.warp(p_block_timestamp);
    vm.roll(p_block_number);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    c0.f(x, b);
    // VERIPUT_FIXED_REPLAY_ASSERTIONS // R0 is already region-wide
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_Cfb2_f_path51p1(address p_msg_sender, uint256 p_block_timestamp, uint256 p_block_number, uint256 x, bool b) public {
    _veriput_parameterized(p_msg_sender, p_block_timestamp, p_block_number, x, b);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { Cfb2CovTest_Cfb2_f_concrete51p1__basis_root__W _veriput_w = new Cfb2CovTest_Cfb2_f_concrete51p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_2(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract Cfb2CovTest_Cfb2_f_concrete51p1__basis_root__W is Test {
  Cfb2 c0;
  function setUp() public {
    c0 = new Cfb2();
  }
  // claim: sol:@C@Cfb2@F@f#27:path:15
  // witness-fingerprint-sha256: c1a465a1f26774cd26dbadf99566a94709816ba3d4940f4729139d3491632af6
  // claim: sol:@C@Cfb2@F@f#27:path:26
  // witness-fingerprint-sha256: 9ad7aca192f99a549cbe1d579b255a624e28c80a721f75a8c77de68f72b44828
  // claim: sol:@C@Cfb2@F@f#27:path:51
  // witness-fingerprint-sha256: 723a1d04508f2c46e8486c8c7ae12133c1f9a9b669d284270f2dc39a4cbb8cb7
  function _w_test_cov_2() public {
    // [asserted] path exits normally; a revert fails the test
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(0)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    c0.f(uint256(9), true);
    _veriput_concrete_completed = true;
    assertTrue(_veriput_concrete_completed, "fixed witness call must complete");
  }
  // claim: sol:@C@Cfb2@F@f#27:path:195
  // witness-fingerprint-sha256: 8891aceaa2df521a0735e0c7e8750eda4a1a3447ec8abc36710966b0294ab8b5
  // claim: sol:@C@Cfb2@F@f#27:path:771
  // witness-fingerprint-sha256: f200db453816488e1637be76cde12314dc322945c3700728be59ae40f3ca051d
  // claim: sol:@C@Cfb2@F@f#27:path:2
  // witness-fingerprint-sha256: e13f6722b4709f0da02cfea28ce6c51dd0c7cd427b9a4a78986028638bec146b
}
