// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {Rubixi} from "../src/flat.sol";

contract RubixiCovTest_Rubixi_fallback_put6p1_p1_part_part0_r_w is Test {
  Rubixi c0;
  function setUp() public {
    c0 = new Rubixi();
  }
  // claim: sol:@C@Rubixi@F@fallback#52:path:6
  // witness-fingerprint-sha256: cabfa61068b4678a3f2813f4e680a15081345a39b84dcc0de8ddde7487be2630
  // claim: sol:@C@Rubixi@F@fallback#52:path:2
  // witness-fingerprint-sha256: 0bda2effa41b850c53025ab18c08ad11a233a582e9fbd624167241cecf9ee293

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@Rubixi@F@fallback#52:path:6   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   block.number in [115792089237316195423570985008687907853269984665640564039457584007913129639935, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   block.timestamp in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   msg.sender in [4294967295, 11348359941645608]
  //   msg.value in [0, 0]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.prevrandao == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'2': '(none)', '6': 'msg.value==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, block.timestamp
  // ORACLE: 28 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 28 over POST-STATE, read through vm.load at
  // the slot solc reports, and 0 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): balance: post >= pre (entailed by a stronger rung that also HOLDS on balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balance: post <= pre (entailed by a stronger rung that also HOLDS on balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): collectedFees: post >= pre (entailed by a stronger rung that also HOLDS on collectedFees, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): collectedFees: post <= pre (entailed by a stronger rung that also HOLDS on collectedFees, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): feePercent: post >= pre (entailed by a stronger rung that also HOLDS on feePercent, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): feePercent: post <= pre (entailed by a stronger rung that also HOLDS on feePercent, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): pyramidMultiplier: post >= pre (entailed by a stronger rung that also HOLDS on pyramidMultiplier, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): pyramidMultiplier: post <= pre (entailed by a stronger rung that also HOLDS on pyramidMultiplier, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): payoutOrder: post >= pre (entailed by a stronger rung that also HOLDS on payoutOrder, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): payoutOrder: post <= pre (entailed by a stronger rung that also HOLDS on payoutOrder, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balance: post == (pre + msg.value) (entailed by a stronger rung that also HOLDS on balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balance: post in [state.balance, state.balance] (entailed by a stronger rung that also HOLDS on balance, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): feePercent: post == (pre + msg.value) (entailed by a stronger rung that also HOLDS on feePercent, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): feePercent: post in [state.feePercent, state.feePercent] (entailed by a stronger rung that also HOLDS on feePercent, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): pyramidMultiplier: post == (pre + msg.value) (entailed by a stronger rung that also HOLDS on pyramidMultiplier, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): pyramidMultiplier: post in [state.pyramidMultiplier, state.pyramidMultiplier] (entailed by a stronger rung that also HOLDS on pyramidMultiplier, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): payoutOrder: post == (pre + msg.value) (entailed by a stronger rung that also HOLDS on payoutOrder, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): payoutOrder: post in [state.payoutOrder, state.payoutOrder] (entailed by a stronger rung that also HOLDS on payoutOrder, so asserting it detects nothing the stronger one misses)
  //   path guard DROPPED: '!(msg.value < return_value$__ESBMC_ether$1)' (`return_value$__ESBMC_ether$1` is not nameable in this PUT)
  //   environment ESTABLISHED: msg.sender in [4294967295, 11348359941645608] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (no prank was present, so one was inserted)
  //   environment ESTABLISHED: block.timestamp in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: vm.warp now takes the bound() fuzz parameter `p_block_timestamp`
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639935 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, uint256 p_block_timestamp) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 4294967295, 11348359941645608)));
    p_block_timestamp = bound(p_block_timestamp, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_balance = uint256(vm.load(address(c0), bytes32(uint256(0))));
    uint256 _pre_collectedFees = uint256(vm.load(address(c0), bytes32(uint256(1))));
    uint256 _pre_feePercent = uint256(vm.load(address(c0), bytes32(uint256(2))));
    uint256 _pre_pyramidMultiplier = uint256(vm.load(address(c0), bytes32(uint256(3))));
    uint256 _pre_payoutOrder = uint256(vm.load(address(c0), bytes32(uint256(4))));
    vm.warp(p_block_timestamp);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    (bool ok1, ) = address(c0).call(hex"deadbeef");
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(4294967295)) && p_block_timestamp == uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) {
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(0)))), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(1)))), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(5)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(2)))), uint256(10), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(4)))), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(3)))), uint256(300), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_balance = uint256(vm.load(address(c0), bytes32(uint256(0))));
    uint256 _post_collectedFees = uint256(vm.load(address(c0), bytes32(uint256(1))));
    uint256 _post_feePercent = uint256(vm.load(address(c0), bytes32(uint256(2))));
    uint256 _post_pyramidMultiplier = uint256(vm.load(address(c0), bytes32(uint256(3))));
    uint256 _post_payoutOrder = uint256(vm.load(address(c0), bytes32(uint256(4))));
    assertEq(_post_balance, _pre_balance, "balance: post == pre");
    assertEq(_post_collectedFees, _pre_collectedFees, "collectedFees: post == pre");
    assertEq(_post_feePercent, _pre_feePercent, "feePercent: post == pre");
    assertEq(_post_pyramidMultiplier, _pre_pyramidMultiplier, "pyramidMultiplier: post == pre");
    assertEq(_post_payoutOrder, _pre_payoutOrder, "payoutOrder: post == pre");
    assertGe(_post_collectedFees, _pre_collectedFees, "collectedFees: post - pre in [msg.value, msg.value] with post >= pre");
    unchecked { assertGe(_post_collectedFees - _pre_collectedFees, 0, "collectedFees: post - pre in [msg.value, msg.value] with post >= pre"); }
    unchecked { assertLe(_post_collectedFees - _pre_collectedFees, 0, "collectedFees: post - pre in [msg.value, msg.value] with post >= pre"); }
    assertEq(_post_balance, _pre_balance, "balance: post == state.balance");
    assertGe(_post_balance, 0, "balance: post in [0, pre]");
    assertLe(_post_balance, _pre_balance, "balance: post in [0, pre]");
    assertGe(_post_balance, 0, "balance: post in [0, state.balance]");
    assertLe(_post_balance, _pre_balance, "balance: post in [0, state.balance]");
    assertEq(_post_feePercent, _pre_feePercent, "feePercent: post == state.feePercent");
    assertGe(_post_feePercent, 0, "feePercent: post in [0, pre]");
    assertLe(_post_feePercent, _pre_feePercent, "feePercent: post in [0, pre]");
    assertGe(_post_feePercent, 0, "feePercent: post in [0, state.feePercent]");
    assertLe(_post_feePercent, _pre_feePercent, "feePercent: post in [0, state.feePercent]");
    assertEq(_post_pyramidMultiplier, _pre_pyramidMultiplier, "pyramidMultiplier: post == state.pyramidMultiplier");
    assertGe(_post_pyramidMultiplier, 0, "pyramidMultiplier: post in [0, pre]");
    assertLe(_post_pyramidMultiplier, _pre_pyramidMultiplier, "pyramidMultiplier: post in [0, pre]");
    assertGe(_post_pyramidMultiplier, 0, "pyramidMultiplier: post in [0, state.pyramidMultiplier]");
    assertLe(_post_pyramidMultiplier, _pre_pyramidMultiplier, "pyramidMultiplier: post in [0, state.pyramidMultiplier]");
    assertEq(_post_payoutOrder, _pre_payoutOrder, "payoutOrder: post == state.payoutOrder");
    assertGe(_post_payoutOrder, 0, "payoutOrder: post in [0, pre]");
    assertLe(_post_payoutOrder, _pre_payoutOrder, "payoutOrder: post in [0, pre]");
    assertGe(_post_payoutOrder, 0, "payoutOrder: post in [0, state.payoutOrder]");
    assertLe(_post_payoutOrder, _pre_payoutOrder, "payoutOrder: post in [0, state.payoutOrder]");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertTrue(ok1, "covered receive/fallback path must return normally");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_Rubixi_fallback_path6p1_part_part0_r_w(address p_msg_sender, uint256 p_block_timestamp) public {
    _veriput_parameterized(p_msg_sender, p_block_timestamp);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { RubixiCovTest_Rubixi_fallback_concrete6p1__basis_part0_r_w__W _veriput_w = new RubixiCovTest_Rubixi_fallback_concrete6p1__basis_part0_r_w__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract RubixiCovTest_Rubixi_fallback_concrete6p1__basis_part0_r_w__W is Test {
  Rubixi c0;
  function setUp() public {
    c0 = new Rubixi();
  }
  // claim: sol:@C@Rubixi@F@fallback#52:path:6
  // witness-fingerprint-sha256: cabfa61068b4678a3f2813f4e680a15081345a39b84dcc0de8ddde7487be2630
  function _w_test_cov_0() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(0)))), uint256(0), "entry pin state.balance did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(1))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(1)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(1)))), uint256(0), "entry pin state.collectedFees did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(5))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(0) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(5)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(5)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "entry pin state.creator did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(2))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(10) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(2)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(2)))), uint256(10), "entry pin state.feePercent did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(4))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(4)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(4)))), uint256(0), "entry pin state.payoutOrder did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(3))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(300) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(3)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(3)))), uint256(300), "entry pin state.pyramidMultiplier did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(4294967295)), address(uint160(0)));
    (bool ok1, ) = address(c0).call(hex"deadbeef");
    assertTrue(ok1, "covered receive/fallback path must return normally");
    uint256 _veriput_fixed_state_balance_0 = uint256(vm.load(address(c0), bytes32(uint256(0))));
    assertEq(_veriput_fixed_state_balance_0, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_collectedFees_1 = uint256(vm.load(address(c0), bytes32(uint256(1))));
    assertEq(_veriput_fixed_state_collectedFees_1, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_creator_2 = (uint256(vm.load(address(c0), bytes32(uint256(5)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_creator_2, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_feePercent_3 = uint256(vm.load(address(c0), bytes32(uint256(2))));
    assertEq(_veriput_fixed_state_feePercent_3, uint256(10), "fixed witness state");
    uint256 _veriput_fixed_state_payoutOrder_4 = uint256(vm.load(address(c0), bytes32(uint256(4))));
    assertEq(_veriput_fixed_state_payoutOrder_4, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_pyramidMultiplier_5 = uint256(vm.load(address(c0), bytes32(uint256(3))));
    assertEq(_veriput_fixed_state_pyramidMultiplier_5, uint256(300), "fixed witness state");
  }
  // claim: sol:@C@Rubixi@F@fallback#52:path:2
  // witness-fingerprint-sha256: 0bda2effa41b850c53025ab18c08ad11a233a582e9fbd624167241cecf9ee293
}
