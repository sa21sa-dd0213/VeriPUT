// SPDX-License-Identifier: MIT
// Auto-generated by VeriPUT after ESBMC emitted no concrete test.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {BERNIE} from "../src/flat.sol";

contract BERNIECovTest_BERNIE_reflect_put2p1 is Test {
  BERNIE c0;
  function setUp() public {
    c0 = new BERNIE();
    // ESBMC runtime fixture: local Foundry has no code at 0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D.
    address _esbmc_ext_mock_0 = address(0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D);
    vm.etch(_esbmc_ext_mock_0, hex"60006000f3");
    vm.mockCall(_esbmc_ext_mock_0, abi.encodeWithSignature("WETH()"), abi.encode(address(0)));
    vm.mockCall(_esbmc_ext_mock_0, abi.encodeWithSignature("factory()"), abi.encode(address(0)));
  }
  // claim: sol:@C@BERNIE@F@reflect#915:path:2

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@BERNIE@F@reflect#915:path:2   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'255': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, msg.value, tAmount
  // ORACLE: rollback exit plus 19 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (and funded with `vm.deal(p_msg_sender, p_msg_value)`, because the call sends value and the sender pays) (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: the compiler-only nonpayable reject replay was rewritten as a low-level value-bearing call taking the bound() fuzz parameter `p_msg_value`
  // 14 snapshot local(s) HOISTED to storage: 3 fuzz parameter(s) + 14 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_marketing; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_MAX; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_tTotal; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_rTotal; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_tFeeTotal; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_isExcluded_msg_sender; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_rOwned_state_marketing; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_marketing; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_MAX; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_tTotal; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_rTotal; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_tFeeTotal; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_isExcluded_msg_sender; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_rOwned_state_marketing; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, uint256 p_msg_value, uint256 tAmount) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    p_msg_value = bound(p_msg_value, 1, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    tAmount = bound(tAmount, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // pre-state for the oracle, at this path's own entry
    _pre_marketing = (uint256(vm.load(address(c0), bytes32(uint256(11)))) & 1461501637330902918203684832716283019655932542975);
    _pre_MAX = uint256(vm.load(address(c0), bytes32(uint256(12))));
    _pre_tTotal = uint256(vm.load(address(c0), bytes32(uint256(13))));
    _pre_rTotal = uint256(vm.load(address(c0), bytes32(uint256(14))));
    _pre_tFeeTotal = uint256(vm.load(address(c0), bytes32(uint256(15))));
    _pre_isExcluded_msg_sender = (uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(8))))) & 255);
    _pre_rOwned_state_marketing = uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(11)))) & 1461501637330902918203684832716283019655932542975))), uint256(4)))));
    // VeriPUT synthesized this call from source declarations; the
    // certified region and all oracle claims still come from ESBMC.
    vm.deal(p_msg_sender, p_msg_value);
    vm.prank(p_msg_sender);
    (bool _esbmc_value_gate_ok, ) = address(c0).call{value: p_msg_value}(abi.encodeWithSignature("reflect(uint256)", tAmount));
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_marketing = (uint256(vm.load(address(c0), bytes32(uint256(11)))) & 1461501637330902918203684832716283019655932542975);
    _post_MAX = uint256(vm.load(address(c0), bytes32(uint256(12))));
    _post_tTotal = uint256(vm.load(address(c0), bytes32(uint256(13))));
    _post_rTotal = uint256(vm.load(address(c0), bytes32(uint256(14))));
    _post_tFeeTotal = uint256(vm.load(address(c0), bytes32(uint256(15))));
    _post_isExcluded_msg_sender = (uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(8))))) & 255);
    _post_rOwned_state_marketing = uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(11)))) & 1461501637330902918203684832716283019655932542975))), uint256(4)))));
    assertEq(_post_marketing, _pre_marketing, "marketing: post == pre");
    assertEq(_post_MAX, _pre_MAX, "MAX: post == pre");
    assertEq(_post_tTotal, _pre_tTotal, "_tTotal: post == pre");
    assertEq(_post_rTotal, _pre_rTotal, "_rTotal: post == pre");
    assertEq(_post_tFeeTotal, _pre_tFeeTotal, "_tFeeTotal: post == pre");
    assertEq(_post_isExcluded_msg_sender, _pre_isExcluded_msg_sender, "_isExcluded[msg.sender]: post == pre");
    assertEq(_post_rOwned_state_marketing, _pre_rOwned_state_marketing, "_rOwned[state.marketing]: post == pre");
    assertGe(_post_marketing, _pre_marketing, "marketing: post >= pre");
    assertLe(_post_marketing, _pre_marketing, "marketing: post <= pre");
    assertGe(_post_MAX, _pre_MAX, "MAX: post >= pre");
    assertLe(_post_MAX, _pre_MAX, "MAX: post <= pre");
    assertGe(_post_tTotal, _pre_tTotal, "_tTotal: post >= pre");
    assertLe(_post_tTotal, _pre_tTotal, "_tTotal: post <= pre");
    assertGe(_post_rTotal, _pre_rTotal, "_rTotal: post >= pre");
    assertLe(_post_rTotal, _pre_rTotal, "_rTotal: post <= pre");
    assertGe(_post_tFeeTotal, _pre_tFeeTotal, "_tFeeTotal: post >= pre");
    assertLe(_post_tFeeTotal, _pre_tFeeTotal, "_tFeeTotal: post <= pre");
    assertGe(_post_rOwned_state_marketing, _pre_rOwned_state_marketing, "_rOwned[state.marketing]: post >= pre");
    assertLe(_post_rOwned_state_marketing, _pre_rOwned_state_marketing, "_rOwned[state.marketing]: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_esbmc_value_gate_ok, "value sent to a non-payable entry must revert");

    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    // Structural ABI-gate replay: R0 is already asserted over the certified nonzero-value region.
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_BERNIE_reflect_path2p1(address p_msg_sender, uint256 p_msg_value, uint256 tAmount) public {
    _veriput_parameterized(p_msg_sender, p_msg_value, tAmount);
  }
}
