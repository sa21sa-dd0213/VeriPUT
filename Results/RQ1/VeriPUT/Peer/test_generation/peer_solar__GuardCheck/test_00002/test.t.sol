// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {GuardCheck} from "../src/flat.sol";

contract GuardCheckCovTest_GuardCheck_donate_put6p1 is Test {
  GuardCheck c0;
  function setUp() public {
    c0 = new GuardCheck();
  }
  // claim: sol:@C@GuardCheck@F@donate#84:path:31
  // witness-fingerprint-sha256: 9e463d9cd1c834a304815f56e1b5cea373b049f5c22fd535b4f2435bb53dd762
  // claim: sol:@C@GuardCheck@F@donate#84:path:123
  // witness-fingerprint-sha256: af2abac192dbf486e9ec9dcd7190e5476b7a44d8ed2555b8f66c9804bd2f6be4
  // claim: sol:@C@GuardCheck@F@donate#84:path:122
  // witness-fingerprint-sha256: 47e9cf67e86e8c5f8b52716cd187c74a7f5c5b88e88e08721b4ca92ed4a423b2
  // claim: sol:@C@GuardCheck@F@donate#84:path:59
  // witness-fingerprint-sha256: df19ab47db18359d90be42bce33f0db3e0cdecfea2b20f364c997fd5fa41556a
  // claim: sol:@C@GuardCheck@F@donate#84:path:6
  // witness-fingerprint-sha256: ac3a75146b7e59b8a86b2f1ec99844a27247cb88feda64a077fbd24e17e98cb4
  // claim: sol:@C@GuardCheck@F@donate#84:path:2
  // witness-fingerprint-sha256: d172cb0205a5b67517d7524ef5df83707b282c37935ef58aa08fb7f8f9d11d06

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@GuardCheck@F@donate#84:path:6   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   addr in [4294967296, 1461501637330902918203684832716283019655932542975]
  //   block.number in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   block.timestamp in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   msg.sender in [0, 4294967295]
  //   msg.value in [0, 0]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.prevrandao == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'122': '(none)', '123': '(none)', '2': 'addr==0', '31': '(none)', '59': '(none)', '6': 'msg.value==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, block.timestamp, block.number, addr
  // ORACLE: the FIRST LAYER ONLY, and that is the rule rather than a shortfall.
  // Stage 1 reports this complete path exits through a revert. Post-state
  // change and return-value rungs are not observable on the chain for this path.
  // They are dropped and counted below. Frame rungs such as `post == pre`
  // remain executable after the revert because the chain observes restored
  // storage. The exit itself is also asserted: the call MUST fail.
  // That is a real oracle -- it goes RED on a
  // contract that stops reverting -- and it replaces a `try {} catch {}`
  // body that could not fail whatever happened.
  //   path guard ESTABLISHED: `!(addr != 0)` -> `vm.assume(uint256(uint160(addr)) != 0);`
  //   environment ESTABLISHED: msg.sender in [0, 4294967295]  \ {0} is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it, and the 1 punched value(s) are excluded by vm.assume; 0 is excluded because Foundry cannot prank address(0), and the remaining executable subregion is a subset of the certified region (and funded with `vm.deal(p_msg_sender, 0)`, because the call sends value and the sender pays) (no prank was present, so one was inserted)
  //   environment ESTABLISHED: msg.value in [0, 0] is ESTABLISHED: the payable high-level call now carries the certified constant `0`
  //   environment ESTABLISHED: block.timestamp in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: vm.warp now takes the bound() fuzz parameter `p_block_timestamp`
  //   environment ESTABLISHED: block.number in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: vm.roll now takes the bound() fuzz parameter `p_block_number`
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, uint256 p_block_timestamp, uint256 p_block_number, address addr) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 0, 4294967295)));
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    p_block_timestamp = bound(p_block_timestamp, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    p_block_number = bound(p_block_number, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    addr = address(uint160(bound(uint256(uint160(addr)), 4294967296, 1461501637330902918203684832716283019655932542975)));
    // complete-path guard recovered from the emit report
    vm.assume(uint256(uint160(addr)) != 0);
    // [revert-tolerant] outcome not asserted
    vm.deal(p_msg_sender, 0);
    vm.warp(p_block_timestamp);
    vm.roll(p_block_number);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    bool _put_ok = true;
    try c0.donate{value: 0}(addr) {} catch { _put_ok = false; }
    // VERIPUT_FIXED_REPLAY_ASSERTIONS // R0 is already region-wide
    assertFalse(_put_ok, "path enc=6p1 exits through a REVERT: the call must fail on the unmodified contract");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_GuardCheck_donate_path6p1(address p_msg_sender, uint256 p_block_timestamp, uint256 p_block_number, address addr) public {
    _veriput_parameterized(p_msg_sender, p_block_timestamp, p_block_number, addr);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { GuardCheckCovTest_GuardCheck_donate_concrete6p1__basis_root__W _veriput_w = new GuardCheckCovTest_GuardCheck_donate_concrete6p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_4(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract GuardCheckCovTest_GuardCheck_donate_concrete6p1__basis_root__W is Test {
  GuardCheck c0;
  function setUp() public {
    c0 = new GuardCheck();
  }
  // claim: sol:@C@GuardCheck@F@donate#84:path:31
  // witness-fingerprint-sha256: 9e463d9cd1c834a304815f56e1b5cea373b049f5c22fd535b4f2435bb53dd762
  // claim: sol:@C@GuardCheck@F@donate#84:path:123
  // witness-fingerprint-sha256: af2abac192dbf486e9ec9dcd7190e5476b7a44d8ed2555b8f66c9804bd2f6be4
  // claim: sol:@C@GuardCheck@F@donate#84:path:122
  // witness-fingerprint-sha256: 47e9cf67e86e8c5f8b52716cd187c74a7f5c5b88e88e08721b4ca92ed4a423b2
  // claim: sol:@C@GuardCheck@F@donate#84:path:59
  // witness-fingerprint-sha256: df19ab47db18359d90be42bce33f0db3e0cdecfea2b20f364c997fd5fa41556a
  // claim: sol:@C@GuardCheck@F@donate#84:path:6
  // witness-fingerprint-sha256: ac3a75146b7e59b8a86b2f1ec99844a27247cb88feda64a077fbd24e17e98cb4
  function _w_test_cov_4() public {
    // [revert-tolerant] outcome not asserted
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(4294967295)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    try c0.donate(address(uint160(1461501637330902918203684832716283019655932542975))) {
      _veriput_concrete_completed = true;
    } catch {}
    assertFalse(_veriput_concrete_completed, "fixed witness call must revert");
  }
  // claim: sol:@C@GuardCheck@F@donate#84:path:2
  // witness-fingerprint-sha256: d172cb0205a5b67517d7524ef5df83707b282c37935ef58aa08fb7f8f9d11d06
}
