// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {GOLIATH} from "../src/flat.sol";

contract GOLIATHCovTest_GOLIATH_transfer_put31p1_p1_part_part0_w_r is Test {
  GOLIATH c0;
  function setUp() public {
    c0 = new GOLIATH();
  }
  // claim: sol:@C@GOLIATH@F@transfer#287:path:31
  // witness-fingerprint-sha256: 0cbd7b36917af6acb04f213f1395e20d32c40033630a2f176bceb73c4c4f09e9
  // claim: sol:@C@GOLIATH@F@transfer#287:path:30
  // witness-fingerprint-sha256: 08b72ec9566eea66e74a06bc916a4a1fea61f0e470961c28dccbb97e9c95b4c5
  // claim: sol:@C@GOLIATH@F@transfer#287:path:14
  // witness-fingerprint-sha256: a79f19a96232405ea8dd6a643f504f0c107723feab174b74c9ba9694e4dab0b3
  // claim: sol:@C@GOLIATH@F@transfer#287:path:6
  // witness-fingerprint-sha256: 8647e621e5f31bff8df06c32e12ebd3b2a6b9015be7ccc12dd7340de01364771
  // claim: sol:@C@GOLIATH@F@transfer#287:path:2
  // witness-fingerprint-sha256: 584d4fcbc530a546316e44e6008f7eb2ba8ddb78676893bc4cdba1469387c7f4

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@GOLIATH@F@transfer#287:path:31   depth=4
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   amount in [0, 0]
  //   msg.sender in [2093481895, 4186961191]
  //   recipient in [1, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': 'recipient==0', '30': '(none)', '31': 'amount==0', '6': 'msg.sender==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, recipient, amount
  // ORACLE: 22 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 21 over POST-STATE, read through vm.load at
  // the slot solc reports, and 1 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): mainWallet: post >= pre (entailed by a stronger rung that also HOLDS on mainWallet, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): mainWallet: post <= pre (entailed by a stronger rung that also HOLDS on mainWallet, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): taxPercent: post >= pre (entailed by a stronger rung that also HOLDS on taxPercent, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): taxPercent: post <= pre (entailed by a stronger rung that also HOLDS on taxPercent, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[msg.sender]: post >= pre (entailed by a stronger rung that also HOLDS on _balances[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[msg.sender]: post <= pre (entailed by a stronger rung that also HOLDS on _balances[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[recipient]: post >= pre (entailed by a stronger rung that also HOLDS on _balances[recipient], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[recipient]: post <= pre (entailed by a stronger rung that also HOLDS on _balances[recipient], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[state.mainWallet]: post >= pre (entailed by a stronger rung that also HOLDS on _balances[state.mainWallet], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _balances[state.mainWallet]: post <= pre (entailed by a stronger rung that also HOLDS on _balances[state.mainWallet], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): mainWallet: post == (pre + amount) (entailed by a stronger rung that also HOLDS on mainWallet, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): taxPercent: post == (pre + amount) (entailed by a stronger rung that also HOLDS on taxPercent, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): taxPercent: post in [state.taxPercent, state.taxPercent] (entailed by a stronger rung that also HOLDS on taxPercent, so asserting it detects nothing the stronger one misses)
  //   path guard ESTABLISHED: `!(sender != 0)` -> `vm.assume(uint256(uint160(p_msg_sender)) != 0);`
  //   path guard ESTABLISHED: `!(recipient != 0)` -> `vm.assume(uint256(uint160(recipient)) != 0);`
  //   path guard ESTABLISHED: `!(senderBalance >= amount)` -> `vm.assume(_pre_balances_msg_sender >= 0);`
  //   environment ESTABLISHED: msg.sender in [2093481895, 4186961191] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(1)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  // 10 snapshot local(s) HOISTED to storage: 3 fuzz parameter(s) + 10 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_mainWallet; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_taxPercent; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_balances_msg_sender; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_balances_recipient; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_balances_state_mainWallet; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_mainWallet; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_taxPercent; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_balances_msg_sender; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_balances_recipient; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_balances_state_mainWallet; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, address recipient, uint256 amount) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 2093481895, 4186961191)));
    recipient = address(uint160(bound(uint256(uint160(recipient)), 1, 1461501637330902918203684832716283019655932542975)));
    amount = bound(amount, 0, 0);
    // pre-state for the oracle, at this path's own entry
    _pre_mainWallet = (uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975);
    _pre_taxPercent = ((uint256(vm.load(address(c0), bytes32(uint256(6)))) >> 160) & 255);
    _pre_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    _pre_balances_recipient = uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(1)))));
    _pre_balances_state_mainWallet = uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975))), uint256(1)))));
    // complete-path guard recovered from the emit report
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    vm.assume(uint256(uint160(recipient)) != 0);
    vm.assume(_pre_balances_msg_sender >= 0);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [asserted] path exits normally; a revert fails the test
    bool _put_ret = c0.transfer(recipient, amount);
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(2093481895)) && recipient == address(uint160(1)) && amount == uint256(0)) {
      assertEq(_put_ret, true, "fixed witness return");
      assertEq(uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(1)), uint256(1))))), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(2093481895)), uint256(1))))), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(3)))), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq(((uint256(vm.load(address(c0), bytes32(uint256(6)))) >> 160) & 255), uint256(1), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_mainWallet = (uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975);
    _post_taxPercent = ((uint256(vm.load(address(c0), bytes32(uint256(6)))) >> 160) & 255);
    _post_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    _post_balances_recipient = uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(1)))));
    _post_balances_state_mainWallet = uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975))), uint256(1)))));
    assertEq(_post_mainWallet, _pre_mainWallet, "mainWallet: post == pre");
    assertEq(_post_taxPercent, _pre_taxPercent, "taxPercent: post == pre");
    assertEq(_post_balances_msg_sender, _pre_balances_msg_sender, "_balances[msg.sender]: post == pre");
    assertEq(_post_balances_recipient, _pre_balances_recipient, "_balances[recipient]: post == pre");
    assertEq(_post_balances_state_mainWallet, _pre_balances_state_mainWallet, "_balances[state.mainWallet]: post == pre");
    unchecked { assertEq(_post_balances_msg_sender, (uint256(_pre_balances_msg_sender) - uint256(amount)), "_balances[msg.sender]: post == (state._balances[msg.sender] - amount)"); }
    assertGe(_post_balances_recipient, _pre_balances_recipient, "_balances[recipient]: post - pre in [amount, amount] with post >= pre");
    unchecked { assertGe(_post_balances_recipient - _pre_balances_recipient, amount, "_balances[recipient]: post - pre in [amount, amount] with post >= pre"); }
    unchecked { assertLe(_post_balances_recipient - _pre_balances_recipient, amount, "_balances[recipient]: post - pre in [amount, amount] with post >= pre"); }
    assertEq(_post_mainWallet, _pre_mainWallet, "mainWallet: post == state.mainWallet");
    assertGe(_post_mainWallet, 0, "mainWallet: post in [0, pre]");
    assertLe(_post_mainWallet, _pre_mainWallet, "mainWallet: post in [0, pre]");
    assertGe(_post_mainWallet, 0, "mainWallet: post in [0, (amount + 1461501637330902918203684832716283019655932542975)]");
    unchecked { assertLe(_post_mainWallet, (uint256(amount) + uint256(1461501637330902918203684832716283019655932542975)), "mainWallet: post in [0, (amount + 1461501637330902918203684832716283019655932542975)]"); }
    assertEq(_post_taxPercent, _pre_taxPercent, "taxPercent: post == state.taxPercent");
    assertGe(_post_taxPercent, 0, "taxPercent: post in [0, pre]");
    assertLe(_post_taxPercent, _pre_taxPercent, "taxPercent: post in [0, pre]");
    assertGe(_post_taxPercent, 0, "taxPercent: post in [0, state.taxPercent]");
    assertLe(_post_taxPercent, _pre_taxPercent, "taxPercent: post in [0, state.taxPercent]");
    assertGe(_post_taxPercent, 0, "taxPercent: post in [0, (amount + 255)]");
    unchecked { assertLe(_post_taxPercent, (uint256(amount) + uint256(255)), "taxPercent: post in [0, (amount + 255)]"); }
    assertTrue(_put_ret, "return: return == true");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_GOLIATH_transfer_path31p1_part_part0_w_r(address p_msg_sender, address recipient, uint256 amount) public {
    _veriput_parameterized(p_msg_sender, recipient, amount);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { GOLIATHCovTest_GOLIATH_transfer_concrete31p1__basis_part0_w_r__W _veriput_w = new GOLIATHCovTest_GOLIATH_transfer_concrete31p1__basis_part0_w_r__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract GOLIATHCovTest_GOLIATH_transfer_concrete31p1__basis_part0_w_r__W is Test {
  GOLIATH c0;
  function setUp() public {
    c0 = new GOLIATH();
  }
  // claim: sol:@C@GOLIATH@F@transfer#287:path:31
  // witness-fingerprint-sha256: 0cbd7b36917af6acb04f213f1395e20d32c40033630a2f176bceb73c4c4f09e9
  function _w_test_cov_0() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(0) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "entry pin state._owner$44 did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(3))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(3)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(3)))), uint256(0), "entry pin state._totalSupply did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(6))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(0) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(6)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "entry pin state.mainWallet did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(6))));
      _w = (_w & ~uint256(372682917519380244141939632342652170012262798458880)) | ((uint256(1) & 255) << 160);
      vm.store(address(c0), bytes32(uint256(6)), bytes32(_w));
    }
    assertEq(((uint256(vm.load(address(c0), bytes32(uint256(6)))) >> 160) & 255), uint256(1), "entry pin state.taxPercent did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // [asserted] path exits normally; a revert fails the test
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(2093481895)), address(uint160(0)));
    bool _veriput_concrete_return = c0.transfer(address(uint160(1)), uint256(0));
    assertEq(_veriput_concrete_return, true, "fixed witness return must match");
    uint256 _veriput_fixed_state_balances_1_1 = uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(1)), uint256(1)))));
    assertEq(_veriput_fixed_state_balances_1_1, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_balances_2093481895_2 = uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(2093481895)), uint256(1)))));
    assertEq(_veriput_fixed_state_balances_2093481895_2, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_owner_44_3 = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_owner_44_3, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_totalSupply_4 = uint256(vm.load(address(c0), bytes32(uint256(3))));
    assertEq(_veriput_fixed_state_totalSupply_4, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_mainWallet_5 = (uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_mainWallet_5, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_taxPercent_6 = ((uint256(vm.load(address(c0), bytes32(uint256(6)))) >> 160) & 255);
    assertEq(_veriput_fixed_state_taxPercent_6, uint256(1), "fixed witness state");
  }
  // claim: sol:@C@GOLIATH@F@transfer#287:path:30
  // witness-fingerprint-sha256: 08b72ec9566eea66e74a06bc916a4a1fea61f0e470961c28dccbb97e9c95b4c5
  // claim: sol:@C@GOLIATH@F@transfer#287:path:14
  // witness-fingerprint-sha256: a79f19a96232405ea8dd6a643f504f0c107723feab174b74c9ba9694e4dab0b3
  // claim: sol:@C@GOLIATH@F@transfer#287:path:6
  // witness-fingerprint-sha256: 8647e621e5f31bff8df06c32e12ebd3b2a6b9015be7ccc12dd7340de01364771
  // claim: sol:@C@GOLIATH@F@transfer#287:path:2
  // witness-fingerprint-sha256: 584d4fcbc530a546316e44e6008f7eb2ba8ddb78676893bc4cdba1469387c7f4
}
