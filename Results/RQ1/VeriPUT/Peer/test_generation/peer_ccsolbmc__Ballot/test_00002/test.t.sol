// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {Ballot} from "../src/flat.sol";

contract BallotCovTest_0_Ballot_con1F_put7p1 is Test {
  Ballot c0;
  function setUp() public {
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new Ballot();
    vm.stopPrank();
  }
  // claim: sol:@C@Ballot@F@con1F#71:path:7
  // witness-fingerprint-sha256: 1a18a9b5233e1cc01f30dbba774f248db69419310a7184649b03820f4474822d

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@Ballot@F@con1F#71:path:7   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   proposalNames.length in [0, 0]
  //   state.chairperson in [0, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'7': 'proposalNames.length==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, proposalNames
  // ORACLE: 8 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 8 over POST-STATE, read through vm.load at
  // the slot solc reports, and 0 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung DROPPED: state.proposalNames.length (no storage slot: solc's layout does not list it, so the R2 endpoint cannot be read before the call)
  //   rung IMPLIED (not lost, not asserted twice): voters[msg.sender].weight: post != pre (entailed by a stronger rung that also HOLDS on voters[msg.sender].weight, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[msg.sender].weight: post >= pre (entailed by a stronger rung that also HOLDS on voters[msg.sender].weight, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[msg.sender].delegate: post >= pre (entailed by a stronger rung that also HOLDS on voters[msg.sender].delegate, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[msg.sender].delegate: post <= pre (entailed by a stronger rung that also HOLDS on voters[msg.sender].delegate, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[state.chairperson].delegate: post >= pre (entailed by a stronger rung that also HOLDS on voters[state.chairperson].delegate, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[state.chairperson].delegate: post <= pre (entailed by a stronger rung that also HOLDS on voters[state.chairperson].delegate, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[msg.sender].vote: post >= pre (entailed by a stronger rung that also HOLDS on voters[msg.sender].vote, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[msg.sender].vote: post <= pre (entailed by a stronger rung that also HOLDS on voters[msg.sender].vote, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[state.chairperson].vote: post >= pre (entailed by a stronger rung that also HOLDS on voters[state.chairperson].vote, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): voters[state.chairperson].vote: post <= pre (entailed by a stronger rung that also HOLDS on voters[state.chairperson].vote, so asserting it detects nothing the stronger one misses)
  //   path guard DROPPED: 'i < proposalNames.length' (`i` is assigned inside the unit before this source decision may execute, so it is not the calldata coordinate)
  //   entry-state bound DROPPED: state.chairperson in [0, 1461501637330902918203684832716283019655932542975] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query -- the rungs were proved about the constructor's own value. Establishing a fuzz-chosen value here would test entry states the proof never covered, which is how this PUT came back RED on the unmodified contract. Both endpoints are the type's own limits, so there is not even an in-region check to make: the bound says nothing at all)
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (no prank was present, so one was inserted)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  // 16 snapshot local(s) HOISTED to storage: 2 fuzz parameter(s) + 16 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_voters_msg_sender__weight; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_voters_msg_sender__delegate; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_voters_state_chairperson__delegate; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_voters_msg_sender__vote; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_voters_state_chairperson__vote; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_voters_msg_sender__voted; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_voters_state_chairperson__voted; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_chairperson; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_voters_msg_sender__weight; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_voters_msg_sender__delegate; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_voters_state_chairperson__delegate; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_voters_msg_sender__vote; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_voters_state_chairperson__vote; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_voters_msg_sender__voted; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_voters_state_chairperson__voted; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_chairperson; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, bytes32[] memory proposalNames) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    proposalNames = new bytes32[](0);
    // pre-state for the oracle, at this path's own entry
    _pre_voters_msg_sender__weight = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    _pre_voters_msg_sender__delegate = ((uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(1)))) + 1))) >> 8) & 1461501637330902918203684832716283019655932542975);
    _pre_voters_state_chairperson__delegate = ((uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975))), uint256(1)))) + 1))) >> 8) & 1461501637330902918203684832716283019655932542975);
    _pre_voters_msg_sender__vote = uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(1)))) + 2)));
    _pre_voters_state_chairperson__vote = uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975))), uint256(1)))) + 2)));
    _pre_voters_msg_sender__voted = (uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(1)))) + 1))) & 255);
    _pre_voters_state_chairperson__voted = (uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975))), uint256(1)))) + 1))) & 255);
    _pre_chairperson = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    // [asserted] path exits normally; a revert fails the test
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    c0.con1F(proposalNames);
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(0)) && proposalNames.length == uint256(0)) {
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_voters_msg_sender__weight = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    _post_voters_msg_sender__delegate = ((uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(1)))) + 1))) >> 8) & 1461501637330902918203684832716283019655932542975);
    _post_voters_state_chairperson__delegate = ((uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975))), uint256(1)))) + 1))) >> 8) & 1461501637330902918203684832716283019655932542975);
    _post_voters_msg_sender__vote = uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(1)))) + 2)));
    _post_voters_state_chairperson__vote = uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975))), uint256(1)))) + 2)));
    _post_voters_msg_sender__voted = (uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(p_msg_sender, uint256(1)))) + 1))) & 255);
    _post_voters_state_chairperson__voted = (uint256(vm.load(address(c0), bytes32(uint256(keccak256(abi.encode(address(uint160((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975))), uint256(1)))) + 1))) & 255);
    _post_chairperson = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    assertGt(_post_voters_msg_sender__weight, _pre_voters_msg_sender__weight, "voters[msg.sender].weight: post > pre");
    assertEq(_post_voters_msg_sender__delegate, _pre_voters_msg_sender__delegate, "voters[msg.sender].delegate: post == pre");
    assertEq(_post_voters_state_chairperson__delegate, _pre_voters_state_chairperson__delegate, "voters[state.chairperson].delegate: post == pre");
    assertEq(_post_voters_msg_sender__vote, _pre_voters_msg_sender__vote, "voters[msg.sender].vote: post == pre");
    assertEq(_post_voters_state_chairperson__vote, _pre_voters_state_chairperson__vote, "voters[state.chairperson].vote: post == pre");
    assertEq(_post_voters_msg_sender__voted, _pre_voters_msg_sender__voted, "voters[msg.sender].voted: post == pre");
    assertEq(_post_voters_state_chairperson__voted, _pre_voters_state_chairperson__voted, "voters[state.chairperson].voted: post == pre");
    assertEq(_post_chairperson, uint256(uint160(p_msg_sender)), "chairperson: post == msg.sender");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_Ballot_con1F_path7p1(address p_msg_sender, bytes32[] memory proposalNames) public {
    _veriput_parameterized(p_msg_sender, proposalNames);
  }
}

contract BallotCovTest_1 is Test {
  Ballot c0;
  function setUp() public {
    c0 = new Ballot();
  }
  // claim: sol:@C@Ballot@F@con1F#71:path:2
  // witness-fingerprint-sha256: 067146b0450ad4f36c284b0d6b106a844bcb235045ef6c2ba2563d859d43a5f7
}
