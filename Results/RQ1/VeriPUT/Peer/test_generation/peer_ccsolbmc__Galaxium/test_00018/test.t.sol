// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {Galaxium} from "../src/flat.sol";

contract GalaxiumCovTest_Galaxium_transfer_put126p1 is Test {
  Galaxium c0;
  function setUp() public {
    c0 = new Galaxium();
  }
  // claim: sol:@C@Galaxium@F@transfer#337:path:127
  // witness-fingerprint-sha256: aa11d04a7801142823c6a6b6979cacf3a1d2636d9b43eb66df752d0f9ee927a3
  // claim: sol:@C@Galaxium@F@transfer#337:path:126
  // witness-fingerprint-sha256: 524c44b443b467b9c2fb5e653afee9d0968c6fba789f579e82b455a84ab96f89
  // claim: sol:@C@Galaxium@F@transfer#337:path:30
  // witness-fingerprint-sha256: 5ad56ed35e18a9829a3cdc38756ed1bb027868b456875f0ce8f6b053cd2f43a0
  // claim: sol:@C@Galaxium@F@transfer#337:path:14
  // witness-fingerprint-sha256: 5ad56d1e3040c0b8527325300a9954711f1dc2b804befe1816ad874094003951
  // claim: sol:@C@Galaxium@F@transfer#337:path:2
  // witness-fingerprint-sha256: 4d8cc0677ce449d927c8433fb677417cfe4fe7ff48830d614d7e5bdfcddebdba

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@Galaxium@F@transfer#337:path:126   depth=6
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   amount in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]  \ {0}
  //   msg.sender in [1, 11348359941645608]
  //   recipient in [1, 1461501637330902918203684832716283019655932542975]
  //   state.bots$230[recipient] in [0, 0]
  //   state.paused$155 in [0, 0]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'126': 'state.paused$155==0', '127': 'amount==0, state.paused$155==0', '14': 'msg.sender==0, state.paused$155==0', '30': 'recipient==0, state.paused$155==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, recipient, amount
  // ORACLE: rollback exit plus 9 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   rung DROPPED: state.bots[sender] (the key `sender` is not a declared parameter of this unit, so the PUT has no expression for it)
  //   rung DROPPED: all return rungs DROPPED: the emitted call is a `try`/`catch` statement -- that shape IS this path's exit-kind expectation, and a reverting execution has no return value to assert
  //   path guard ESTABLISHED: `!(sender != 0)` -> `vm.assume(uint256(uint160(p_msg_sender)) != 0);`
  //   path guard ESTABLISHED: `!(recipient != 0)` -> `vm.assume(uint256(uint160(recipient)) != 0);`
  //   path guard ESTABLISHED: `!(!(senderBalance >= amount))` -> `vm.assume(_pre_balances_msg_sender < amount);`
  //   path guard DROPPED: '!(!bots[sender] && !bots[recipient])' (`bots[sender]` is not nameable in this PUT)
  //   environment ESTABLISHED: msg.sender in [1, 11348359941645608] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(1)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  // 10 snapshot local(s) HOISTED to storage: 3 fuzz parameter(s) + 10 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_paused; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_bots_230_recipient; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_bots_recipient; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_balances_msg_sender; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_balances_recipient; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_paused; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_bots_230_recipient; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_bots_recipient; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_balances_msg_sender; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_balances_recipient; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, address recipient, uint256 amount) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 11348359941645608)));
    recipient = address(uint160(bound(uint256(uint160(recipient)), 1, 1461501637330902918203684832716283019655932542975)));
    amount = bound(amount, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    vm.assume(amount != 0);
    // entry state the certified region names, ESTABLISHED (not assumed):
    //   state.bots$230[recipient] := 0; state.paused$155 := 0
    {
      uint256 _w = uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(3)))));
      _w = (_w & ~uint256(255)) | ((uint256(0) & 255) << 0);
      vm.store(address(c0), keccak256(abi.encode(recipient, uint256(3))), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(3))))) & 255), uint256(0), "entry pin state.bots$230[recipient] did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(372682917519380244141939632342652170012262798458880)) | ((uint256(0) & 255) << 160);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq(((uint256(vm.load(address(c0), bytes32(uint256(0)))) >> 160) & 255), uint256(0), "entry pin state.paused$155 did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // pre-state for the oracle, at this path's own entry
    _pre_paused = ((uint256(vm.load(address(c0), bytes32(uint256(0)))) >> 160) & 255);
    _pre_bots_230_recipient = (uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(3))))) & 255);
    _pre_bots_recipient = (uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(3))))) & 255);
    _pre_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    _pre_balances_recipient = uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(1)))));
    // complete-path guard recovered from the emit report
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    vm.assume(uint256(uint160(recipient)) != 0);
    vm.assume(_pre_balances_msg_sender < amount);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [revert-tolerant] outcome not asserted
    bool _put_ok = true;
    try c0.transfer(recipient, amount) {} catch { _put_ok = false; }
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(1)) && recipient == address(uint160(1)) && amount == uint256(57896044618658097711785492504343953926634992332820282019728792003956564819968)) {
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(4)))), uint256(0), "fixed witness state");
      assertEq(((uint256(vm.load(address(c0), bytes32(uint256(0)))) >> 160) & 255), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_paused = ((uint256(vm.load(address(c0), bytes32(uint256(0)))) >> 160) & 255);
    _post_bots_230_recipient = (uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(3))))) & 255);
    _post_bots_recipient = (uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(3))))) & 255);
    _post_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    _post_balances_recipient = uint256(vm.load(address(c0), keccak256(abi.encode(recipient, uint256(1)))));
    assertEq(_post_paused, _pre_paused, "paused: post == pre");
    assertEq(_post_bots_230_recipient, _pre_bots_230_recipient, "bots$230[recipient]: post == pre");
    assertEq(_post_bots_recipient, _pre_bots_recipient, "bots[recipient]: post == pre");
    assertEq(_post_balances_msg_sender, _pre_balances_msg_sender, "_balances[msg.sender]: post == pre");
    assertEq(_post_balances_recipient, _pre_balances_recipient, "_balances[recipient]: post == pre");
    assertGe(_post_balances_msg_sender, _pre_balances_msg_sender, "_balances[msg.sender]: post >= pre");
    assertLe(_post_balances_msg_sender, _pre_balances_msg_sender, "_balances[msg.sender]: post <= pre");
    assertGe(_post_balances_recipient, _pre_balances_recipient, "_balances[recipient]: post >= pre");
    assertLe(_post_balances_recipient, _pre_balances_recipient, "_balances[recipient]: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_put_ok, "path enc=126p1 exits through a REVERT: the call must fail on the unmodified contract");
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_Galaxium_transfer_path126p1(address p_msg_sender, address recipient, uint256 amount) public {
    _veriput_parameterized(p_msg_sender, recipient, amount);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { GalaxiumCovTest_Galaxium_transfer_concrete126p1__basis_root__W _veriput_w = new GalaxiumCovTest_Galaxium_transfer_concrete126p1__basis_root__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_1(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract GalaxiumCovTest_Galaxium_transfer_concrete126p1__basis_root__W is Test {
  Galaxium c0;
  function setUp() public {
    c0 = new Galaxium();
  }
  // claim: sol:@C@Galaxium@F@transfer#337:path:127
  // witness-fingerprint-sha256: aa11d04a7801142823c6a6b6979cacf3a1d2636d9b43eb66df752d0f9ee927a3
  // claim: sol:@C@Galaxium@F@transfer#337:path:126
  // witness-fingerprint-sha256: 524c44b443b467b9c2fb5e653afee9d0968c6fba789f579e82b455a84ab96f89
  function _w_test_cov_1() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(1461501637330902918203684832716283019655932542975)) | ((uint256(0) & 1461501637330902918203684832716283019655932542975) << 0);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq((uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "entry pin state._owner$49 did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(4))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(4)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(4)))), uint256(0), "entry pin state._totalSupply did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(372682917519380244141939632342652170012262798458880)) | ((uint256(0) & 255) << 160);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq(((uint256(vm.load(address(c0), bytes32(uint256(0)))) >> 160) & 255), uint256(0), "entry pin state.paused$155 did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // [revert-tolerant] outcome not asserted
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(1)), address(uint160(0)));
    bool _veriput_concrete_completed = false;
    try c0.transfer(address(uint160(1)), uint256(57896044618658097711785492504343953926634992332820282019728792003956564819968)) {
      _veriput_concrete_completed = true;
    } catch {}
    assertFalse(_veriput_concrete_completed, "fixed witness call must revert");
    uint256 _veriput_fixed_state_owner_49_0 = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_veriput_fixed_state_owner_49_0, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_totalSupply_1 = uint256(vm.load(address(c0), bytes32(uint256(4))));
    assertEq(_veriput_fixed_state_totalSupply_1, uint256(0), "fixed witness state");
    uint256 _veriput_fixed_state_paused_155_2 = ((uint256(vm.load(address(c0), bytes32(uint256(0)))) >> 160) & 255);
    assertEq(_veriput_fixed_state_paused_155_2, uint256(0), "fixed witness state");
  }
  // claim: sol:@C@Galaxium@F@transfer#337:path:30
  // witness-fingerprint-sha256: 5ad56ed35e18a9829a3cdc38756ed1bb027868b456875f0ce8f6b053cd2f43a0
  // claim: sol:@C@Galaxium@F@transfer#337:path:14
  // witness-fingerprint-sha256: 5ad56d1e3040c0b8527325300a9954711f1dc2b804befe1816ad874094003951
  // claim: sol:@C@Galaxium@F@transfer#337:path:2
  // witness-fingerprint-sha256: 4d8cc0677ce449d927c8433fb677417cfe4fe7ff48830d614d7e5bdfcddebdba
}
