// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {AddressArrayUtilsContract} from "../src/flat.sol";

contract AddressArrayUtilsContractCovTest_AddressArrayUtilsContract_contains_put7p1_p1_part_part0_r is Test {
  AddressArrayUtilsContract c0;
  function setUp() public {
    c0 = new AddressArrayUtilsContract(new address[](4));
  }
  // claim: sol:@C@AddressArrayUtilsContract@F@contains#1309:path:107, sol:@C@AddressArrayUtilsContract@F@contains#1309:path:27, sol:@C@AddressArrayUtilsContract@F@contains#1309:path:427, sol:@C@AddressArrayUtilsContract@F@contains#1309:path:7
  // witness-fingerprint-sha256: 74b51c844e772793fc36e69e2534fa075c069f0664b18141924926f447a3f39a
  // claim: sol:@C@AddressArrayUtilsContract@F@contains#1309:path:12
  // witness-fingerprint-sha256: fb13666b629888dce921f9bfdf0bbc92fb9b99549b4f6e78f2fcd80dd87a35e0
  // claim: sol:@C@AddressArrayUtilsContract@F@contains#1309:path:2
  // witness-fingerprint-sha256: b0052a65547cd34c722a2747d487fd2d0458c8ec611585705fe26ad13a0ca957

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@AddressArrayUtilsContract@F@contains#1309:path:7   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   A.length in [0, 0]
  //   a in [0, 0]
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'107': 'A.length==2', '12': 'a==0', '27': 'A.length==1', '427': 'A.length==3', '7': 'A.length==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, A, a
  // ORACLE: 1 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 0 over POST-STATE, read through vm.load at
  // the slot solc reports, and 1 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   path guard DROPPED: 'i < length' (`i` is not nameable in this PUT)
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (no prank was present, so one was inserted)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, address[] memory A, address a) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    A = new address[](0);
    a = address(uint160(bound(uint256(uint160(a)), 0, 0)));
    // [asserted] path exits normally; a revert fails the test
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    bool _put_ret = c0.contains(A, a);
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(0)) && A.length == uint256(0) && a == address(uint160(0))) {
      assertEq(_put_ret, false, "fixed witness return");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    assertFalse(_put_ret, "return: return == false");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_AddressArrayUtilsContract_contains_path7p1_part_part0_r(address p_msg_sender, address[] memory A, address a) public {
    _veriput_parameterized(p_msg_sender, A, a);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { AddressArrayUtilsContractCovTest_AddressArrayUtilsContract_contains_concrete7p1__basis_part0_r__W _veriput_w = new AddressArrayUtilsContractCovTest_AddressArrayUtilsContract_contains_concrete7p1__basis_part0_r__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract AddressArrayUtilsContractCovTest_AddressArrayUtilsContract_contains_concrete7p1__basis_part0_r__W is Test {
  AddressArrayUtilsContract c0;
  function setUp() public {
    c0 = new AddressArrayUtilsContract(new address[](4));
  }
  // claim: sol:@C@AddressArrayUtilsContract@F@contains#1309:path:107, sol:@C@AddressArrayUtilsContract@F@contains#1309:path:27, sol:@C@AddressArrayUtilsContract@F@contains#1309:path:427, sol:@C@AddressArrayUtilsContract@F@contains#1309:path:7
  // witness-fingerprint-sha256: 74b51c844e772793fc36e69e2534fa075c069f0664b18141924926f447a3f39a
  function _w_test_cov_0() public {
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(0))));
      _w = (_w & ~uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)) | ((uint256(0) & 115792089237316195423570985008687907853269984665640564039457584007913129639935) << 0);
      vm.store(address(c0), bytes32(uint256(0)), bytes32(_w));
    }
    assertEq(uint256(vm.load(address(c0), bytes32(uint256(0)))), uint256(0), "entry pin state.addressesA_dynarray_len[(&_ESBMC_Object_AddressArrayUtilsContract)->$address] did NOT land: vm.store wrote a word the contract does not read back at this slot, so the test is not inside the certified region and every rung below is about a different state");
    // [revert-tolerant] outcome not asserted
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(0)), address(uint160(0)));
    bool _veriput_concrete_return = c0.contains(new address[](0), address(uint160(0)));
    assertEq(_veriput_concrete_return, false, "fixed witness return must match");
  }
  // claim: sol:@C@AddressArrayUtilsContract@F@contains#1309:path:12
  // witness-fingerprint-sha256: fb13666b629888dce921f9bfdf0bbc92fb9b99549b4f6e78f2fcd80dd87a35e0
  // claim: sol:@C@AddressArrayUtilsContract@F@contains#1309:path:2
  // witness-fingerprint-sha256: b0052a65547cd34c722a2747d487fd2d0458c8ec611585705fe26ad13a0ca957
}
