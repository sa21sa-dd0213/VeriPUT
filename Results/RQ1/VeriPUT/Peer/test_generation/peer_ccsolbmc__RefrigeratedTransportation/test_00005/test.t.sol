// SPDX-License-Identifier: MIT
// Auto-generated by VeriPUT after ESBMC emitted no concrete test.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {RefrigeratedTransportation} from "../src/flat.sol";

contract RefrigeratedTransportationCovTest_RefrigeratedTransportation_asadf_put2p1 is Test {
  RefrigeratedTransportation c0;
  function setUp() public {
    c0 = new RefrigeratedTransportation();
  }
  // claim: sol:@C@RefrigeratedTransportation@F@asadf#123:path:2

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@RefrigeratedTransportation@F@asadf#123:path:2   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, msg.value, device, supplyChainOwner, supplyChainObserver
  // ORACLE: rollback exit plus 25 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (and funded with `vm.deal(p_msg_sender, p_msg_value)`, because the call sends value and the sender pays) (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: the compiler-only nonpayable reject replay was rewritten as a low-level value-bearing call taking the bound() fuzz parameter `p_msg_value`
  // 26 snapshot local(s) HOISTED to storage: 5 fuzz parameter(s) + 26 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_State; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_Owner; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_InitiatingCounterparty; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_Counterparty; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_Device; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_SupplyChainOwner; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_SupplyChainObserver; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_MinHumidity; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_MaxHumidity; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_MinTemperature; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_MaxTemperature; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_ComplianceSensorReading; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_ComplianceStatus; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_State; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_Owner; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_InitiatingCounterparty; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_Counterparty; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_Device; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_SupplyChainOwner; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_SupplyChainObserver; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_MinHumidity; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_MaxHumidity; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_MinTemperature; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_MaxTemperature; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_ComplianceSensorReading; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_ComplianceStatus; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, uint256 p_msg_value, address device, address supplyChainOwner, address supplyChainObserver) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    p_msg_value = bound(p_msg_value, 1, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    device = address(uint160(bound(uint256(uint160(device)), 0, 1461501637330902918203684832716283019655932542975)));
    supplyChainOwner = address(uint160(bound(uint256(uint160(supplyChainOwner)), 0, 1461501637330902918203684832716283019655932542975)));
    supplyChainObserver = address(uint160(bound(uint256(uint160(supplyChainObserver)), 0, 1461501637330902918203684832716283019655932542975)));
    // pre-state for the oracle, at this path's own entry
    _pre_State = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    _pre_Owner = ((uint256(vm.load(address(c0), bytes32(uint256(0)))) >> 8) & 1461501637330902918203684832716283019655932542975);
    _pre_InitiatingCounterparty = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 1461501637330902918203684832716283019655932542975);
    _pre_Counterparty = (uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975);
    _pre_Device = (uint256(vm.load(address(c0), bytes32(uint256(4)))) & 1461501637330902918203684832716283019655932542975);
    _pre_SupplyChainOwner = (uint256(vm.load(address(c0), bytes32(uint256(5)))) & 1461501637330902918203684832716283019655932542975);
    _pre_SupplyChainObserver = (uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975);
    _pre_MinHumidity = uint256(vm.load(address(c0), bytes32(uint256(7))));
    _pre_MaxHumidity = uint256(vm.load(address(c0), bytes32(uint256(8))));
    _pre_MinTemperature = uint256(vm.load(address(c0), bytes32(uint256(9))));
    _pre_MaxTemperature = uint256(vm.load(address(c0), bytes32(uint256(10))));
    _pre_ComplianceSensorReading = uint256(vm.load(address(c0), bytes32(uint256(12))));
    _pre_ComplianceStatus = (uint256(vm.load(address(c0), bytes32(uint256(13)))) & 255);
    // VeriPUT synthesized this call from source declarations; the
    // certified region and all oracle claims still come from ESBMC.
    vm.deal(p_msg_sender, p_msg_value);
    vm.prank(p_msg_sender);
    (bool _esbmc_value_gate_ok, ) = address(c0).call{value: p_msg_value}(abi.encodeWithSignature("asadf(address,address,address,int256,int256,int256,int256)", device, supplyChainOwner, supplyChainObserver, int256(1), int256(1), int256(1), int256(1)));
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_State = (uint256(vm.load(address(c0), bytes32(uint256(0)))) & 255);
    _post_Owner = ((uint256(vm.load(address(c0), bytes32(uint256(0)))) >> 8) & 1461501637330902918203684832716283019655932542975);
    _post_InitiatingCounterparty = (uint256(vm.load(address(c0), bytes32(uint256(1)))) & 1461501637330902918203684832716283019655932542975);
    _post_Counterparty = (uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975);
    _post_Device = (uint256(vm.load(address(c0), bytes32(uint256(4)))) & 1461501637330902918203684832716283019655932542975);
    _post_SupplyChainOwner = (uint256(vm.load(address(c0), bytes32(uint256(5)))) & 1461501637330902918203684832716283019655932542975);
    _post_SupplyChainObserver = (uint256(vm.load(address(c0), bytes32(uint256(6)))) & 1461501637330902918203684832716283019655932542975);
    _post_MinHumidity = uint256(vm.load(address(c0), bytes32(uint256(7))));
    _post_MaxHumidity = uint256(vm.load(address(c0), bytes32(uint256(8))));
    _post_MinTemperature = uint256(vm.load(address(c0), bytes32(uint256(9))));
    _post_MaxTemperature = uint256(vm.load(address(c0), bytes32(uint256(10))));
    _post_ComplianceSensorReading = uint256(vm.load(address(c0), bytes32(uint256(12))));
    _post_ComplianceStatus = (uint256(vm.load(address(c0), bytes32(uint256(13)))) & 255);
    assertEq(_post_State, _pre_State, "State: post == pre");
    assertEq(_post_Owner, _pre_Owner, "Owner: post == pre");
    assertEq(_post_InitiatingCounterparty, _pre_InitiatingCounterparty, "InitiatingCounterparty: post == pre");
    assertEq(_post_Counterparty, _pre_Counterparty, "Counterparty: post == pre");
    assertEq(_post_Device, _pre_Device, "Device: post == pre");
    assertEq(_post_SupplyChainOwner, _pre_SupplyChainOwner, "SupplyChainOwner: post == pre");
    assertEq(_post_SupplyChainObserver, _pre_SupplyChainObserver, "SupplyChainObserver: post == pre");
    assertEq(_post_MinHumidity, _pre_MinHumidity, "MinHumidity: post == pre");
    assertEq(_post_MaxHumidity, _pre_MaxHumidity, "MaxHumidity: post == pre");
    assertEq(_post_MinTemperature, _pre_MinTemperature, "MinTemperature: post == pre");
    assertEq(_post_MaxTemperature, _pre_MaxTemperature, "MaxTemperature: post == pre");
    assertEq(_post_ComplianceSensorReading, _pre_ComplianceSensorReading, "ComplianceSensorReading: post == pre");
    assertEq(_post_ComplianceStatus, _pre_ComplianceStatus, "ComplianceStatus: post == pre");
    assertGe(_post_Owner, _pre_Owner, "Owner: post >= pre");
    assertLe(_post_Owner, _pre_Owner, "Owner: post <= pre");
    assertGe(_post_InitiatingCounterparty, _pre_InitiatingCounterparty, "InitiatingCounterparty: post >= pre");
    assertLe(_post_InitiatingCounterparty, _pre_InitiatingCounterparty, "InitiatingCounterparty: post <= pre");
    assertGe(_post_Counterparty, _pre_Counterparty, "Counterparty: post >= pre");
    assertLe(_post_Counterparty, _pre_Counterparty, "Counterparty: post <= pre");
    assertGe(_post_Device, _pre_Device, "Device: post >= pre");
    assertLe(_post_Device, _pre_Device, "Device: post <= pre");
    assertGe(_post_SupplyChainOwner, _pre_SupplyChainOwner, "SupplyChainOwner: post >= pre");
    assertLe(_post_SupplyChainOwner, _pre_SupplyChainOwner, "SupplyChainOwner: post <= pre");
    assertGe(_post_SupplyChainObserver, _pre_SupplyChainObserver, "SupplyChainObserver: post >= pre");
    assertLe(_post_SupplyChainObserver, _pre_SupplyChainObserver, "SupplyChainObserver: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_esbmc_value_gate_ok, "value sent to a non-payable entry must revert");

    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    // Structural ABI-gate replay: R0 is already asserted over the certified nonzero-value region.
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_RefrigeratedTransportation_asadf_path2p1(address p_msg_sender, uint256 p_msg_value, address device, address supplyChainOwner, address supplyChainObserver) public {
    _veriput_parameterized(p_msg_sender, p_msg_value, device, supplyChainOwner, supplyChainObserver);
  }
}
