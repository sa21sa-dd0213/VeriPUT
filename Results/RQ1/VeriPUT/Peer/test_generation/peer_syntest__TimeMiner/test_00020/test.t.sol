// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {TimeMiner} from "../src/flat.sol";

contract TimeMinerCovTest_0_TimeMiner_totalSupply_concrete3_fb is Test {
  TimeMiner c0;
  function setUp() public {
    vm.warp(28894133473853308133840455678399317948584757907555434631641687012838857513629);
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new TimeMiner();
    vm.stopPrank();
  }
  // claim: sol:@C@TimeMiner@F@totalSupply#370:path:3
  // witness-fingerprint-sha256: 58cff533904a5df7573b10c070ef0dcacb9a3e625dbcf22814f23f1459993b7f
  function test_cov_0() public {
    vm.warp(29291674063428444607996697616066474614645941784352448897219048408129696950593);
    vm.prank(address(uint160(0)));
    // [asserted] path exits normally; a revert fails the test
    uint256 _veriput_concrete_return = c0.totalSupply();
    assertEq(_veriput_concrete_return, uint256(110427941548649020598956093796432407239217743554726184882600387580788756000000), "fixed witness return must match");
  }
}

contract TimeMinerCovTest_1 is Test {
  TimeMiner c0;
  function setUp() public {
    vm.warp(1);
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new TimeMiner();
    vm.stopPrank();
  }
  // claim: sol:@C@TimeMiner@F@totalSupply#370:path:2
  // witness-fingerprint-sha256: f735a891b693ed84864932e9b29e0d2a98ff485f061d1c782f4aaabbb8b6f28a
}
