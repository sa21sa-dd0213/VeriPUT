// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {TimeMiner} from "../src/flat.sol";

contract TimeMinerCovTest_0_TimeMiner_tokensToClaim_concrete3_fb is Test {
  TimeMiner c0;
  function setUp() public {
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129623552);
    vm.startPrank(address(uint160(4294967295)), address(uint160(4294967295)));
    c0 = new TimeMiner();
    vm.stopPrank();
  }
  // claim: sol:@C@TimeMiner@F@tokensToClaim#525:path:3
  // witness-fingerprint-sha256: 51e8f43389f92ad27e5fd5778bb8697e6c50354ef83595cb48256a0e0f068b6d
  function test_cov_0() public {
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129634351);
    vm.prank(address(uint160(0)));
    // [asserted] path exits normally; a revert fails the test
    uint256 _veriput_concrete_return = c0.tokensToClaim(address(uint160(4294967295)));
    assertEq(_veriput_concrete_return, uint256(13000000), "fixed witness return must match");
  }
}

contract TimeMinerCovTest_1 is Test {
  TimeMiner c0;
  function setUp() public {
    vm.warp(1);
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new TimeMiner();
    vm.stopPrank();
  }
  // claim: sol:@C@TimeMiner@F@tokensToClaim#525:path:2
  // witness-fingerprint-sha256: a7ef741ef9c345dceb19ae356e327ba18a318649d46a943a9be0df20d2c6286c
}
