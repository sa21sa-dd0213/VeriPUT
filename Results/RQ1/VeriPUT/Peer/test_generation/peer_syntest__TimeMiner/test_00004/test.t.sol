// SPDX-License-Identifier: MIT
// Auto-generated by VeriPUT after ESBMC emitted no concrete test.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {TimeMiner} from "../src/flat.sol";

contract TimeMinerCovTest_TimeMiner_balanceOf_put2p1 is Test {
  TimeMiner c0;
  function setUp() public {
    c0 = new TimeMiner();
  }
  // claim: sol:@C@TimeMiner@F@balanceOf#398:path:2

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@TimeMiner@F@balanceOf#398:path:2   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'3': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, msg.value, _user
  // ORACLE: rollback exit plus 19 restored post-state frame assertion(s).
  // This path exits through a ROLLBACK revert. The call MUST fail, and
  // after the revert the chain observes restored storage, so frame rungs
  // such as `post == pre` remain executable oracles. Change and return
  // rungs are still dropped because they refer to a pre-rollback moment.
  //   rung DROPPED: all return rungs DROPPED: the `retlive` witness came back HOLDS, not REFUTED -- no execution of this path was shown to reach a return, so every other return rung holds VACUOUSLY
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (and funded with `vm.deal(p_msg_sender, p_msg_value)`, because the call sends value and the sender pays) (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: msg.value in [1, 115792089237316195423570985008687907853269984665640564039457584007913129639935] is ESTABLISHED and FUZZED: the compiler-only nonpayable reject replay was rewritten as a low-level value-bearing call taking the bound() fuzz parameter `p_msg_value`
  // 14 snapshot local(s) HOISTED to storage: 3 fuzz parameter(s) + 14 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_info_admin; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_coinCreationTime; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_coinWorkingTime; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_stableCoinSystem; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_supplydivision; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_supplymultiply; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_totalSupply; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_admin; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_coinCreationTime; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_coinWorkingTime; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_stableCoinSystem; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_supplydivision; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_supplymultiply; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_totalSupply; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, uint256 p_msg_value, address _user) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    p_msg_value = bound(p_msg_value, 1, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    _user = address(uint160(bound(uint256(uint160(_user)), 0, 1461501637330902918203684832716283019655932542975)));
    // pre-state for the oracle, at this path's own entry
    _pre_info_admin = (uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975);
    _pre_info_coinCreationTime = uint256(vm.load(address(c0), bytes32(uint256(7))));
    _pre_info_coinWorkingTime = uint256(vm.load(address(c0), bytes32(uint256(6))));
    _pre_info_stableCoinSystem = (uint256(vm.load(address(c0), bytes32(uint256(5)))) & 255);
    _pre_info_supplydivision = uint256(vm.load(address(c0), bytes32(uint256(3))));
    _pre_info_supplymultiply = uint256(vm.load(address(c0), bytes32(uint256(4))));
    _pre_info_totalSupply = uint256(vm.load(address(c0), bytes32(uint256(0))));
    // VeriPUT synthesized this call from source declarations; the
    // certified region and all oracle claims still come from ESBMC.
    vm.deal(p_msg_sender, p_msg_value);
    vm.prank(p_msg_sender);
    (bool _esbmc_value_gate_ok, ) = address(c0).call{value: p_msg_value}(abi.encodeWithSignature("balanceOf(address)", _user));
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_info_admin = (uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975);
    _post_info_coinCreationTime = uint256(vm.load(address(c0), bytes32(uint256(7))));
    _post_info_coinWorkingTime = uint256(vm.load(address(c0), bytes32(uint256(6))));
    _post_info_stableCoinSystem = (uint256(vm.load(address(c0), bytes32(uint256(5)))) & 255);
    _post_info_supplydivision = uint256(vm.load(address(c0), bytes32(uint256(3))));
    _post_info_supplymultiply = uint256(vm.load(address(c0), bytes32(uint256(4))));
    _post_info_totalSupply = uint256(vm.load(address(c0), bytes32(uint256(0))));
    assertEq(_post_info_admin, _pre_info_admin, "info.admin: post == pre");
    assertEq(_post_info_coinCreationTime, _pre_info_coinCreationTime, "info.coinCreationTime: post == pre");
    assertEq(_post_info_coinWorkingTime, _pre_info_coinWorkingTime, "info.coinWorkingTime: post == pre");
    assertEq(_post_info_stableCoinSystem, _pre_info_stableCoinSystem, "info.stableCoinSystem: post == pre");
    assertEq(_post_info_supplydivision, _pre_info_supplydivision, "info.supplydivision: post == pre");
    assertEq(_post_info_supplymultiply, _pre_info_supplymultiply, "info.supplymultiply: post == pre");
    assertEq(_post_info_totalSupply, _pre_info_totalSupply, "info.totalSupply: post == pre");
    assertGe(_post_info_admin, _pre_info_admin, "info.admin: post >= pre");
    assertLe(_post_info_admin, _pre_info_admin, "info.admin: post <= pre");
    assertGe(_post_info_coinCreationTime, _pre_info_coinCreationTime, "info.coinCreationTime: post >= pre");
    assertLe(_post_info_coinCreationTime, _pre_info_coinCreationTime, "info.coinCreationTime: post <= pre");
    assertGe(_post_info_coinWorkingTime, _pre_info_coinWorkingTime, "info.coinWorkingTime: post >= pre");
    assertLe(_post_info_coinWorkingTime, _pre_info_coinWorkingTime, "info.coinWorkingTime: post <= pre");
    assertGe(_post_info_supplydivision, _pre_info_supplydivision, "info.supplydivision: post >= pre");
    assertLe(_post_info_supplydivision, _pre_info_supplydivision, "info.supplydivision: post <= pre");
    assertGe(_post_info_supplymultiply, _pre_info_supplymultiply, "info.supplymultiply: post >= pre");
    assertLe(_post_info_supplymultiply, _pre_info_supplymultiply, "info.supplymultiply: post <= pre");
    assertGe(_post_info_totalSupply, _pre_info_totalSupply, "info.totalSupply: post >= pre");
    assertLe(_post_info_totalSupply, _pre_info_totalSupply, "info.totalSupply: post <= pre");
    // VERIPUT_ORACLE_REFINEMENT_END
    assertFalse(_esbmc_value_gate_ok, "value sent to a non-payable entry must revert");

    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    // Structural ABI-gate replay: R0 is already asserted over the certified nonzero-value region.
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_TimeMiner_balanceOf_path2p1(address p_msg_sender, uint256 p_msg_value, address _user) public {
    _veriput_parameterized(p_msg_sender, p_msg_value, _user);
  }
}
