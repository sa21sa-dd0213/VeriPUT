// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test, Vm} from "forge-std/Test.sol";
import {AnyswapV5ERC20} from "../src/flat.sol";

contract AnyswapV5ERC20CovTest_AnyswapV5ERC20_Swapout_put31p1_p1_part_part0_w is Test {
  AnyswapV5ERC20 c0;
  function setUp() public {
    c0 = new AnyswapV5ERC20();
  }
  // claim: sol:@C@AnyswapV5ERC20@F@Swapout#726:path:14
  // witness-fingerprint-sha256: f6b05ac5e92fae941637afe6c6367c5907d5e0a5bd0ae518adf14f5685e5fd17
  // claim: sol:@C@AnyswapV5ERC20@F@Swapout#726:path:31
  // witness-fingerprint-sha256: 95a8e33878618f4e5405e0a9b6b8680dfadce461c55de9de03e854fbc4e40671
  // claim: sol:@C@AnyswapV5ERC20@F@Swapout#726:path:30
  // witness-fingerprint-sha256: 7388910c1d84e75efb5b8a4f6aea76ffa4441c8fd5398fc71cfd2827a7356b25
  // claim: sol:@C@AnyswapV5ERC20@F@Swapout#726:path:2
  // witness-fingerprint-sha256: 72c575090bf0088df988c44c9cc70e8478ad240420013f9703eb136cd47777ce

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@AnyswapV5ERC20@F@Swapout#726:path:31   depth=4
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   amount in [0, 0]
  //   bindaddr in [1, 1461501637330902918203684832716283019655932542975]
  //   msg.sender in [1, 2147483647]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'14': 'bindaddr==0, state._vaultOnly==0', '30': 'msg.sender==0, state._vaultOnly==0', '31': 'state._vaultOnly==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = True
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, amount, bindaddr
  // ORACLE: 10 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 9 over POST-STATE, read through vm.load at
  // the slot solc reports, and 1 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): _totalSupply: post >= pre (entailed by a stronger rung that also HOLDS on _totalSupply, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): _totalSupply: post <= pre (entailed by a stronger rung that also HOLDS on _totalSupply, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balanceOf[msg.sender]: post >= pre (entailed by a stronger rung that also HOLDS on balanceOf[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balanceOf[msg.sender]: post <= pre (entailed by a stronger rung that also HOLDS on balanceOf[msg.sender], so asserting it detects nothing the stronger one misses)
  //   path guard ESTABLISHED: `!(!_vaultOnly)` -> `vm.assume(_pre_vaultOnly == 0);`
  //   path guard ESTABLISHED: `!(bindaddr != 0)` -> `vm.assume(uint256(uint160(bindaddr)) != 0);`
  //   path guard ESTABLISHED: `!(account != 0)` -> `vm.assume(uint256(uint160(p_msg_sender)) != 0);`
  //   environment ESTABLISHED: msg.sender in [1, 2147483647] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(1)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, uint256 amount, address bindaddr) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 2147483647)));
    amount = bound(amount, 0, 0);
    bindaddr = address(uint160(bound(uint256(uint160(bindaddr)), 1, 1461501637330902918203684832716283019655932542975)));
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_totalSupply = uint256(vm.load(address(c0), bytes32(uint256(5))));
    uint256 _pre_vaultOnly = ((uint256(vm.load(address(c0), bytes32(uint256(6)))) >> 8) & 255);
    uint256 _pre_balanceOf_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(4)))));
    // complete-path guard recovered from the emit report
    vm.assume(_pre_vaultOnly == 0);
    vm.assume(uint256(uint160(bindaddr)) != 0);
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [asserted] path exits normally; a revert fails the test
    vm.recordLogs();
    bool _put_ret = c0.Swapout(amount, bindaddr);
    Vm.Log[] memory _veriputFixedLogs = vm.getRecordedLogs();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(1)) && amount == uint256(0) && bindaddr == address(uint160(1))) {
      assertEq(_put_ret, true, "fixed witness return");
      assertEq(_veriputFixedLogs.length, 1);
      assertEq(_veriputFixedLogs[0].emitter, address(c0));
      assertEq(_veriputFixedLogs[0].topics.length, 3);
      assertEq(_veriputFixedLogs[0].topics[0], keccak256("LogSwapout(address,address,uint256)"));
      assertEq(_veriputFixedLogs[0].topics[1], bytes32(uint256(uint160(address(uint160(1))))));
      assertEq(_veriputFixedLogs[0].topics[2], bytes32(uint256(uint160(address(uint160(1))))));
      assertEq(_veriputFixedLogs[0].data, abi.encode(uint256(0)));
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(6)))) & 255), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(5)))), uint256(0), "fixed witness state");
      assertEq(((uint256(vm.load(address(c0), bytes32(uint256(6)))) >> 8) & 255), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(1)), uint256(4))))), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(2)))) & 255), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(7)))), uint256(172800), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(16)))), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(12)))), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(14)))), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(15)))), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(11)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(13)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq(((uint256(vm.load(address(c0), bytes32(uint256(2)))) >> 8) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
      assertEq((uint256(vm.load(address(c0), bytes32(uint256(10)))) & 1461501637330902918203684832716283019655932542975), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_totalSupply = uint256(vm.load(address(c0), bytes32(uint256(5))));
    uint256 _post_vaultOnly = ((uint256(vm.load(address(c0), bytes32(uint256(6)))) >> 8) & 255);
    uint256 _post_balanceOf_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(4)))));
    assertEq(_post_totalSupply, _pre_totalSupply, "_totalSupply: post == pre");
    assertEq(_post_vaultOnly, _pre_vaultOnly, "_vaultOnly: post == pre");
    assertEq(_post_balanceOf_msg_sender, _pre_balanceOf_msg_sender, "balanceOf[msg.sender]: post == pre");
    assertGe(_pre_totalSupply, _post_totalSupply, "_totalSupply: pre - post in [amount, amount] with pre >= post");
    unchecked { assertGe(_pre_totalSupply - _post_totalSupply, amount, "_totalSupply: pre - post in [amount, amount] with pre >= post"); }
    unchecked { assertLe(_pre_totalSupply - _post_totalSupply, amount, "_totalSupply: pre - post in [amount, amount] with pre >= post"); }
    assertGe(_pre_balanceOf_msg_sender, _post_balanceOf_msg_sender, "balanceOf[msg.sender]: pre - post in [amount, amount] with pre >= post");
    unchecked { assertGe(_pre_balanceOf_msg_sender - _post_balanceOf_msg_sender, amount, "balanceOf[msg.sender]: pre - post in [amount, amount] with pre >= post"); }
    unchecked { assertLe(_pre_balanceOf_msg_sender - _post_balanceOf_msg_sender, amount, "balanceOf[msg.sender]: pre - post in [amount, amount] with pre >= post"); }
    assertTrue(_put_ret, "return: return == true");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_AnyswapV5ERC20_Swapout_path31p1_part_part0_w(address p_msg_sender, uint256 amount, address bindaddr) public {
    _veriput_parameterized(p_msg_sender, amount, bindaddr);
  }
}
