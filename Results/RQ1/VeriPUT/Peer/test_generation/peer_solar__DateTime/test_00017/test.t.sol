// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test} from "forge-std/Test.sol";
import {DateTime} from "../src/flat.sol";

contract DateTimeCovTest_DateTime_leapYearsBefore_put3p1_p1_part_part0_r is Test {
  DateTime c0;
  function setUp() public {
    c0 = new DateTime();
  }
  // claim: sol:@C@DateTime@F@leapYearsBefore#95:path:3
  // witness-fingerprint-sha256: 774f2bb6976f3ab097271a21512b73a995a6394c6483f25006251378f5c731cb
  // claim: sol:@C@DateTime@F@leapYearsBefore#95:path:2
  // witness-fingerprint-sha256: 0faa804109a9ad6214f8fbba456806a3851d9201a5236f490b61c4324e76a2a7

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@DateTime@F@leapYearsBefore#95:path:3   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   year in [115792089237316195423570985008687907853269984664372913439229354606416426434565, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 0
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 0
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'3': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = True
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, year
  // ORACLE: 3 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 0 over POST-STATE, read through vm.load at
  // the slot solc reports, and 3 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (no prank was present, so one was inserted)
  //   environment ESTABLISHED: block.timestamp == 0 is ESTABLISHED with `vm.warp(0)` before the target call
  //   environment ESTABLISHED: block.number == 0 is ESTABLISHED with `vm.roll(0)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, uint256 year) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    year = bound(year, 115792089237316195423570985008687907853269984664372913439229354606416426434565, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    // [asserted] path exits normally; a revert fails the test
    vm.warp(0);
    vm.roll(0);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    uint256 _put_ret = c0.leapYearsBefore(year);
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(0)) && year == uint256(115792089237316195423570985008687907853269984664372913439229354606416426434565)) {
      assertEq(_put_ret, uint256(28079581640049177390215963864606817654417971281110431509013118492055983410382), "fixed witness return");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    assertGe(uint256(_put_ret), 0, "return: return in [0, 28079581640049177390215963864606817654417971281417836779568464121918933937683]");
    assertLe(uint256(_put_ret), 28079581640049177390215963864606817654417971281417836779568464121918933937683, "return: return in [0, 28079581640049177390215963864606817654417971281417836779568464121918933937683]");
    assertTrue(uint256(_put_ret) != 0, "return: return != 0");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_DateTime_leapYearsBefore_path3p1_part_part0_r(address p_msg_sender, uint256 year) public {
    _veriput_parameterized(p_msg_sender, year);
    // VERIPUT_WITNESS_REPLAY: the retained concrete witness runs on every execution (PUT is strictly stronger than the replay test).
    { DateTimeCovTest_DateTime_leapYearsBefore_concrete3p1__basis_part0_r__W _veriput_w = new DateTimeCovTest_DateTime_leapYearsBefore_concrete3p1__basis_part0_r__W(); _veriput_w.setUp(); _veriput_w._w_test_cov_0(); }
}
}

// ---- retained concrete witness (renamed) ----
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
contract DateTimeCovTest_DateTime_leapYearsBefore_concrete3p1__basis_part0_r__W is Test {
  DateTime c0;
  function setUp() public {
    c0 = new DateTime();
  }
  // claim: sol:@C@DateTime@F@leapYearsBefore#95:path:3
  // witness-fingerprint-sha256: 774f2bb6976f3ab097271a21512b73a995a6394c6483f25006251378f5c731cb
  function _w_test_cov_0() public {
    // VERIPUT_FIXED_REPLAY_STATE_PROJECTION {"state.DAY_IN_SECONDS":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":86400},"state.HOUR_IN_SECONDS":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":3600},"state.LEAP_YEAR_IN_SECONDS":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":31622400},"state.MINUTE_IN_SECONDS":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":60},"state.ORIGIN_YEAR":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":1970},"state.YEAR_IN_SECONDS":{"certificate":"fixed-replay-state-constant-or-immutable/v1","kind":"fixed-replay-state-constant-or-immutable","value":31536000}}
    // [asserted] path exits normally; a revert fails the test
    vm.warp(0);
    vm.roll(0);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(address(uint160(0)), address(uint160(0)));
    uint256 _veriput_concrete_return = c0.leapYearsBefore(uint256(115792089237316195423570985008687907853269984664372913439229354606416426434565));
    assertEq(_veriput_concrete_return, uint256(28079581640049177390215963864606817654417971281110431509013118492055983410382), "fixed witness return must match");
  }
  // claim: sol:@C@DateTime@F@leapYearsBefore#95:path:2
  // witness-fingerprint-sha256: 0faa804109a9ad6214f8fbba456806a3851d9201a5236f490b61c4324e76a2a7
}
