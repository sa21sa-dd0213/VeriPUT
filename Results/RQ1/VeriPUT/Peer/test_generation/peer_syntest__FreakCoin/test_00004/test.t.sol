// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test, Vm} from "forge-std/Test.sol";
import {FreakCoin} from "../src/flat.sol";

contract FreakCoinCovTest_FreakCoin_approve_put3p1 is Test {
  FreakCoin c0;
  function setUp() public {
    vm.startPrank(address(uint160(1)), address(uint160(1)));
    c0 = new FreakCoin();
    vm.stopPrank();
  }
  // claim: sol:@C@FreakCoin@F@approve#313:path:3
  // witness-fingerprint-sha256: c7e1faf2b4ce7e0875f4b90fd85c68d52f13487b72ae7379de9d74005a760387
  // claim: sol:@C@FreakCoin@F@approve#313:path:2
  // witness-fingerprint-sha256: dccce921a46702133fbda505d5c6e849e4d5cd2cd14ac48c7acccd697bcb5270

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@FreakCoin@F@approve#313:path:3   depth=1
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   _spender in [0, 1461501637330902918203684832716283019655932542975]
  //   _tokens in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   msg.sender in [1, 1461501637330902918203684832716283019655932542975]
  //   state.info.admin in [0, 1461501637330902918203684832716283019655932542975]
  //   state.info.anon_pad$1 in [0, 18446744073709551615]
  //   state.info.anon_pad$5 in [0, 1329227995784915872903807060280344575]
  //   state.info.final_supply in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   state.info.initialSetup in [0, 1]
  //   state.info.totalSupply in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   state.info.uniswapV2PairAddress in [0, 1461501637330902918203684832716283019655932542975]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'3': '(none)'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = False
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, _spender, _tokens
  // ORACLE: 6 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 5 over POST-STATE, read through vm.load at
  // the slot solc reports, and 1 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): info.admin: post >= pre (entailed by a stronger rung that also HOLDS on info.admin, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): info.admin: post <= pre (entailed by a stronger rung that also HOLDS on info.admin, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): info.final_supply: post >= pre (entailed by a stronger rung that also HOLDS on info.final_supply, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): info.final_supply: post <= pre (entailed by a stronger rung that also HOLDS on info.final_supply, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): info.totalSupply: post >= pre (entailed by a stronger rung that also HOLDS on info.totalSupply, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): info.totalSupply: post <= pre (entailed by a stronger rung that also HOLDS on info.totalSupply, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): info.uniswapV2PairAddress: post >= pre (entailed by a stronger rung that also HOLDS on info.uniswapV2PairAddress, so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): info.uniswapV2PairAddress: post <= pre (entailed by a stronger rung that also HOLDS on info.uniswapV2PairAddress, so asserting it detects nothing the stronger one misses)
  //   entry-state bound DROPPED: state.info.admin in [0, 1461501637330902918203684832716283019655932542975] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query -- the rungs were proved about the constructor's own value. Establishing a fuzz-chosen value here would test entry states the proof never covered, which is how this PUT came back RED on the unmodified contract. Both endpoints are the type's own limits, so there is not even an in-region check to make: the bound says nothing at all)
  //   entry-state bound DROPPED: state.info.anon_pad$1 (no storage slot: solc's layout does not list it, so it is a constant/immutable and no test can set it)
  //   entry-state bound DROPPED: state.info.anon_pad$5 (no storage slot: solc's layout does not list it, so it is a constant/immutable and no test can set it)
  //   entry-state bound DROPPED: state.info.final_supply in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query -- the rungs were proved about the constructor's own value. Establishing a fuzz-chosen value here would test entry states the proof never covered, which is how this PUT came back RED on the unmodified contract. Both endpoints are the type's own limits, so there is not even an in-region check to make: the bound says nothing at all)
  //   entry-state bound DROPPED: state.info.totalSupply in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query -- the rungs were proved about the constructor's own value. Establishing a fuzz-chosen value here would test entry states the proof never covered, which is how this PUT came back RED on the unmodified contract. Both endpoints are the type's own limits, so there is not even an in-region check to make: the bound says nothing at all)
  //   entry-state bound DROPPED: state.info.uniswapV2PairAddress in [0, 1461501637330902918203684832716283019655932542975] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query -- the rungs were proved about the constructor's own value. Establishing a fuzz-chosen value here would test entry states the proof never covered, which is how this PUT came back RED on the unmodified contract. Both endpoints are the type's own limits, so there is not even an in-region check to make: the bound says nothing at all)
  //   environment ESTABLISHED: msg.sender in [1, 1461501637330902918203684832716283019655932542975] is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it (replacing `vm.prank(address(uint160(0)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  // 10 snapshot local(s) HOISTED to storage: 4 fuzz parameter(s) + 10 snapshot local(s) exceed the 12-slot budget (solc stack reach); the reads and every assertion are unchanged
  uint256 _pre_info_admin; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_final_supply; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_initialSetup; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_totalSupply; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _pre_info_uniswapV2PairAddress; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_admin; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_final_supply; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_initialSetup; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_totalSupply; // VERIPUT_HOISTED_SNAPSHOT
  uint256 _post_info_uniswapV2PairAddress; // VERIPUT_HOISTED_SNAPSHOT
  function _veriput_parameterized(address p_msg_sender, address _spender, uint256 _tokens, uint8 s_info_initialSetup) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 1, 1461501637330902918203684832716283019655932542975)));
    _spender = address(uint160(bound(uint256(uint160(_spender)), 0, 1461501637330902918203684832716283019655932542975)));
    _tokens = bound(_tokens, 0, 115792089237316195423570985008687907853269984665640564039457584007913129639935);
    s_info_initialSetup = uint8(bound(uint256(s_info_initialSetup), 0, 1));
    // entry state the certified region names, ESTABLISHED (not assumed):
    //   state.info.initialSetup := s_info_initialSetup in [0, 1] (SET and FUZZED: freed at the certification entry)
    {
      uint256 _w = uint256(vm.load(address(c0), bytes32(uint256(3))));
      _w = (_w & ~uint256(372682917519380244141939632342652170012262798458880)) | ((uint256(uint256(s_info_initialSetup)) & 255) << 160);
      vm.store(address(c0), bytes32(uint256(3)), bytes32(_w));
    }
    assertLe(((uint256(vm.load(address(c0), bytes32(uint256(3)))) >> 160) & 255), uint256(1), "the entry state is OUTSIDE the certified region: state.info.initialSetup was assumed in [0, 1] when the path was certified, so a value outside it means the assumption was vacuous and the rungs below were proved about no execution this test can reach");
    // pre-state for the oracle, at this path's own entry
    _pre_info_admin = (uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975);
    _pre_info_final_supply = uint256(vm.load(address(c0), bytes32(uint256(4))));
    _pre_info_initialSetup = ((uint256(vm.load(address(c0), bytes32(uint256(3)))) >> 160) & 255);
    _pre_info_totalSupply = uint256(vm.load(address(c0), bytes32(uint256(0))));
    _pre_info_uniswapV2PairAddress = (uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [asserted] path exits normally; a revert fails the test
    vm.recordLogs();
    bool _put_ret = c0.approve(_spender, _tokens);
    Vm.Log[] memory _veriputFixedLogs = vm.getRecordedLogs();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(0)) && _spender == address(uint160(0)) && _tokens == uint256(0) && s_info_initialSetup == uint8(0)) {
      assertEq(_put_ret, true, "fixed witness return");
      assertEq(_veriputFixedLogs.length, 1);
      assertEq(_veriputFixedLogs[0].emitter, address(c0));
      assertEq(_veriputFixedLogs[0].topics.length, 3);
      assertEq(_veriputFixedLogs[0].topics[0], keccak256("Approval(address,address,uint256)"));
      assertEq(_veriputFixedLogs[0].topics[1], bytes32(uint256(uint160(address(uint160(0))))));
      assertEq(_veriputFixedLogs[0].topics[2], bytes32(uint256(uint160(address(uint160(0))))));
      assertEq(_veriputFixedLogs[0].data, abi.encode(uint256(0)));
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    _post_info_admin = (uint256(vm.load(address(c0), bytes32(uint256(2)))) & 1461501637330902918203684832716283019655932542975);
    _post_info_final_supply = uint256(vm.load(address(c0), bytes32(uint256(4))));
    _post_info_initialSetup = ((uint256(vm.load(address(c0), bytes32(uint256(3)))) >> 160) & 255);
    _post_info_totalSupply = uint256(vm.load(address(c0), bytes32(uint256(0))));
    _post_info_uniswapV2PairAddress = (uint256(vm.load(address(c0), bytes32(uint256(3)))) & 1461501637330902918203684832716283019655932542975);
    assertEq(_post_info_admin, _pre_info_admin, "info.admin: post == pre");
    assertEq(_post_info_final_supply, _pre_info_final_supply, "info.final_supply: post == pre");
    assertEq(_post_info_initialSetup, _pre_info_initialSetup, "info.initialSetup: post == pre");
    assertEq(_post_info_totalSupply, _pre_info_totalSupply, "info.totalSupply: post == pre");
    assertEq(_post_info_uniswapV2PairAddress, _pre_info_uniswapV2PairAddress, "info.uniswapV2PairAddress: post == pre");
    assertTrue(_put_ret, "return: return == true");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_FreakCoin_approve_path3p1(address p_msg_sender, address _spender, uint256 _tokens, uint8 s_info_initialSetup) public {
    _veriput_parameterized(p_msg_sender, _spender, _tokens, s_info_initialSetup);
  }
}
