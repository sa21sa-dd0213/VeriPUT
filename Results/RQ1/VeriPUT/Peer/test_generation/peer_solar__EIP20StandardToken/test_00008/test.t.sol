// SPDX-License-Identifier: MIT
// Auto-generated by ESBMC 8.2.0
// Foundry coverage test reconstructed from ESBMC counterexamples.
// Calls marked [revert-tolerant] are wrapped in try/catch: ESBMC could
// not confirm the call's outcome, so an undetected revert (e.g.
// require(cond, CustomError)) does not FAIL the assertion-free replay.
// The wrap does NOT assert a revert; a non-reverting call runs normally.
pragma solidity >=0.8.0;

import {Test, Vm} from "forge-std/Test.sol";
import {EIP20StandardToken} from "../src/flat.sol";

contract EIP20StandardTokenCovTest_EIP20StandardToken_transfer_put7p1 is Test {
  EIP20StandardToken c0;
  function setUp() public {
    c0 = new EIP20StandardToken();
  }
  // claim: sol:@C@EIP20StandardToken@F@transfer#122:path:6
  // witness-fingerprint-sha256: f9a4d82cf060bdfa86fe00f71208751e56c011047e2049822ac3ffc6e2ab9669
  // claim: sol:@C@EIP20StandardToken@F@transfer#122:path:7
  // witness-fingerprint-sha256: a3db62349a11d7619f266fe8ca794314714c43d358e5a81b470687e71ac4e21b
  // claim: sol:@C@EIP20StandardToken@F@transfer#122:path:2
  // witness-fingerprint-sha256: 2b2b04da13b74288d6249a106e5bea98081702159105c0b59f6af2ac8a818b58

  // ===================== PUT (stage 4) =====================
  // claim: sol:@C@EIP20StandardToken@F@transfer#122:path:7   depth=2
  // CELL GATE -- --focus-function at --solidity-max-tx 1, matching the LOCKED branch-coverage baseline, which is measured to run at one transaction. May NOT be quoted as the method's reach
  // CERTIFIED REGION (stage 2), certified by an independent
  // `assume(box); assert(tr == pi)` query, not by the subtraction:
  //   _to in [0, 1461501637330902918203684832716283019655932542975]
  //   _value in [0, 0]
  //   msg.sender in [0, 4294967295]
  //   state.balances$253[msg.sender] in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935]
  //   PIN block.basefee == 0
  //   PIN block.blobbasefee == 0
  //   PIN block.chainid == 0
  //   PIN block.coinbase == 0
  //   PIN block.difficulty == 0
  //   PIN block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN block.prevrandao == 0
  //   PIN block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934
  //   PIN msg.value == 0
  //   PIN tx.gasprice == 0
  // WIDTH PROVENANCE (stage-2 switches for this ROW, not per coordinate):
  //   claim_budget = 0
  //   cut_policy = spec
  //   esbmc_args = ['--overflow-check', '--div-by-zero-check', '--path-cov-arith-resolve', '--unwindsetname', '_ESBMC_alloc_nested_2d:0:16,nondet_string:0:33', '--extcall-nondet']
  //   geometric_bracket = False
  //   level0 = True
  //   level0_perturb = True
  //   level0_points = {'6': '(none)', '7': '_value==0'}
  //   max_holes = 3
  //   max_region_pieces = 4
  //   max_tx = 1
  //   probe_ladder = True
  //   probe_ladder_budget = 8
  //   probes = 8
  //   refine_rounds = 2
  //   region_refinement_used = True
  //   scope = focus
  //   shrink_rounds = 4
  //   sibling_subtraction = True
  //   skip_bracket = True
  // Width sources that ran: subtraction of the sibling paths
  // Arguments the region does NOT bound keep the counterexample's own
  // literal: the region is a statement about THAT slice, and generalising
  // over them would be a claim the certification never made.
  // FUZZ COORDINATES: msg.sender, _to, _value
  // ORACLE: 12 assertion(s) from the surviving (HOLDS) rungs of
  // --path-cov-assert -- 11 over POST-STATE, read through vm.load at
  // the slot solc reports, and 1 over the unit's OWN RETURN
  // VALUE, bound from the call below. A return rung is emitted only when the
  // ladder's `retlive` witness was REFUTED, i.e. only when this path was
  // shown to reach a return at all.
  //   rung IMPLIED (not lost, not asserted twice): balances$253[msg.sender]: post >= pre (entailed by a stronger rung that also HOLDS on balances$253[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balances$253[msg.sender]: post <= pre (entailed by a stronger rung that also HOLDS on balances$253[msg.sender], so asserting it detects nothing the stronger one misses)
  //   rung IMPLIED (not lost, not asserted twice): balances$253[msg.sender]: post == (pre + _value) (entailed by a stronger rung that also HOLDS on balances$253[msg.sender], so asserting it detects nothing the stronger one misses)
  //   path guard ESTABLISHED: `!(balances[msg.sender] >= _value)` -> `vm.assume(_pre_balances_msg_sender >= 0);`
  //   entry-state bound DROPPED: state.balances$253[msg.sender] in [0, 115792089237316195423570985008687907853269984665640564039457584007913129639935] (width > 1, DROPPED: the entry state is not havoc'd, so this bound constrained nothing in the query; both endpoints are the type's own limits, so there is not even an in-region check to make)
  //   environment ESTABLISHED: msg.sender in [0, 4294967295]  \ {0} is ESTABLISHED and FUZZED: the governing vm.prank now takes the bound() fuzz parameter `p_msg_sender`, so this PUT ranges over the certified sender interval rather than over one point of it, and the 1 punched value(s) are excluded by vm.assume; 0 is excluded because Foundry cannot prank address(0), and the remaining executable subregion is a subset of the certified region (replacing `vm.prank(address(uint160(4294967295)));`)
  //   environment ESTABLISHED: block.timestamp == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.number == 115792089237316195423570985008687907853269984665640564039457584007913129639934 is ESTABLISHED with `vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934)` before the target call
  //   environment ESTABLISHED: block.chainid == 0 is ESTABLISHED with `vm.chainId(0)` before the target call
  //   environment ESTABLISHED: block.basefee == 0 is ESTABLISHED with `vm.fee(0)` before the target call
  //   environment ESTABLISHED: block.blobbasefee == 0 is ESTABLISHED with `vm.blobBaseFee(0)` before the target call
  //   environment ESTABLISHED: block.difficulty is established by the matching block.prevrandao cheatcode
  //   environment ESTABLISHED: block.prevrandao == 0 is ESTABLISHED with `vm.prevrandao(uint256(0))` before the target call
  //   environment ESTABLISHED: tx.gasprice == 0 is ESTABLISHED with `vm.txGasPrice(0)` before the target call
  //   environment ESTABLISHED: block.coinbase == 0 is ESTABLISHED with `vm.coinbase(address(uint160(0)))` before the target call
  function _veriput_parameterized(address p_msg_sender, address _to, uint256 _value) internal {
    p_msg_sender = address(uint160(bound(uint256(uint160(p_msg_sender)), 0, 4294967295)));
    vm.assume(uint256(uint160(p_msg_sender)) != 0);
    _to = address(uint160(bound(uint256(uint160(_to)), 0, 1461501637330902918203684832716283019655932542975)));
    _value = bound(_value, 0, 0);
    // pre-state for the oracle, at this path's own entry
    uint256 _pre_balances_253_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    uint256 _pre_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    uint256 _pre_balances__to = uint256(vm.load(address(c0), keccak256(abi.encode(_to, uint256(1)))));
    // complete-path guard recovered from the emit report
    vm.assume(_pre_balances_msg_sender >= 0);
    vm.warp(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.roll(115792089237316195423570985008687907853269984665640564039457584007913129639934);
    vm.chainId(0);
    vm.fee(0);
    vm.blobBaseFee(0);
    vm.prevrandao(uint256(0));
    vm.txGasPrice(0);
    vm.coinbase(address(uint160(0)));
    vm.prank(p_msg_sender);
    // [asserted] path exits normally; a revert fails the test
    vm.recordLogs();
    bool _put_ret = c0.transfer(_to, _value);
    Vm.Log[] memory _veriputFixedLogs = vm.getRecordedLogs();
    // VERIPUT_FIXED_REPLAY_ASSERTIONS
    if (p_msg_sender == address(uint160(4294967295)) && _to == address(uint160(4294967295)) && _value == uint256(0)) {
      assertEq(_put_ret, true, "fixed witness return");
      assertEq(_veriputFixedLogs.length, 1);
      assertEq(_veriputFixedLogs[0].emitter, address(c0));
      assertEq(_veriputFixedLogs[0].topics.length, 3);
      assertEq(_veriputFixedLogs[0].topics[0], keccak256("Transfer(address,address,uint256)"));
      assertEq(_veriputFixedLogs[0].topics[1], bytes32(uint256(uint160(address(uint160(4294967295))))));
      assertEq(_veriputFixedLogs[0].topics[2], bytes32(uint256(uint160(address(uint160(4294967295))))));
      assertEq(_veriputFixedLogs[0].data, abi.encode(uint256(0)));
      assertEq(uint256(vm.load(address(c0), keccak256(abi.encode(address(uint160(4294967295)), uint256(1))))), uint256(0), "fixed witness state");
      assertEq(uint256(vm.load(address(c0), bytes32(uint256(0)))), uint256(0), "fixed witness state");
    }
    // VERIPUT_ORACLE_REFINEMENT_BEGIN
    uint256 _post_balances_253_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    uint256 _post_balances_msg_sender = uint256(vm.load(address(c0), keccak256(abi.encode(p_msg_sender, uint256(1)))));
    uint256 _post_balances__to = uint256(vm.load(address(c0), keccak256(abi.encode(_to, uint256(1)))));
    assertEq(_post_balances_253_msg_sender, _pre_balances_253_msg_sender, "balances$253[msg.sender]: post == pre");
    assertGe(_pre_balances_msg_sender, _post_balances_msg_sender, "balances[msg.sender]: pre - post in [_value, _value] with pre >= post");
    unchecked { assertGe(_pre_balances_msg_sender - _post_balances_msg_sender, _value, "balances[msg.sender]: pre - post in [_value, _value] with pre >= post"); }
    unchecked { assertLe(_pre_balances_msg_sender - _post_balances_msg_sender, _value, "balances[msg.sender]: pre - post in [_value, _value] with pre >= post"); }
    assertGe(_post_balances__to, _pre_balances__to, "balances[_to]: post - pre in [_value, _value] with post >= pre");
    unchecked { assertGe(_post_balances__to - _pre_balances__to, _value, "balances[_to]: post - pre in [_value, _value] with post >= pre"); }
    unchecked { assertLe(_post_balances__to - _pre_balances__to, _value, "balances[_to]: post - pre in [_value, _value] with post >= pre"); }
    assertGe(_post_balances_253_msg_sender, 0, "balances$253[msg.sender]: post in [0, pre]");
    assertLe(_post_balances_253_msg_sender, _pre_balances_253_msg_sender, "balances$253[msg.sender]: post in [0, pre]");
    assertGe(_post_balances_253_msg_sender, 0, "balances$253[msg.sender]: post in [0, (_value + 115792089237316195423570985008687907853269984665640564039457584007913129639935)]");
    unchecked { assertLe(_post_balances_253_msg_sender, (uint256(_value) + uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)), "balances$253[msg.sender]: post in [0, (_value + 115792089237316195423570985008687907853269984665640564039457584007913129639935)]"); }
    assertTrue(_put_ret, "return: return == true");
    // VERIPUT_ORACLE_REFINEMENT_END
  }


  // Final RQ1 PUT: parameterized execution plus the retained fixed executions.
  function test_put_EIP20StandardToken_transfer_path7p1(address p_msg_sender, address _to, uint256 _value) public {
    _veriput_parameterized(p_msg_sender, _to, _value);
  }
}
